# Known limitations and follow-up task breakdown

This is the single place tracking both **accepted constraints** (things
that aren't going to change — inherent DDS/CRTMNU behavior, or a
reasonable default already in place) and **genuinely actionable follow-up
work**, for both the DSPF (screen) designer and the Menu designer. This
used to be split across README's own "Known limitations"/"Planned
enhancements" sections and this file; README now just links here. DSPF
designer tasks are prefixed `L`; Menu designer tasks are prefixed `M` so
the two sets can never collide on an ID even though they're tracked in
one doc.

Not every accepted-constraint bullet below has a matching task — see
[Known limitations (accepted constraints)](#known-limitations-accepted-constraints)
for those. Only genuinely fixable gaps are tracked as tasks in the
sections below that.

Update the Status column (`not started` / `in progress` / `done`) when you
pick up or finish a task so parallel sessions don't collide, and check
`git fetch` / `git log --oneline main..FETCH_HEAD` before every push —
upstream drift between parallel sessions is the main risk here, same as
it was for the picker screens.

---

## Known limitations (accepted constraints)

These are accepted constraints or inherent DDS/CRTMNU behaviors, not open
work — nothing below is tracked as a task. See the sections after this
one for genuinely fixable, tracked work.

### DSPF (screen) designer

- `WINDOW` positions that depend on a runtime value (`*DFT`, or a
  program-to-system field name) can't be known at design time, so they
  render at a fixed placeholder position with a dashed border instead
  (staggered per-window in compare mode so multiple placeholders don't
  overlap). `WINDOW(record-format-name)` (inheriting another record's
  geometry) is fully resolved, as is every other `WINDOW`/`WDWBORDER`
  form.
- `CHCCTL` (per-choice runtime field-setting logic) has no visual
  representation - it's a logic construct, not a layout one.
- Deleting a named field that something else in the source looks like it
  references by name (e.g. `REFFLD`) is blocked on a confirmation dialog
  naming those lines, but confirming still doesn't rewrite the reference
  itself - there's nothing sensible to auto-fix it TO (same reasoning as
  rename's own limitation below).
- `EDTCDE(Y)`/`EDTCDE(W)` "date edit" codes are left at the field's coded
  length rather than a guessed display width, since their separator
  width depends on the job's runtime `DATSEP` attribute - not knowable
  at design time, the same ambiguity that keeps `WINDOW(*DFT)` a
  placeholder above. Every other `EDTCDE`/`EDTWRD` case gets an exact
  computed width.
- M/P (Message text/Program-to-system) field usages aren't covered by
  real SDA's own field-keyword "For Field Type" table, so their keyword
  panels fail open (show every category) by design rather than guessing
  which apply.
- Choice selection type (`SNGCHCFLD`/`MLTCHCFLD`), Choice keywords, and
  Choice colors & attributes stay constant-excluded, since they require
  real, named, indicator-controlled field semantics a constant
  structurally can't have.
- The real SDA `WINDOW` screen's "Roll" column isn't a DDS keyword at
  all - it turned out to be SDA's own in-terminal roll-key editing
  convenience, so there's nothing to model.

### Menu designer

- **Compile Menu (CRTMNU)** requires the DDS record format to be named
  exactly the same as the menu member - CRTMNU's own requirement, not an
  iSDA choice.
- No command-key (`CAxx`/`CFxx`) assignment UI, unlike the DSPF designer -
  CRTMNU-compiled numbered-option menus don't use them in practice (F3=Exit,
  F12=Cancel etc. are handled by CRTMNU's own generated program logic, not
  by DDS command keys the menu designer would let you assign).
- **Compile Menu only ever produces `TYPE(*DSPF)` menus - by design, not
  as a gap to fill.** `CRTMNU` also supports `TYPE(*PGM)` (calls a program
  directly, with no display file or screen at all - nothing for a visual
  DDS screen designer to design) and `TYPE(*UIM)` (written in UIM's own
  tag-based panel-group language, compiled via `CRTPNLGRP`, a completely
  different, non-DDS markup that would need its own dedicated designer
  rather than an extension of this one). `TYPE(*DSPF)` is the only menu
  type backed by an actual DDS-designed screen, which is why it's the
  only one real SDA's own "Menu design" mode - and this DSPF/MNUDDS-
  focused tool - ever produces.

## Planned enhancements (tracked tasks)

Forward-looking, fixable work. Every currently-tracked DSPF designer task
(L1 through L55, including L5d-ii) and every currently-tracked menu
designer task (M1 through M8) is **done** — see the task tables below for
the full history if a past decision needs revisiting. The P series (below,
new feature work rather than a bug fix) is deliberately **not started** -
deprioritized until after the L/M-series bug-fix backlog is fully
stabilized. If a new limitation surfaces, add it as a new task continuing
the `L`/`M` numbering rather than folding it into an existing row.

## High priority

Broad user-facing impact — either a common DDS pattern the pickers can't
model at all today, or a data-integrity risk.

| Task | Description | Depends on | Status |
| --- | --- | --- | --- |
| **L1** | **Multi-instance conditioned keywords — foundation.** Every dedicated picker manages ONE instance of its keyword(s) at a time, conditioned as a whole via the generic keyword editor's Conditioning toggle. Real DDS allows MULTIPLE independently-conditioned instances of the same keyword (e.g. `COLOR(RED)` under indicator 10 and `COLOR(GRN)` under indicator 20 on the same field). Build a generic, reusable "repeatable conditioned instance" UI component that any picker panel can wrap around its `getX`/`setX` pair — same shape as Task R3's `INDTXT`/`SETOF`/`CHANGE` repeatable-row list on the SFL picker (already a working precedent for "multiple instances, one indicator each, add/remove rows"), generalized so it isn't SFL-specific. This is the foundation task; L1a/L1b/L1c below wire it into specific panels. | — (design + build the component itself; no picker changes yet) | **done** — `DspfWriter.getRepeatableKeywordInstances`/`setRepeatableKeywordInstances` (full per-instance conditioning, not just a bare indicator) + `WebviewClientHelpers.repeatableConditionedInstancesHtml`/`wireRepeatableConditionedInstances` (repeatable list + Conditioning accordion + add/remove, caller-supplied payload). See CHANGELOG `[0.9.55]` and `src/test/repeatableConditionedInstances.test.js`. L1a/L1b/L1c can now be picked up independently. |
| **L1a** | Wire L1's component into the **Color & attributes** picker (`COLOR`/`DSPATR`) — the single most common multi-instance case in real DDS (state-dependent field styling). | L1 | **done** — `DspfWriter.getColorAttrStates`/`setColorAttrStates` (positionally pairs same-condition COLOR/DSPATR instances into states rather than merging/losing data on a signature collision) + `WebviewClientHelpers.colorAttrStatesHtml`/`wireColorAttrStatesEditor`, wired into the field/constant props panel. Along the way, L1's own "+ Add" mechanism was corrected from "append a blank instance and commit it" (which silently evaporates on the next re-render for a payload that can be entirely empty) to a permanently-visible staging row read on Add — see `renderStaging`/`readNewInstance` in `repeatableConditionedInstancesHtml`/`wireRepeatableConditionedInstances` (both are optional trailing params; every existing caller that doesn't pass them - including L1b's own `makeDefaultInstance`-style usage and L1c's `makeDefaultSflMsg`/`makeDefaultSflMsgId` below, all added independently around the same time - keeps working exactly as before). See CHANGELOG `[0.9.55]`, `src/test/colorAttrStates.test.js`, and the updated Color & attributes scenario in `src/test/dspfWebview.test.js`. |
| **L1b** | Wire L1's component into the **Error message** picker (`ERRMSG`/`ERRMSGID`) — several message/condition pairs tried in order. Done — replaced the old single-instance "Error message" text box (in the Validity check panel) with a new, correctly-visibility-gated "Error messages" accordion built on L1's `repeatableConditionedInstancesHtml`/`wireRepeatableConditionedInstances`. `DspfWriter.getErrorMessageInstances`/`setErrorMessageInstances` read/write both `ERRMSG('text' [respInd])` and `ERRMSGID(msgid [library/]msgfile [respInd] [&msgdata])` as one merged, ordered list (verified against IBM's DDS Reference V4R5, ERRMSG/ERRMSGID keyword section, Figure 174, and the real SDA "Define Error Messages" screen `screens/field-level/character/error-messages/image171.png`) - note library/msgfile is written as ONE slash-qualified token, not the two separate space-separated tokens the old code would have guessed at. Along the way, fixed a real visibility bug: ERRMSG/ERRMSGID are valid on Output-only fields too (IBM's own rule), not just Input-or-Both like the neighboring Validity check keywords they used to share a gate with - they now have their own `errorMessages` visibility flag. Also worked around a genuine UX gap in L1's own generic component (independently rediscovered and properly fixed on the L1a side above via `renderStaging`/`readNewInstance` - both approaches coexist safely since the new params are optional/backward compatible): since it commits on every field change immediately (no batch Apply), a freshly-added or kind-switched instance with blank required fields would otherwise vanish again on the very next re-render (its own setX correctly drops incomplete entries to avoid writing malformed DDS) - fixed here by seeding a non-blank placeholder (`'New message'`, or `MSGID`/`MSGFILE`) so the row survives until the user overwrites it. See CHANGELOG and `src/test/dspfWriter.test.js`/`src/test/dspfWebview.test.js` (Task L1b sections). | L1 | done |
| **L1c** | Wire L1's component into the **Subfile Messages** panel (`SFLMSG`/`SFLMSGID`) on the SFLCTL picker (Task R4). | L1, R4 (already done) | **done** — `sflCtlPanelsHtml`/`wireSflCtlPanels` in `webviewClientHelpers.js` now wire L1's generic component into SFLMSG and SFLMSGID as two SEPARATE repeatable groups (not paired the way Color & attributes pairs COLOR+DSPATR - real DDS lets each of SFLMSG/SFLMSGID repeat independently, per this doc's own L1 entry). Replaced the old single-primary-instance `getSflMsgId`/`setSflMsgId` with per-instance `parseSflMsgIdParams`/`formatSflMsgIdParams` in `dspfWriter.js` (an incomplete SFLMSGID - blank message ID or file - is still never committed, same guarantee the superseded functions gave). Also factored `quoteDdsLiteral`/`unquoteDdsLiteral` out of `getFileQuotedText`/`setFileQuotedText` so SFLMSG's per-instance quoting reuses the exact same convention. Uses the same non-blank-placeholder default (`'New message'`, `MSGID`/`MSGFILE`) L1b's own `makeDefaultInstance`-style approach uses, rather than L1a's newer staging-row mechanism - both are backward-compatible, optional-param approaches on the same shared component, so either is a valid pattern for a future picker to follow. See the Task L1c scenarios in `src/test/dspfWebview.test.js` and CHANGELOG. |
| **L1d** | Wire L1's component into the **Keying options** picker (`CHECK`'s ME/ER/MF/FE/RB/RZ/RL/LC codes). | L1 | **done** — `CHECK` is shared between TWO UI panels (Keying options: ME/ER/MF/FE/RB/RZ/RL/LC; Validity check: AB/VN/VNE/M10/M11 + M10F/M11F immediate variants), both reading/writing the SAME keyword - converting only one to multi-instance would have been a real data-loss bug (the untouched panel's old single-merged-instance setter would collapse every instance back into one on its very next edit), so BOTH panels were converted together via one new shared pair, `checkInstancesHtml`/`wireCheckInstancesEditor` in `webviewClientHelpers.js`, parameterized by which code subset each panel owns. Each panel only ever reads/writes its OWN codes within an instance, always re-reading (never caching) the other panel's codes at commit time - verified end-to-end in `src/test/dspfWebview.test.js` (checking ME via Keying options, then AB via Validity check on that SAME instance, correctly merges to `CHECK(ME AB)` on one keyword; adding a second instance from either panel correctly leaves the first alone). Replaced `dspfWriter.js`'s old single-primary-instance `getCheckOptions`/`setCheckOptions` with `parseCheckCodes`/`formatCheckCodes`, used directly with Task L1's own `getRepeatableKeywordInstances`/`setRepeatableKeywordInstances` (CHECK's payload is just its raw code list already - no dedicated instance-shape wrapper needed, unlike ERRMSG/ERRMSGID's Task L1b). Each panel seeds its own non-blank placeholder on "+ Add" (Keying options: `ME`; Validity check: `AB`) - same trap every other L1-based picker's `makeDefaultInstance` already guards against. `KEYBRD` (the other Keying-options keyword) was deliberately left single-instance - see Known limitations in the README. |
| **L2** | **Delete-field reference cleanup.** Done — deleting a field with likely references elsewhere (the same advisory `findLikelyNameReferences` scan rename falls back on) is now blocked on an actionable confirmation dialog FIRST (naming the reference count/lines and warning they'll be left dangling) rather than deleting immediately and only warning afterward via a passive toast. Confirming still leaves the references unrewritten — there's still no sensible auto-fix target, same as rename's own limitation — so this is the "actionable prompt" option from the two named in the original task description, not the "auto-remove/comment-out" one (blindly rewriting an arbitrary keyword's free-text parameters from a substring match risked corrupting valid DDS worse than leaving it for the person to review). A field with no detected references still deletes immediately, unchanged — no confirmation click added to the common case. New generic `showConfirmDialog` UI helper in `buildWebviewTemplate.js` (a DOM-built modal, not `window.confirm`, to match the app's theme and avoid blocking the whole webview process) — scoped to field deletion only per this task; record deletion (`commitDeleteRecord`) has the identical gap but is out of scope here. See `runDeleteWarningScenario` in `dspfWebview.test.js`. | — (standalone) | done |
| **L3** | **`MNUBARCHC` Text field / Return field variants.** Done — `DspfEngine.parseMenubarChoice` now recognizes a `&text-field` reference as an alternative to the literal-text form, plus an optional trailing `&return-field` token (both verified against IBM's own MNUBARCHC keyword reference, Figures 213/214, and the real SDA screen `screens/field-level/menu-bar-choice/choice-keyword/image193.png`). `DspfWriter.getMenubarChoices`/`setMenubarChoices` carry `returnField` through symmetrically (writer already half-supported `&text-field` on write; this closes the read-side gap and adds the return field on both sides). The picker's MNUBARCHC row editor deliberately collapses SDA's separate "Text field"/"Text" entries into the SAME text box - typing `&NAME` there is a field reference, anything else a literal - matching the codebase's existing `&`-prefix convention for the sibling `CHOICE` keyword, plus a new "Return field" box. See `src/test/dspfEngine.test.js`, `src/test/dspfWriter.test.js`, and `src/test/dspfWebview.test.js` (Task L3 sections) and CHANGELOG. | D5 (already done — this extends its existing choice picker) | done |
| **L5** | **Extend L1's repeatable-conditioned-instance component to the remaining single-instance pickers - and, for panels where that turned out to be the wrong fix, surface per-keyword conditioning the simpler way instead.** L5 itself (the umbrella row) is split into four independently-workable pieces below — **L5a (Input keywords), L5b (General keywords), L5c (Database reference), and L5d (the record-level pickers)**. L1d already covered one of L5's originally-named pieces (Keying options' `CHECK` codes, and — since `CHECK` is shared — Validity check's own `CHECK` codes too); Validity check's OWN validity keyword (`RANGE`/`COMP`/`VALUES`, unrelated to `CHECK`) — `DspfWriter.getValidityCheckInstances`/`setValidityCheckInstances` (built on L1's foundation; no positional pairing needed, unlike L1a's COLOR+DSPATR, since RANGE/COMP/VALUES are mutually exclusive alternative keyword names rather than two keywords combined into one state) + `WebviewClientHelpers.validityCheckInstancesHtml`/`wireValidityCheckInstances`, following L1b's ERRMSG-style per-row "kind" selector — and Message ID (`MSGID` — real DDS commonly carries MULTIPLE independently-conditioned `MSGID` keywords on one field, e.g. `MSGID(&MIC001 HISLIB/HISMSGF)` under a response indicator alongside an unconditioned fallback `MSGID(*NONE)`; `getMessageIdInstances`/`setMessageIdInstances` are thin wrappers over L1's own foundation, since MSGID's argument text needed no further decomposition) are now also done, the same general way. See `src/test/validityCheckInstances.test.js` and the Task L5 scenarios in `src/test/dspfWebview.test.js`. | L1 (done) | **done** — Keying options / CHECK (via L1d), Validity check's RANGE/COMP/VALUES, and Message ID all done; L5a/L5b/L5c/L5d (including its own L5d-i/L5d-ii split) all done — every L5 piece is now closed. |
| **L5a** | **L5 piece: Input keywords panel** — done, but NOT via L1's multi-instance component. Checked the real SDA screen (`screens/field-level/character/input-keywords/image167.png`): `DUP`/`BLANKS`/`CHANGE`/`CHGINPDFT` each get exactly ONE "Resp" indicator slot on SDA's own screen, not a repeatable list — plain presence flags with no different "state" to switch between, so a single occurrence's own AND/OR indicator expression already covers everything real DDS allows; L1's component would have been the wrong fix here. What the panel actually lacked was the simpler per-keyword Conditioning toggle the parallel "Surface the per-keyword Conditioning toggle" effort (see M1's sibling task, and commit `d363f88`) had just landed everywhere else. `WebviewClientHelpers.inputKeywordsHtml`/`wireInputKeywordsEditor` rewritten onto `flagRowHtml`/`wireFlagRow`, reusing `DspfWriter.getFileFlagKeyword`/`setFileFlagKeyword` directly (no new writer functions needed — a presence flag needs no dedicated parsing); each keyword now commits immediately and independently, replacing the old batch-Apply-button flow. Old `getInputKeywords`/`setInputKeywords` kept for backward compatibility. See the Task L5 scenarios in `src/test/dspfWebview.test.js`. | L1 (done) | done |
| **L5b** | **L5 piece: General keywords panel** — done, same fix and same reasoning as L5a (checked against the panel's own text-bearing keywords — `ALIAS`/`INDTXT`/`DFT`/`DFTVAL`/`FLDCSRPRG`/`HLPID` — plus its boolean flags `PUTRETAIN`/`OVRDTA`/`OVRATR`/`CHRID`/`IGCALTTYP`/`NOCCSID`; none carry a different VALUE per conditioned state, so `flagRowHtml`'s checkbox+optional-param-box shape fits every row here too). `WebviewClientHelpers.generalFieldKeywordsHtml`/`wireGeneralFieldKeywordsEditor` rewritten the same way as L5a, replacing the old batch-Apply-button flow with immediate per-keyword commits. Old `getGeneralFieldKeywords`/`setGeneralFieldKeywords` kept for backward compatibility. See the Task L5 scenarios in `src/test/dspfWebview.test.js`. | L1 (done) | done |
| **L5c** | **L5 piece: Database reference panel** — done, same fix and same reasoning as L5a (checked against the real SDA screen `screens/field-level/character/database-reference/image170.png`: `DLTCHK`/`DLTEDT` each get exactly ONE "Resp" slot, same as L5a's keywords). `WebviewClientHelpers.referenceOverridesHtml`/`wireReferenceOverridesEditor` rewritten onto `flagRowHtml`/`wireFlagRow`. Old `getReferenceOverrides`/`setReferenceOverrides` kept for backward compatibility. See the Task L5 scenarios in `src/test/dspfWebview.test.js`. | L1 (done) | done |
| **L5d** | **L5 piece: the record-level pickers** onto L1's repeatable-conditioned-instance component — the broadest of the four remaining pieces (spans multiple record-level panels, not one keyword family). Split into L5d-i/L5d-ii below, the same way L5 itself was split into L5a-d. | L1 (done) | **done** — L5d-i and L5d-ii both done (see their own rows below); L5d itself needs no further work. |
| **L5d-i** | **L5d piece: record-level Indicator / screen-control keywords panel** (`CLEAR`/`PAGEDOWN`/`PAGEUP`/`HOME`/`HELP`/`HLPRTN`/`VLDCMDKEY`/`SETOF`/`CHANGE`/`INDTXT`). Done — checked the real SDA "Define Indicator Keywords" screen for a plain record (`screens/record-level/base-record-keywords/indicator/image19.png`) and for a SFLCTL record (`.../indicator/image41.png`): unlike L5a/L5b/L5c's own keywords, this screen genuinely IS a repeatable row table (`Keyword` / `Indicators+` / `Resp` / `Text`, add as many rows as needed, even multiple rows of the SAME keyword under different indicators) — so L1's component WAS the right fit here, the opposite finding from L5a/b/c. `CFnn`/`CAnn` appear on that same real screen too but were deliberately left out — they already have their own dedicated Command keys panel elsewhere in the record's properties. New `DspfWriter.getRecordIndicatorInstances`/`setRecordIndicatorInstances` (built on L1's foundation; `resp`/`text` kept as separate instance fields rather than one opaque `parameters` string, since only `INDTXT` among these ten keywords carries a text component) + `WebviewClientHelpers.recordIndicatorInstanceRowHtml`/`recordIndicatorInstancesHtml`/`wireRecordIndicatorInstances`, following L5's own ERRMSG/validity-check "kind selector per row" pattern. Replaces the old one-`flagRowHtml`-per-keyword treatment on the base Record Keywords panel's Indicator tab, AND replaces the too-narrow `indicatorTextRowsHtml` (`INDTXT`/`SETOF`/`CHANGE` only — Task R3's own SFL/SFLMSG/PDNSFLCTL screen, confirmed narrower via `.../indicator/image33.png` and `window-subfile-wndsfl/indicator/image85.png`) on the SFLCTL picker's own Indicator tab, since a SFLCTL record's real screen shows the SAME fuller keyword set as a plain record's. SFL/SFLMSG/PDNSFLCTL's own Indicator tabs are untouched — their real screens genuinely only offer `INDTXT`/`SETOF`/`CHANGE`, so R3's existing component remains correct there. See `src/test/recordIndicatorInstances.test.js` and the Task L5d scenarios in `src/test/dspfWebview.test.js`. | L1 (done) | done |
| **L5d-ii** | **L5d piece: any remaining record-level panels not covered by L5d-i** (e.g. the base Record Keywords panel's other categories — App help, Help, Output, Input, Overlay, Print — and any other record-type-specific picker not yet checked). Not yet surveyed — **check each keyword's own real SDA screenshot first** (per L5a/b/c's and L5d-i's own findings above) before assuming L1's component is the right fit; several may turn out to be single-Resp-indicator flags that just need `flagRowHtml` instead. | L1 (done) | **done** — surveyed all 6 remaining categories (Help, Output, Input, Overlay, Print were already checked as part of General earlier and confirmed fine) against their real SDA screenshots (`screens/record-level/base-record-keywords/{application-help,help,output,input,overlay,print}/`): Help/Output/Input/Overlay/Print all genuinely are single-Resp-indicator-slot flags, correctly already modeled via `flagRowHtml` — no changes needed there. **Application Help was a real bug, not a modeling-style question**: real SDA's own "Define Application Help" screen shows a "Help number: N of M / Next help number" cycling header that turned out to matter — `HLPPNLGRP`/`HLPEXCLD`/`HLPBDY`/`HLPARA` are Help-SPECIFICATION-level keywords (DDS's own separate "H" line-type, distinct from record-level keywords, confirmed against IBM's DDS Reference), and a record can carry SEVERAL help specifications, each with its own full HLPPNLGRP/HLPEXCLD/HLPBDY/HLPARA group — not a single occurrence shared by the whole record. This codebase's own parser (`dspfParser.ts`'s `nameTypeFor` -> `'HELP'`) and writer (`dspfWriter.js`'s H-spec serialization) already modeled `record.helpEntries[]` correctly, and an existing "Help entries" list/select UI (`helpEntriesListHtml`, `renderHelpProps`) already let a person add/select/delete individual help specifications - but the record-level "Application help" tab was reading/writing HLPPNLGRP/HLPEXCLD/HLPBDY/HLPARA from the RECORD's own top-level `keywords`, which can never be where real DDS puts them; every commit through that tab was writing keywords that don't mean what the picker implied. Fix: removed that record-level tab entirely (from both `WebviewClientHelpers.recordKeywordsPanelsHtml`/`wireRecordKeywordsPanels` and `buildWebviewTemplate.js`'s `rkTabs` list, for both the plain-record and USRDFN-narrowed variants) and added new `WebviewClientHelpers.applicationHelpFieldsHtml`/`wireApplicationHelpFields` (thin `flagRowHtml`/`wireFlagRow` wrappers, same shape L5a/b/c already established, just pointed at an arbitrary keywords array instead of a record's) wired into `renderHelpProps` so the dedicated fields now correctly target the SELECTED help entry's own `keywords`, with the raw keyword editor still available below for anything else an H specification might carry. See the Task L5d-ii scenarios in `src/test/dspfWebview.test.js` (`runApplicationHelpScenario`) and the updated `runUsrDfnPickerScenario` (narrowed subset is now General/Help/Print, 3-of-7, not 4-of-8). |
| **L6** | **`WINDOW` picker's "Message line" row.** Real SDA's `WINDOW` screen has a "Message line" row with no confirmed DDS keyword behind it in this codebase yet — currently left out of the `WINDOW`-specific picker, reachable only via the raw Keywords editor. First verify the actual keyword (check `docs/sda-reference/screens/record-level/window/` reference screenshots and IBM's DDS Reference for what SDA writes when that row is set — likely `WDWMSGLIN`/`WDWMSGREC` but confirm the exact syntax/args before implementing anything), THEN add it to the picker if confirmed. If no real keyword is found (i.e. it turns out to be a display-only SDA convenience with no DDS output), downgrade this to a README "Known limitations" note instead of implementing a picker for it — don't guess at keyword syntax and ship it unverified. | — (research first, implementation second) | **done** — confirmed against IBM's own DDS Reference (`WINDOW`'s formal syntax is `WINDOW(... window-lines window-positions [*MSGLIN\|*NOMSGLIN] [*RSTCSR\|*NORSTCSR])`, not `WDWMSGLIN`/`WDWMSGREC` — those names were this task's own placeholder guesses, correctly left unimplemented pending verification) and cross-checked against this codebase's own `dspfEngine.js#resolveWindow`, which ALREADY reads the identical trailing `*NOMSGLIN` token to decide whether a rendered window reserves its own last line for messages — i.e. the grid-rendering side (untouched, per this codebase's own sacred-grid constraint) already understood this keyword correctly; only the `WINDOW`-specific picker's reader/writer had never modeled it. `DspfWriter.getWindowParamsKeyword`/`setWindowParamsKeyword` now read/write `msgLine` (boolean, defaulting to `true` — `*MSGLIN` is IBM's own documented default, so a "Yes" message line never needs to WRITE a token, only "No" appends `*NOMSGLIN`) alongside the existing reference/sized/positioned mode fields. Along the way, fixed a real pre-existing gap this shared token-parsing surfaced: a WINDOW keyword already carrying a trailing `*MSGLIN`/`*NOMSGLIN` token used to be misclassified into the picker's `'other'` (unrecognized-shape) catch-all, since the old code required an EXACT 3-or-4-token count with nothing else present — the trailing option token is now stripped out before the mode-detection checks run, for both forms. Not offered on the bare "Referenced window" form (`WINDOW(record-name)`), which has no room for the token and always inherits the referenced window's own setting instead — same point `resolveWindow`'s own code comment already made. New "Message line" checkbox on the Window Parameters panel (`webviewClientHelpers.js`'s `windowPanelsHtml`/`wireWindowPanels`), hidden when "Referenced window" mode is selected, committed via the same "Apply window parameters" button as the geometry fields (not an independent immediate-commit flag row, since it's part of the same `WINDOW` keyword's own parameter list). Discovered along the way (documented, not fixed here — see Task L7 below): the picker's existing "Restrict cursor to window" checkbox models `RSTCSR` as its own bogus standalone keyword rather than this same `WINDOW` keyword's own trailing `*RSTCSR`/`*NORSTCSR` sub-parameter. See the new Task L6 scenarios in `src/test/dspfWebview.test.js`'s `runWindowPickerScenario`. |
| **L7** | **`WINDOW`-specific picker's "Restrict cursor to window" checkbox models `RSTCSR` as a bogus standalone keyword, not `WINDOW`'s own trailing `*RSTCSR`/`*NORSTCSR` sub-parameter.** Done — confirmed against IBM's own DDS Reference: unlike `PULLDOWN`'s own `*SLTIND`/`*RSTCSR` sub-flags (Task R10, an unrelated keyword this task doesn't touch), plain `WINDOW`'s trailing cursor-restriction token defaults to `*RSTCSR` (restricted), the opposite default `PULLDOWN` uses. `DspfWriter.getWindowParamsKeyword`/`setWindowParamsKeyword` (Task L6's own functions) now also read/write `rstcsr` (boolean, default `true`) from/to the SAME optional trailing token pair `*MSGLIN`/`*NOMSGLIN` already occupies, written in IBM's documented order (`*MSGLIN`/`*NOMSGLIN` before `*RSTCSR`/`*NORSTCSR`) — so only the non-default `*NORSTCSR` case ever needs writing, same "no-op when it's the default" shape `msgLine` already has. The picker's Window Parameters panel gained a second checkbox next to Message line; the old standalone-keyword flag row (`DspfWriter.getFileFlagKeyword`/`setFileFlagKeyword(keywords, 'RSTCSR', ...)`) is gone. Self-healing: `setWindowParamsKeyword` now unconditionally strips any leftover standalone `RSTCSR` line on every commit (that line was never valid DDS - any file carrying one, written by this app's own earlier R7 picker, would already fail to compile), regardless of whether the caller's own state even mentions `rstcsr` - the getter deliberately does NOT consult that legacy line's presence/absence either, since it never had a real, salvageable DDS meaning to begin with; the true trailing token (or its documented default) is the only source of truth going forward. `runWindowPickerScenario` (and the WNDSFCTL combination scenario, Task R9) updated/extended in `dspfWebview.test.js` to cover the corrected default, the toggle-both-tokens-together ordering, the *NORSTCSR pre-fill case, and the self-heal-on-commit behavior. | — (standalone; touches the same `WINDOW` keyword Task L6 just extended) | done |
| **L8** | **`Compile Display File` (`CRTDSPF`) command — the DSPF designer has no compile action at all**, unlike the Menu designer's own "Compile Menu (CRTMNU)" button (`dspfDesigner.compileMenu`, `extension.ts`'s `compileMenu()`) — noticed while answering a user question, not from README's own lists (this gap isn't mentioned in either "Known limitations" or "Planned enhancements" there yet). Done — ported `compileMenu()`'s own pattern (`code-for-ibmi.runCommand` API, `resourceScheme == member` guard via `parseMemberUri`, save-dirty-editor-first, `vscode.window.withProgress` + surfaced-verbatim `CommandResult.stderr`/`stdout` on failure) into a new `compileDspf()`, scoped down to the single step `CRTDSPF FILE(library/objectName) SRCFILE(library/srcFile) SRCMBR(member) REPLACE(*YES)` — no message-file/`ADDMSGD`/`CRTMNU` steps, since those are `MNUDDS`-specific and N/A for a plain `DSPF` member. Unlike `CRTMNU`'s own hard record-format-name-matching requirement (which `compileMenu()` checks up front), plain `CRTDSPF` has no equivalent naming constraint to pre-validate, so `compileDspf()` skips straight to the compile. New command `dspfDesigner.compileDspf`, contributed the same two ways `compileMenu` is (`commandPalette` with `resourceScheme == member`; a matching "Compile Display File (CRTDSPF)" button — `compileDspfBtn` — added to the DSPF designer webview's own File section in `buildWebviewTemplate.js`, right next to "File attributes", wired via a new `'compileDspf'` postMessage case in `DspfDesignerEditorProvider`'s message handler). New `.compile-btn` CSS rule added to `buildWebviewTemplate.js` (previously only defined in `buildMenuWebviewTemplate.js`). See `src/test/compileDspf.test.js`, the updated `registers all commands`/`compileDspf message` checks in `src/test/extension.test.js`, and the new Task L8 check in `src/test/dspfWebview.test.js`. | — (standalone; ports the existing `compileMenu()` pattern to a plain DSPF member) | done |

## Medium priority

Real but narrower-impact gaps — worth doing, lower urgency than the High
tier above.

| Task | Description | Depends on | Status |
| --- | --- | --- | --- |
| **L4** | **`CRTSRCPF` support in "Create New Display File."** Done — the remote-path wizard now checks whether the source physical file exists (`CHKOBJ`) before running `ADDPFM`, and if it doesn't, offers to create it (`CRTSRCPF`) via a confirmation prompt naming the file, rather than letting `ADDPFM` fail with a raw CPF error. Declining is a silent cancel (same as every other prompt in this flow); if `CRTSRCPF` itself fails, that failure is surfaced and `ADDPFM` is never attempted. `RCDLEN` is left to `CRTSRCPF`'s own default (`*SRC`/112, the standard DDS source PF record length) rather than hardcoded. Scoped to the DSPF designer's "Create New Display File" only, per this task; "Create New Menu" has the identical gap on its own remote path but is tracked separately as Task M2 below. See the new scenarios in `src/test/createNewDspf.test.js`. | — (standalone) | done |
| **L9** | **"Create New Display File" always starts from a fixed plain-`RECORD` boilerplate — no way to start from a subfile, window, pull-down menu, etc.** The designer's own "+ Add record" wizard already has full-fidelity starter templates for all 9 real-SDA record types (`WebviewClientHelpers.RECORD_TYPES`/`buildTypedRecordPlan`), but that logic never ran at file-creation time. Done — extracted `buildTypedRecordPlan`/`missingDependentMessage` out of `buildWebviewTemplate.js`'s embedded client script into `webviewClientHelpers.js` as plain exported functions (added to the `dist/` copy step in `package.json` alongside `dspfEngine.js`/`dspfWriter.js`/`mnuCmdEngine.js`), so the extension host can require the exact same decision table instead of a second hand-maintained copy. `promptForRecordInfo()` in `extension.ts` gained a `showQuickPick` record-type step (using `RECORD_TYPES`) between the file name and record name prompts; new `buildTypedBoilerplateDspf()` reuses `DspfWriter.insertTypedRecord`/`insertTypedRecordWithDependent`/`insertField` — the same primitives the in-designer wizard calls — against an initially-empty parsed model. Deliberately stays a fast, few-question flow rather than full wizard parity: SFL-family types (SFL/SFLMSG/WDWSFL/PDNSFL) auto-generate their SFLCTL companion name (new `defaultSflctlName()` helper) instead of prompting for one, and SFLMSG takes the wizard's own defaults (line 24, `MSGKEY`/`PGMQ` fields, no 276-byte queue) — all changeable afterward in the designer like any other record/field, same as picking the wrong type entirely and adjusting. **Follow-up (same session): the starter content itself was upgraded from a bare keyword skeleton to a genuinely working worked example per type** — deliberately kept OUT of the shared `buildTypedRecordPlan` table (which the in-designer "+ Add record" wizard also uses, where a full illustrative example would be unwanted clutter on an already-considered screen) and added instead in `buildTypedBoilerplateDspf` itself, which is Create-New-Display-File-only: SFL-family list types' SFLCTL companion now gets the keyword set a subfile actually needs to display at runtime (`SFLSIZ`/`SFLPAG`/`SFLDSP`/`SFLDSPCTL`/`SFLCLR` — left for the user to add via the wizard's own panels, appropriate there but not for a brand-new file) plus `'Opt'`/`'Description'` column headers, and the detail record gets a numbered `OPTN` option field (the standard SDA 1=Select/2=Change/4=Delete convention) ahead of the sample field (SFLMSG gets the SFLCTL additions but no headers/OPTN/field — it needs neither); `PULDWN` gets a real `SNGCHCFLD`/`CHOICE` selection field (three sample choices) instead of a title+field that wouldn't suit a standalone dropdown; `MNUBAR` keeps its title but gets one real `MNUBARCHC` field wired to a NEW, auto-created `PULDWN` companion record (`deriveCompanionName`, `'P1'` suffix) carrying that same sample dropdown content, so opening the designer shows an actual working "File" menu-bar item, not a bare unwired keyword. See the new record-type scenarios in `src/test/createNewDspf.test.js`. | — (standalone; reuses the existing "+ Add record" wizard's own decision table) | done |
| **L10** | **Multi-field select + block move/copy/delete/style — real SDA parity, not a new invention.** Verified against IBM's own SDA reference manual (Chapter 2, "SDA Work Screen Functions"): the Design Image work screen has a documented block convention — `- -` marks the top-left corner of a rectangular block of fields, `= =` marks where the block goes, moving (or, with the right variant, copying) every field inside that block together in one action. iSDA has no equivalent today — `selectedKey` (`buildWebviewTemplate.js`) is a single value; there is no multi-select at all, so the nudge/cut/copy/paste work just shipped only ever acts on one field. Build: shift-click (or ctrl-click) to add a field to the selection, and/or a rubber-band drag on empty canvas to select every field whose bounds fall inside it; extend `startGroupDrag` (already exists for SFLPAG multi-row preview dragging — same underlying "move N fields by the same delta" mechanism, just a different trigger) to cover an arbitrary multi-select group, not only same-record preview rows; extend arrow-key nudge, Delete, and the new Ctrl+X/C/V to operate over the whole selection instead of `selectedKey` alone; extend the Style (Color & attributes) picker to apply to every selected field at once when more than one is selected. Verify field-by-field against `DspfWriter.copyField`/`deleteField`/`applyFieldUpdate` for whether each already accepts a field list or needs a `*Fields` plural sibling (`deleteFields` already exists — check whether `copyFields`/a bulk `applyFieldUpdate` variant needs adding). **Follow-up (later session): the Style (Color & attributes) piece of this task was reported as a bug** — "when I do multi select and change the color attribute, existing color and attributes are removed and newly selected added." Confirmed: `renderMultiFieldProps`'s Style editor built its target state once from the PRIMARY (first-selected) field's own color/attrs, then stamped that SAME full state onto every OTHER selected field via `DspfWriter.setColorAttrStates`, discarding whatever DIFFERENT color/attrs those other fields already had (verified with a repro: FLD1=`COLOR(BLU)`, FLD2=`COLOR(GRN)`, FLD3=no color — just checking one new `DSPATR(HI)` checkbox on FLD1 left all three fields with FLD1's own `COLOR(BLU)`, wiping FLD2's green and inventing a color FLD3 never had). This was actually the row above's own documented design ("extend the Style... picker to apply to every selected field at once"), but the person's stated preference on being asked was to preserve each field's own existing color/attrs and only merge in what was actually changed, rather than uniform-replace. Fixed with two new `DspfWriter` functions: `diffColorAttrStates(oldStates, newStates)` turns the primary field's one before/after edit (the editor's own `onChange` always fires for exactly one change at a time — one color, one attribute checkbox, one "+ Add", or one "Remove") into a small structured diff of what changed; `applyColorAttrStatesDiff(keywords, diff)` replays that diff onto a DIFFERENT field's own keywords, merging into (or removing from) whatever state IT already has under the same conditions instead of overwriting the whole thing. The primary field itself still gets its own directly-edited keywords verbatim (no need to replay a diff onto the field it came from); every other selected field gets the diff replayed. A diff shape the function can't confidently interpret (list length changed by more than one entry in a single edit — shouldn't happen via the UI, but not assumed) returns `null` and the caller falls back to the old uniform-replace rather than guessing. See the new direct unit coverage in `src/test/colorAttrStates.test.js` and the end-to-end `runPreserveOwnStyleScenario`/`runColorChangeVariant` in `src/test/multiSelect.test.js`. | L1 (component precedent), the nudge/cut/copy/paste work (same session) | done |
| **L11** | **Ruler overlay (row/column numbers along the design canvas).** Done — added `DspfEngine.renderRulerColumnsHtml`/`renderRulerRowsHtml` (a two-row tens/ones column ruler and 2-digit-per-line row ruler, both driven off the currently-resolved screen's own `lines`/`columns` so they track whichever `DSPSIZ`/`selectedSizeIndex` is active) plus a new `.ruler-wrap` CSS grid around `#screenOutput` in `buildWebviewTemplate.js` (corner/column-ruler/row-ruler/screen laid out via plain DOM-order grid auto-placement, no explicit `grid-row`/`grid-column` needed) and a session-only "Show ruler (row/column numbers)" toggle next to `compareModeToggle` (`rulerEnabled`, never persisted, matching real SDA's own F14 starting off each time). `updateRuler(screen)` is called from every `render()`/`renderFullOverlay()` path (success, error, no-records, full-overlay) so the ruler tracks the screen preview. `.dspf-screen`'s own `getBoundingClientRect()`-based drag/placement math (`gridMetrics()`) is untouched since it queries `.dspf-screen` directly regardless of the new wrapper nesting. See the new `runRulerScenario()` in `src/test/dspfWebview.test.js`. | — (standalone; purely additive UI, no `dspfWriter.js`/model changes) | done |
| **L12** | **Align-center shortcut for a field/constant's text on its own line.** ~~Confirmed missing~~ — **correction, before any work started: this was a search-grep miss, not a real gap.** Grepped for `align`/`center`-named code and found nothing, but the actual button is named `p-center` ("Center on screen") - it already exists, in the field props panel's Position tab, right next to Line/Column, since before this brainstorm began. It computes the centered column from the field/constant's own current text width against `lastScreen.columns` (already `DSPSIZ`-aware - `lastScreen` is resolved with `selectedSizeIndex` baked in, the same width Task L11's ruler needed too) and populates the Column input - one click, then the same "Apply changes" commit every other props-panel edit already uses. That two-step shape (populate, then commit) isn't a lesser version of real SDA's own `AC` command either - typing `AC` and pressing Enter on the real Design Image screen is exactly the same two logical steps. Already covered by a dedicated test (`src/test/dspfWebview.test.js`, "Center on screen: a constant", asserting the exact midpoint column for an 80-col screen). The only genuinely open sliver from this task's original scope is centering a BLOCK of multiple selected fields together at once (real SDA's own `AC` can apply per-line across a block) - that's not worth its own task ID; fold it into Task L10 once multi-select lands there instead, rather than tracking a near-empty task here. **Follow-up (later session): that sliver was reported back as a bug** ("align options like top/left/right/center aren't available under field attributes") - true in the narrow sense that Left/Right/Top/Bottom/Distribute all shipped with L10's own Align section (`renderMultiFieldProps` in `buildWebviewTemplate.js`) but Center never actually got added when L10 landed, despite this row saying it would be folded in. Fixed: a new "Center on screen" button in that same Align section computes the SELECTION's combined bounding box (leftmost left edge to rightmost right edge, using the same `lastScreen`-resolved occupied-width lookup the existing align buttons already use for right/bottom edges) and centers that box against the current screen width (`lastScreen.columns`, the same `DSPSIZ`-aware source `p-center` itself reads) - then shifts every selected field by that one shared delta, so the block translates as a single unit and keeps every field's position relative to the others (deliberately NOT each field centering itself independently, which would collapse the block onto one column instead of moving it). See the new `runCenterGroupScenario` in `src/test/multiSelect.test.js`. | — | **done** — reclassified; the button already existed, see above; the one remaining sliver (block-centering) is now also done, see follow-up |
| **L13** | **Record/file-level DDS comment lines have no UI** — real SDA's "Edit comments" (option 2 on Work with Display Records, per-record) and F15 (file-level comments) let you attach documentation text stored as plain DDS comment lines (position 7 = `*`), separate from actual keywords. Confirmed the parser already handles these correctly and they already survive round-trips untouched (`dspfParser.ts` collects them into a `comments[]` array keyed by source line, and since this codebase only ever makes targeted line edits - never full-file regeneration - an existing comment line is never at risk of being silently dropped) - the gap is purely that nothing in `buildWebviewTemplate.js` ever surfaces that array: no way to VIEW or ADD a comment through the designer itself, only by dropping into the raw source editor. Build: a small "Comments" panel (file-level props, alongside file-level keywords; and record-level props, alongside the record's own keywords) listing existing comment lines for that scope and letting new ones be added/edited/deleted, writing plain `*`-prefixed lines via `DspfWriter` the same targeted-line-edit way every other commit already works. | — (standalone; parser-side support already exists, this is UI-only) | **done** — `dspfParser.ts`'s comment collection was also quietly fixed along the way: it used to push plain BLANK filler lines into `comments[]` right alongside real `*`-flagged ones (indistinguishable, both `{line, text: ''}`), which would have surfaced as confusing empty "ghost" rows in the new panel - blank lines are no longer collected at all (parsing behavior is unchanged, only the tracking). New `DspfWriter.getFileComments`/`getRecordComments` scope the flat, file-wide array: file-level = everything before the FIRST record's own header line (the same "preamble" area file keywords like `DSPSIZ` live in); record-level = from that record's own header line up to (but not including) the NEXT record's header line, or EOF for the last record - deliberately wider than `getFullRecordLineRange` (which stops at the last field), since a comment trailing after the last field but before the next record's header still reads as belonging to THIS record. New `addComment`/`updateComment`/`deleteComment` do the actual line-level CRUD (a comment is always exactly one physical line - DDS's `+`/`-` continuation never applies to a comment line). New shared `commentsListHtml`/`wireCommentsSection` in `buildWebviewTemplate.js` (list of editable text rows + delete buttons + an "+ Add comment" button, same visual shape as the existing Hidden-fields section) wired into a new "Comments" tab on the file-level props panel and appended to the record-level props panel's existing "Structure" tab. New comments default to landing right after the scope's own header (file: line 0/very top; record: right before the first field, the same spot `insertField` itself defaults to for a record with no fields yet) when the scope has none yet, else right after the LAST existing one - the same "always appended at the end of what's already there" placement rule `insertField` uses. See the new Task L13 scenario in `src/test/dspfWebview.test.js` and the new comment-CRUD block in `src/test/dspfWriter.test.js`. Scoped to the DSPF designer only (per the `L`-prefix convention) - the menu designer's own file/record props panels don't get this; raise a new `M`-prefixed task if that's wanted. |
| **L14** | **Bulk "Add fields from database file" — lowest priority of this batch.** Real SDA's Design Image screen has an F10 (Database) key: prompts for a file/library/record format, then lets you pick MULTIPLE fields from that file's field list at once and places them on the screen together as REFFLD-based fields (IBM's own manual devotes a full chapter, "Placing Database Fields on the Design", to this workflow). iSDA has something adjacent but narrower: "Resolve Referenced Field via Code for i" (`resolveReferenceTarget`/`fetchReferencedFieldAttributes` in `extension.ts`) only resolves attributes for ONE field the person has already manually created with `REFFLD` set - there's no way to browse a PF/LF's field list and multi-select several fields to auto-create at once. Real productivity gap for the common case of building a screen against an existing database file, but scoped last here since it needs new Code for IBM i round-trips (listing a file's fields, not just resolving one already-named field) and a multi-select picker UI of its own, both bigger lifts than L10-L13. Build: a "+ Add fields from database file" action (record props, near "+ Add field") that prompts for library/file/record format (reusing whatever `code-for-ibmi` API the existing single-field resolver already calls), lists the record format's fields with checkboxes, and on confirm creates one new REFFLD-based field per checked field, stacked down the screen from the current cursor/last-field position (same placement convention `insertField` already uses elsewhere) - each individually draggable/editable afterward like any other field. | — (standalone; extends the existing Code for IBM i integration, doesn't replace it) | **done** — a "+ Fields from database file" button (aside panel, next to "+ Field"/"+ Constant") opens a two-step overlay: library/file inputs -> "List fields" (round-trips to the new `handleListDatabaseFields`/`fetchDatabaseFileFields` in `extension.ts`, which runs `DSPFFD ... OUTPUT(*OUTFILE)` then queries every field ordered by `WHFLDO` - the file's own natural field order) -> a checkbox list (name, length/type/decimals, description text) with "Select all" -> "Add fields" posts the checked ones straight back (no second DSPFFD round-trip - the attributes were already fetched for the list). `handleAddFieldsFromDatabase` creates one new `REFFLD`-based field per selection via `DspfWriter.insertField`, stacked one screen row below the previous (starting one row below the record's own current lowest field), re-parsing the model after each insert - same "never trust a stale record/field reference after an edit" discipline `handleResolveReferencedField` already uses - and name collisions get a fresh suffixed name via the existing `nextAvailableFieldName`. The `mapDspffdRowToAttributes` char-vs-numeric interpretation logic was factored out of `fetchReferencedFieldAttributes` so both functions share it rather than duplicating it. See `runDatabaseFieldsPickerScenario` (webview UI plumbing) in `src/test/dspfWebview.test.js` and the new `extension.test.js` scenario (`listDatabaseFields`/`addFieldsFromDatabase`, including the not-installed/collision/empty-selection cases). **Follow-up fix (same-day gap analysis):** the initial version's `WHFLDO`-only ordering was wrong for a multi-format file (a logical file with more than one record format) - `WHFLDO` only orders correctly WITHIN one format, since it's a per-format sequence, not file-wide, so a multi-format file's fields would have interleaved/misordered results with no indication which format each field belonged to. `fetchDatabaseFileFields` now takes an optional `recordFormat`; when omitted and the file turns out to have more than one distinct `WHNAME` (DSPFFD's own record-format-name column), it returns `{ formats: [...] }` instead of guessing - the webview renders that as a clickable format-picker step before the field checklist, then re-requests scoped to the picked format (`WHERE WHNAME = ...`). Single-format files (the common case - most REFFLD targets are physical files, which structurally can only ever have one format) are completely unaffected. New scenarios in both `extension.test.js` (the multi-format `{ formats }` response and the scoped re-request) and `dspfWebview.test.js` (the format-picker UI step). |
| **L15** | **`MNUBARCHC` picker: the "text" input was effectively unusable** — reported as "unable to type the text column" and the choice text ('File'/'EDIT' etc.) not showing in the picker at all, screenshotted against a real menu-bar record with two existing `MNUBARCHC` choices. Root cause found by rendering the actual generated webview script in jsdom and computing the row's real layout, not by inspecting markup alone: the row packed id(36px)+record(110px)+text(flex:1)+return-field(130px) onto ONE flex line, with id/record/return-field pinned to `flex-shrink:0` (an EARLIER fix, so THEY wouldn't clip - see `.choice-row input`'s own CSS comment) — which meant the text box alone absorbed 100% of any space shortfall. The properties panel's own DEFAULT width (300px, minus 16px padding each side = 268px available) is already narrower than id+record+return-field's combined fixed footprint (276px) BEFORE the text box gets any width at all, so it computed a negative flex-basis, clamped to ~0px: invisible and unclickable, in the DEFAULT layout, not just an unusually narrow custom-resized one. Fixed by restructuring `menuBarChoiceRowHtml` into the same multi-line "block" layout `choiceKeywordRowHtml` (the sibling CHOICE keyword editor) already uses for the identical class of problem: id+pulldown-record share a compact top line (record now `flex:1` too, so it also can't clip), and text/return-field each get their own full-width line below instead of competing for the one flexible slot. Verified via jsdom: focus/typing/Apply all round-trip correctly post-fix (`MNUBARCHC(1 PULLFILE 'Hello World')` written back exactly as typed), for both existing rows and freshly-added ones. Also confirmed (per the same review) that all 4 real DDS `MNUBARCHC` parameters — choice-id, pulldown record, text-or-`&field`, optional `&return-field` — were already correctly modeled end-to-end (`getMenubarChoices`/`setMenubarChoices`); this was purely a CSS layout bug, no data/parameter gap. See the new full-width-line regression checks in `src/test/dspfWebview.test.js` (Task L3's own scenario, extended) and the corrected `.choice-row input` CSS comment/test description in `buildWebviewTemplate.js`/`pulldownOverlayStacking.test.js`. | — (standalone; CSS/layout fix, no keyword/parameter changes) | done |
| **L16** | **System-value constants (`*DATE`/`*TIME`/`*USER`/`*SYSTEM`(`SYSNAME`)/`*PAGNBR`) were actively corrupted by editing, and couldn't be created at all** — reported as "editing or adding them is not correctly handled." Confirmed against IBM's own DDS Reference: these are real field-level keywords (`DATE`/`TIME`/`USER`/`SYSNAME`/`PAGNBR`) applied to an UNNAMED constant field (DDS's own "constant (unnamed) fields require only a location and a keyword" rule) - the system fills in the current date/time/user/system-name/page-number at runtime instead of a literal. `dspfParser.ts`/`DspfEngine` already modeled this correctly as a `CONSTANT` with `constantValue: null` plus the relevant keyword, and `DspfEngine.fieldDisplayText` already rendered a live design-time placeholder for 4 of the 5 - the bugs were entirely in the picker (`buildWebviewTemplate.js`'s `renderFieldProps`/`renderPlacementProps`). Two real bugs found: (1) `isSystemValueConstant` only checked for `DATE`/`TIME`/`PAGNBR`, silently missing `USER` and `SYSNAME` - so a `*USER`/`*SYSTEM` field fell through to the plain-literal-text code path; (2) that fallback path is actively destructive - it always sends `updates.constantValue` from the (blank, since there's no literal) Text input on EVERY "Apply changes" click regardless of what was actually changed, and `DspfWriter.buildFunctionAreaText` writes any non-null `constantValue` as a quoted literal regardless of what keywords are present - so simply opening a `*USER` field's props and clicking Apply for an unrelated reason (e.g. just to nudge its position) silently corrupted it into invalid DDS carrying BOTH an empty `''` literal AND the `USER` keyword on the same line. Separately, "+ Add constant" had no way to create one of these at all - literal text was required. Fixed: `isSystemValueConstant` now recognizes all 5 keywords; the Basic tab shows a dedicated "System value" dropdown (not a Text input) for these, and the Apply handler never touches `constantValue` for them - switching the dropdown replaces whichever system-value keyword was there with the newly chosen one instead. "Center on screen" (which read the now-absent Text input for these) now uses `DspfEngine.displayLength` (newly exported - a pure calculation function, doesn't touch grid rendering) for its width instead. "+ Add constant" gained a "System value instead of literal text" checkbox that swaps in the same dropdown. Also fixed in passing: `DspfEngine.fieldDisplayText`'s design-time placeholder was missing `PAGNBR` (fell through to blank) - now shows `'1'`. Testing surfaced one more real, related bug this same fix needed to work at all: `commitEdit`'s post-commit reselect logic matched by `field.name` only, which every CONSTANT (system-value or plain literal) lacks entirely - so selection silently dropped to nothing after EVERY edit to ANY constant, not just a system-value one, forcing a re-click each time. Fixed by falling back to a `sourceLine` match for unnamed fields (safe since `applyFieldUpdate` only ever rewrites a field's own line(s) in place, never shifting others), routed through the existing `setSingleSelection` helper (Task L10) rather than assigning `selectedKey` directly, which is what actually keeps `selectedKeys` in sync too. See the new Task L16 scenario in `src/test/dspfWebview.test.js`. | — (standalone; picker-only fix, no parser/writer model changes needed) | done |
| **L17** | **`DSPATR` referencing a hidden "program-to-system" field (`USAGE(P)`) wasn't shown in the Color & attributes panel, and got silently dropped on the next edit** — reported as "DSPATR can have parameter as hidden variables... not displaying it correctly in right panel in attributes section." Confirmed against real SDA's own "Select Display Attributes" screen (`docs/sda-reference/screens/field-level/character/display-attributes/image162.png`, user-supplied): real DDS lets a `DSPATR` keyword's parameter be EITHER one or more of the literal attribute codes (`HI`/`RI`/`CS`/`BL`/`ND`/`UL`/`PC`/`MDT`/`PR`/`OID`/`SP`) OR the name of a hidden field (`USAGE(P)`, a "program-to-system" field) whose one-byte runtime value drives the attribute instead — the real screen shows this as its own "Program-to-system field" entry, separate from and above the attribute checkboxes. `WebviewClientHelpers.colorAttrStatesHtml`/`wireColorAttrStatesEditor` (the picker actually wired into the field/constant props panel) never modeled this second form: a hidden field's name matched no checkbox, so it never rendered anywhere in the panel, and `commit()` only ever rebuilt `attrs` from the checked checkboxes - so touching ANYTHING else in that same panel (e.g. toggling an unrelated attribute) silently dropped the hidden field name from the written DDS. Fixed: new `splitAttrsAndPgmField()` in `webviewClientHelpers.js` separates a state's `attrs` into the known checkbox codes vs. a single non-standard token (the hidden field's name), and a new "Program-to-system field" text input (matching the real screen's own label) now renders pre-filled with that name above the checkboxes, in both the multi-state picker (`colorAttrStatesHtml`/`wireColorAttrStatesEditor`) and the older always-unconditioned single-pair editor (`colorAttrEditorHtml`/`wireColorAttrEditor`, kept for API completeness). Every commit path (existing-card edit, the "+ Add" staging row, and the legacy editor) now reads this input back and folds it into the written `DSPATR(...)` parameters alongside any checked codes, so it survives edits instead of evaporating. No `dspfWriter.js`/model changes needed - `getColorAttr`/`getColorAttrStates` already passed every whitespace-separated `DSPATR` token straight through unfiltered; the gap was entirely in the picker not knowing what to do with an unrecognized one. See the new `src/test/colorAttrPgmField.test.js`. | — (standalone; picker-only fix, no parser/writer model changes needed) | done |
| **L18** | **No upfront Code for i connection status.** Found by direct user report (labeled "D" in the source conversation, not sourced from README's own lists - same "found outside README" situation L8/M6 were in): right now the only way to learn Code for i isn't connected is by clicking Compile/Resolve Referenced Field/Add fields from database and getting an error - after the time spent on activation-timing bugs in Code for i integration (see `getConnectedCodeForIBMi`'s own doc comment on lazy activation), a small upfront "IBM i: Connected/Not connected/Not installed" badge in the panel would let people know before they click whether those buttons will work. | — (standalone; reuses the existing `getConnectedCodeForIBMi` pattern) | **done** — new `getCodeForIStatus()` in `extension.ts` (a thin sibling of `getConnectedCodeForIBMi()`, not a rename - callers of the original just want "usable connection or undefined," while the badge specifically needs to distinguish "not installed" from "installed but not connected") pushes a `codeForIStatus` message to both designer panels on `'ready'` (so the badge is populated before anyone clicks anything), right after every Code-for-i-dependent action (Resolve Referenced Field, Add fields from database, Compile Display File, Compile Menu), on a cheap 10-second poll while the panel is open (no round trip to the IBM i itself - just an extension-registry/connection-object check), and on `vscode.extensions.onDidChange` (catches Code for i being installed/uninstalled mid-session); all cleaned up on panel dispose. Webview side (`buildWebviewTemplate.js`/`buildMenuWebviewTemplate.js`) is purely a display for that message - a small badge at the top of the left panel reading "IBM i: connected"/"not connected"/"not installed", styled with `--chrome-accent`/`--warn`/`--ink-dim` respectively (never `--accent`, which stays grid-only). See the new `runCodeForIBadgeScenario` in `src/test/dspfWebview.test.js`, the new Task L18 block in `src/test/extension.test.js`, and CHANGELOG. |
| **L19** | **No search/jump-to-field.** Found by direct user report (labeled "E" in the source conversation): for a screen with many fields there's no way to quickly locate one by name - you have to scan visually or scroll the field list. | — (standalone; DSPF designer only, per the `L`-prefix convention) | **done** — a new "Find field" search box in the DSPF designer's left panel filters every record's fields/constants by name, live as-you-type (case-insensitive substring match), showing a floating results dropdown that names the record when a match isn't the one currently shown. Picking a result (click, or Enter/arrow-keys for keyboard-only use) switches records if needed via `recordSelect.value` + `render()` (which already preserves a still-valid selection - see `rebuildRecordSelect` in `webviewClientHelpers.js`), selects the field the same way every other jump-by-`sourceLine` flow already does (`setSingleSelection` then `render()`, which re-applies the `.selected` canvas class AND re-renders the props panel in one pass), then scrolls/centers the now-rendered `.dspf-field[data-source-line="..."]` element into view. Escape closes the dropdown; an empty query or no matches are both handled explicitly (closed vs. an empty-state row) rather than left ambiguous. Scoped to the DSPF designer only - the menu designer's own option list is short enough that a search box wouldn't earn its keep the way it does for a screen with dozens of fields. See the new `runFieldSearchScenario` in `src/test/dspfWebview.test.js` and CHANGELOG. |
| **L20** | **CNTFLD not selectable in the properties panel.** Found by direct user report ("I don't find CNTFLD in right panel for selection"): `CNTFLD` was entirely missing from `GENERAL_FIELD_KEYWORD_ROWS` in `webviewClientHelpers.js` (the "General keywords" accordion in the field/constant properties panel), so there was no way to ADD or EDIT it from the UI at all - it could only exist on a field if it was already present in the raw DDS source, imported un-editably. Root cause: a stale doc comment on `getGeneralFieldKeywords` claimed CNTFLD "already has its own concerns elsewhere - handled by dspfEngine.js's continued-entry preview," conflating two different concerns - `cntfldFromKeywords` in `dspfEngine.js` (see L17-adjacent work on its own conditioning fix) correctly READS the keyword for the wrap-preview rendering, but nothing anywhere offered it for editing. | — (standalone; found during a spot-check of the L18/L19 delivery) | **done** — added a `cntfld` row to `GENERAL_FIELD_KEYWORD_ROWS`, positioned between `DFTVAL` and `FLDCSRPRG` to match real SDA's own "Select General Keywords" screen order (confirmed against `docs/sda-reference/screens/field-level/character/general/image168.png`), taking a bare (unquoted) numeric parameter same as `ALIAS`/`FLDCSRPRG` - no new picker needed since it fits the existing `flagRowHtml`/`wireFlagRow`/`getFileFlagKeyword`/`setFileFlagKeyword` generic flag-row machinery precisely. Corrected the stale doc comment on `getGeneralFieldKeywords` in `dspfWriter.js`. Note (documented in-line, not fixed here - out of scope for this specific bug report): this shared row list doesn't gate `CNTFLD`/`DFTVAL`/`FLDCSRPRG` out for constants even though real SDA's own constant-specific General screen (`.../constant/general/image190.png`) omits all three - a pre-existing scope note, not something newly introduced by this fix. New regression coverage in the existing General-keywords scenario in `src/test/dspfWebview.test.js`. |
| **L21** | **CHGINPDFT had no way to set its own sub-flags (HI/RI/CS/BL/UL/LC/ME/MF/FE).** Found by direct user report, confirmed against a screenshot of real SDA's own "Change Input Defaults" sub-screen (reached via CHGINPDFT's own "Select parameters" row): every CHGINPDFT flag row in iSDA (File level, Record level, SFLMSG General, Field-level Input keywords) was either a bare on/off checkbox or a free-text parameter box - none exposed the keyword's real 9 sub-flags. | — (standalone; found via the same screenshot that prompted the L20 audit) | **done** — new shared `chgInpDftFlagHtml`/`wireChgInpDftFlag` in `webviewClientHelpers.js` (one component, reused at all 4 call sites per this codebase's own "build once, wire everywhere" convention): renders `flagRowHtml`'s own checkbox+Conditioning row plus 9 sub-flag checkboxes, with a hidden input carrying the space-joined code list so `wireFlagRow`'s existing commit path needed no changes; checking any sub-flag also force-checks the main on/off box (a code with CHGINPDFT absent would otherwise be silently dropped by `setFileFlagKeyword`'s own `if (present)` gate). Wired into File-level General Keywords (`fk-chginpdft`), Record-level General Keywords (shared by `RECORD`/`SFLCTL`/`WINDOW`/etc.), SFLMSG General (`sm-chginpdft`), and Field-level Input keywords. New tests in `src/test/dspfWebview.test.js`: a field-level scenario (9 checkboxes present, multi-code commit, force-check behavior) and a file/record-level scenario (same component under different id prefixes). Note (documented, not fixed here): `ENTFLDATR` shows the exact same "Select parameters" pattern on real SDA's screen but its own sub-screen wasn't captured in the reference screenshots, so its exact valid-value list isn't confidently known yet - flagged as a follow-up rather than guessed at (see Task L26, which later resolved it via web research). |
| **L22** | **Keyword-inventory audit findings.** During the L20/L21 investigation, a broader pass was made through the File-level (`screens/file-level/*`, all 9 categories), Record-level base keywords (`screens/record-level/base-record-keywords/*`, all 8 categories), and Field-level (`screens/field-level/*` - character, numeric, constant, menu-bar-choice, ~30 of ~55 screenshots) reference screens, cross-checked against every keyword name actually handled in the codebase. Field-level `SNGCHCFLD`/`MLTCHCFLD`, `WINDOW`, `DSPATR`, `COLOR`, and the choice-selection/choice-colors pickers were all independently re-verified against their own reference screens and check out correctly - no gaps found there. The remaining ~130 record-level variant screenshots (mostly wiring/reuse per `PICKER-SCREENS-PLAN.md`) and ~25 field-level screenshots (mostly duplicate states of already-checked screens) were not re-audited in this pass. | — (standalone findings) | **done** — see L21/L23/L24/L25/L26 for the four confirmed gaps this audit surfaced. The one remaining item, `ROLLUP`/`ROLLDOWN` (legacy alternate names for `PAGEDOWN`/`PAGEUP` - see `screens/file-level/02-indicator-keywords/image5.png`'s own "Conditioned keywords" line listing both spellings), is now also fixed: `getFileFlagKeyword`/`setFileFlagKeyword` in `dspfWriter.js` gained an optional `altNames` parameter (a legacy-spelling list matched alongside the canonical `name`, and normalized to the canonical spelling the moment that row is actually edited - unchecking PAGEDOWN now also removes a legacy ROLLUP instance instead of leaving an invisible orphan behind), wired into the file-level Indicator Keywords panel's PAGEDOWN/PAGEUP rows (`['ROLLUP']`/`['ROLLDOWN']`). The record-level "Define Indicator Keywords" screen (`getRecordIndicatorInstances`/`setRecordIndicatorInstances`, the repeatable-instance version of the same real SDA screen) had the identical gap and got the same fix - a legacy `ROLLUP`/`ROLLDOWN` instance now reads back with `kind` normalized to `PAGEDOWN`/`PAGEUP`, and `setRecordIndicatorInstances` removes any legacy-spelled instance on every commit (the kind dropdown never offers the legacy spellings as a choice, so a re-added instance always writes the canonical name). New coverage in `src/test/fileKeywordsPicker.test.js` and `src/test/recordIndicatorInstances.test.js`. |
| **L23** | **KEYBRD (Keyboard shift attribute) offered the wrong value list.** Found during the field-level pass of the L22 keyword-inventory audit: the dropdown offered `['S','N','Y','I','D']`, but real SDA's own "Select Keying Options" screen (`screens/field-level/character/keying-options/image164.png`) shows `N/A/X/W/I/D/M/J/O/E/G` (11 letters - no `S` or `Y` at all) - confirmed twice, since the SAME letter set also appears on the "Define Database Reference" screen's own "New keyboard shift" override field (`screens/field-level/character/database-reference/image170.png`). | — (standalone; found while auditing the same screens the L22 MSGLOC/ROLLUP findings came from) | **done** — corrected the dropdown's value list to `['N','A','X','W','I','D','M','J','O','E','G']`, matching both confirming screenshots exactly. Left unlabeled (bare letters, no English description per option) rather than guessing at each one's exact DDS Reference meaning without a confirmed citation - same "flag it, don't guess" posture as `MNUBAR`'s own display-separator note. Updated the one existing test that had been asserting the old wrong value (`'S'`) to instead assert the full corrected 11-value set and use `'N'` (valid under both lists) for its own commit check. |
| **L24** | **MSGLOC was entirely missing.** Found during the L22 audit - real SDA's own "Select Display Sizes" screen shows a "Message Line" column right alongside Order/Display name for each declared `DSPSIZ` size, but `MSGLOC` didn't appear anywhere in the codebase - not parsed specially, not exposed in any picker. Confirmed via IBM's own DDS Reference: `MSGLOC(line-number)` is a genuinely separate file-level keyword (not a 4th `DSPSIZ` triple value - checked against `parseDisplaySizeTriples`/`serializeDisplaySizes` in `dspfWriter.js`), paired with a display size via the SAME display-size-condition mechanism (`displaySizeCondition`) the parser already builds for any keyword's conditioning columns - e.g. the Reference's own `MSGLOC(1)` / `A *DS4 MSGLOC(1)` example. | — (standalone; L22 follow-up) | **done** — new `getFileMsgLocLines`/`setFileMsgLocLines` in `dspfWriter.js`, modeled as `(primary, bySizeName)` matching how the Display Sizes picker's own `sizeList` already distinguishes the primary (unconditioned, order 1) size from any secondary (named, order 2) one. Two new "Message line" inputs on the existing Display Sizes panel, wired into its own Apply button so DSPSIZ order and MSGLOC conditioning always stay in sync - order 1 gets the unconditioned MSGLOC, order 2 gets one conditioned by its own `*DSx` name. New coverage in `runL22FollowUpFixesScenario`. |
| **L25** | **TEXT (pure documentation keyword) was entirely missing.** Found during the L22 audit's field-level pass - a several-record-types' own outer keyword menu shows a "TEXT keyword" item separate from "General keywords" (e.g. `screens/record-level/subfile-sfl/subfile-keywords/image48.png`), and IBM's own DDS Reference confirms it's a plain quoted-string documentation keyword (no compiled/runtime effect) valid at File, Record, and Field level. | — (standalone; L22 follow-up) | **done** — added a `TEXT` input at File level and Record level (reusing the existing `getFileQuotedText`/`setFileQuotedText` pair, same as `HLPTITLE`) and a `text` row in `GENERAL_FIELD_KEYWORD_ROWS` for Field level (matching `DFT`/`DFTVAL`'s own raw-text-box convention, since that row list's mechanism is uniformly raw text for every quoted keyword already in it). New coverage in `runL22FollowUpFixesScenario`. |
| **L26** | **ENTFLDATR had no way to set its own color/attributes - only a free-text box.** Found alongside L21 (same "Select parameters" pattern as CHGINPDFT), initially flagged rather than fixed since its own sub-screen wasn't in the captured reference screenshots. Resolved via web research against a real DDS source example (`ENTFLDATR((*COLOR BLU) (*DSPATR HI UL))`) plus IBM's own keyword-reference table confirming ENTFLDATR is valid at File, Record, and Field level with the exact same `(*COLOR c) (*DSPATR a a)` shape `CHCAVAIL`/`CHCUNAVAIL`/`CHCSLT` already use. | — (standalone; L22 follow-up, resolved after further research) | **done** — new `entFldAtrHtml`/`wireEntFldAtrEditor` in `webviewClientHelpers.js` (color select + `HI`/`RI`/`CS`/`BL`/`ND`/`UL` checkboxes - the same `WDWBORDER_ATTRS` subset every other compound `(*COLOR)`/`(*DSPATR)` keyword in this codebase already offers), reusing `DspfWriter.getChoiceColorState`/`setChoiceColorState` directly rather than a near-duplicate pair - those functions are already generic over `keywordName` despite the "Choice" name. Wired into File-level and Record-level General Keywords, replacing the old free-text box. New coverage in `runL22FollowUpFixesScenario` (`src/test/dspfWebview.test.js`). |
| **L27** | **Command keys (`CAnn`/`CFnn`) can't be conditioned by indicators** — reported as "Cmd keys can also have conditionings." Confirmed: `setCommandKey` in `dspfWriter.js` always wrote `conditions: []` regardless of what was already there, and `parseCommandKeys` never surfaced a key's own `conditions` for the UI to read - so real DDS's standard indicator-conditioning (position 7-16, turning the whole keyword on/off based on indicator state - a SEPARATE mechanism from the embedded response indicator that `CAnn(indicator 'text')`'s own first parameter sets ON when the key is pressed) was invisible and unsettable for command keys specifically, even though every other keyword in this codebase already supports it. | L1 (component precedent - `conditionsEditorHtml`/`wireConditionsEditor`, the same generic conditioning component every other conditioned keyword already uses) | **done** — `parseCommandKeys` now returns each key's own `conditions` directly; `setCommandKey` takes an optional `conditions` argument (defaults to `[]` - fully backward compatible with every pre-existing call site). Each existing command-key chip (file-level and record-level "Command keys" sections) gets its own "Conditioning" toggle/editor, reusing the same generic `conditionsEditorHtml`/`wireConditionsEditor` component - editing it replaces that ONE key's conditions in place via `setCommandKey`, preserving its own indicator/text. Deliberately kept the existing one-definition-per-number-per-scope model (conditioning that single definition on/off) rather than also building multiple independently-conditioned instances of the SAME key number - **see Task L31**. No staging-step conditioning on the "+ Add command key" form itself - `conditionsEditorHtml` commits per-click with no batched-until-Add mode, so there's no stable place to hold a not-yet-created key's conditions; add the key first (unconditioned, as always), then expand its own Conditioning toggle. New unit coverage in `src/test/dspfWriter.test.js` and end-to-end coverage in `src/test/dspfWebview.test.js`. |
| **L28** | **The open file's own name in the left panel was buried down near the File attributes/Compile buttons instead of living up top** — reported as a request to move it "below Screen design at top." Confirmed: `#fileStatus` sat well down the panel (after Find field, Record select, +Add record, the placement buttons, the compare/ruler/crosshair toggles, and the indicator list), directly above the "File attributes"/"Compile" buttons - a person opening the designer had to scroll past all of that to confirm which file was even open. Same layout in both designers (DSPF and Menu). | — (standalone; UI-only reorder, no model/keyword changes) | **done** — moved `#fileStatus` to right after the `<h2>Screen Design</h2>`/`<h2>Menu Design</h2>` heading, before the Code for IBM i connection badge, in both `buildWebviewTemplate.js` and `buildMenuWebviewTemplate.js` - now the very first thing shown under the panel's own title, matching the report exactly. The "File attributes"/"Compile" buttons stay where they were; only the file-name display itself moved. New `runFileNamePositionScenario` in both `src/test/dspfWebview.test.js` and `src/test/menuWebview.test.js` asserts the element ordering directly (not just that the text is present somewhere). |
| **L29** | **Windows with no `WDWBORDER` anywhere (record or file) rendered with a plain, unstyled border instead of the real DDS-documented default** — reported as "windows in SDA have default fill/reverse image at the border when we are not providing any windows border parameter." Checked against IBM's own WDWBORDER keyword documentation rather than taking the "fill/reverse image" description at face value: the actual documented default (when the keyword is entirely absent) is period (`.`) for the top border and both top corners, colon (`:`) for both side borders and - the one irregular part - both BOTTOM corners too (matching the side character, not the top-corner/top-border one), with color defaulting to blue and no display attribute; `resolveWdwBorder` in `dspfEngine.js` fell back to a completely empty/unstyled `{color: null, attrs: [], chars: ['','',...]}` in that case instead. | — (standalone; found in the same session as L27/L28/L30) | **done** — `resolveWdwBorder`'s final fallback (neither record nor file has any `WDWBORDER`) now returns the documented default (`{color: COLOR_HEX.BLU, attrs: [], chars: ['.','.','.',':',':',':','.',':']}`, in the same 8-position order `DspfWriter.getWdwBorder`/`renderWindowBorderCharsHtml` already use) instead of an empty one - this puts the window into the existing char-mode border rendering automatically, no new rendering path needed. Scoped deliberately to the "keyword entirely absent" case only, matching the literal report; a genuinely partial `WDWBORDER` does NOT get the same per-sub-parameter defaults applied - **see Task L32**. New `runDefaultWindowBorderScenario` in `src/test/dspfWebview.test.js`, including a guard confirming an explicit `WDWBORDER` still wins outright and isn't affected by the new default. |
| **L30** | **Dragging a window by its move handle made it jump, not slide** — reported as "when dragging windows I feel like it is jumping to right side." Root cause: `.dspf-window-move-handle` spans the window's entire top edge (`left:0; right:0` in its own CSS), but `startWindowMove`'s own `onMove` snapped the window's origin straight to the raw mouse position on every move (as if the cursor itself WAS the window's top-left corner) rather than preserving the offset between where it was actually grabbed and that corner - so the instant a drag started, the window jumped so the grabbed point became its new left edge, visibly shifting the whole window right (since a title-bar grab virtually never lands on the window's own leftmost pixel). Resize never had this problem, since it already computed width/height as a difference from the window's fixed, unmoving origin rather than an absolute position. | — (standalone; found in the same session as L27-L29) | **done** — `startWindowMove` now threads the mousedown event through from the move-handle listener, captures the mouse's own starting point and the window's starting origin at drag-start, and every subsequent `onMove` applies how far the MOUSE has moved as a delta onto that original origin - preserving wherever within the handle it was grabbed, instead of snapping to the cursor. **Not applied to field-dragging** (`startDrag`) even though its own `onMove` has this identical absolute-snap pattern - **see Task L33**. New `runWindowMoveOffOriginScenario` in `src/test/dspfWebview.test.js` (a dedicated, fresh scenario rather than extending the existing `runWindowMoveResizeScenario`, since that one's own move+resize steps already mutate the window's geometry away from its declared starting point) - grabs the move handle in the middle of the title strip rather than at its exact origin pixel, confirming a one-cell mouse nudge moves the window by exactly one cell, not a jump to roughly where it was grabbed. |
| **L31** | **Command keys don't support multiple independently-conditioned instances of the SAME key number** — deferred sliver flagged during Task L27 (indicator conditioning for `CAnn`/`CFnn`). Real SDA's own Design Image screen supports this: e.g. F3 reading "Exit" under one indicator and "Cancel" under another, both as SEPARATE `CA03` lines each with their own conditioning, matching how `COLOR`/`DSPATR` states already work (Task L1). `setCommandKey` kept a strict one-definition-per-number-per-scope model - adding a second instance of an already-used number wasn't possible via the UI at all (`availableCommandKeyNumbers` excluded any number already in use, conditioned or not). | L1 (component precedent for multi-instance conditioned keywords), L27 (single-instance conditioning this extends) | **done** — added a parallel, index-based pair (`setCommandKeyAt`/`removeCommandKeyAt` in `dspfWriter.js`) rather than changing `setCommandKey`/`removeCommandKey`'s own by-number signature - every existing single-instance-per-number caller (including every pre-L31 test) keeps its original behavior completely unchanged. The new pair addresses ONE specific instance by its 0-based ordinal position among ALL command-key keywords in source order (the same order `parseCommandKeys` already returns), so editing or removing one instance of a number can never disturb a SIBLING instance of that same number - the old by-number functions would have collapsed both down to one. `allCommandKeyNumbers()` (always `"01".."24"`) replaces `availableCommandKeyNumbers()` as what the "+ Add command key" picker offers, since excluding an already-used number is exactly what this task needed to stop doing; `availableCommandKeyNumbers` itself is untouched and kept for any caller still wanting the old single-instance exclusion. `commandKeysSectionHtml`/`wireCommandKeysSection` now key every row (remove button, Conditioning toggle, and the toggle's own expand-state and editor id) by `data-index` instead of `data-number` - `data-number` is still rendered on each row for display/backward-compatible selector matching in files with no duplicates, but is no longer what identifies a row internally. New coverage in `src/test/dspfWriter.test.js` (unit-level `setCommandKeyAt`/`removeCommandKeyAt`/`allCommandKeyNumbers`) and `src/test/dspfWebview.test.js` (end-to-end: adding a second CA03 alongside an already-conditioned first one, verifying neither disturbs the other). |
| **L32** | **Partial `WDWBORDER` doesn't get IBM's own per-sub-parameter defaults** — deferred sliver flagged during Task L29 (default border when `WDWBORDER` is entirely absent). IBM's own WDWBORDER documentation specifies defaults for EACH sub-parameter independently, not just when the whole keyword is missing: if `*COLOR` isn't specified, it defaults to blue REGARDLESS of whether `*DSPATR`/`*CHAR` were specified; same for `*CHAR` defaulting to the period/colon pattern. `resolveWdwBorder`'s `parse()` only fills in `color`/`chars` when a WDWBORDER keyword is entirely absent (L29's fix) - a keyword that IS present but only sets, say, `*DSPATR`, still leaves `color: null`/`chars: ['',...]`  rather than falling back to the same documented per-parameter defaults. Not fixed alongside L29 since it would silently change the rendered appearance of every window that already has SOME `WDWBORDER` specified today - a much bigger, more visible change than the original report needed. | L29 (this is a generalization of that fix) | **done** — `resolveWdwBorder`'s `parse()` (`dspfEngine.js`) now applies the documented per-sub-parameter default independently for EACH group left unset on a WDWBORDER keyword that IS present: no `*COLOR` -> `COLOR_HEX.BLU`, no `*CHAR` -> the same `['.','.','.',':',':',':','.',':']` pattern L29 already established, regardless of which other sub-parameters WERE specified. An explicit `*COLOR`/`*CHAR` still always wins outright - only the sub-parameter group(s) genuinely absent from the keyword get backfilled. `*DSPATR` needed no equivalent change: its own documented default (no display attribute) is already exactly what an empty `attrs` array represents. This is a deliberate, visible behavior change for every window with a genuinely partial `WDWBORDER` today (the scope L29 explicitly deferred) - e.g. `WDWBORDER((*COLOR RED))` alone now ALSO renders the default period/colon character overlay (in red, since its own `*COLOR` still wins), switching that window into char-mode where it previously rendered as a plain colored box. Updated the two existing tests that encoded the old (pre-L32) partial-`WDWBORDER` behavior to assert the new per-parameter-default behavior instead (`src/test/dspfEngine.test.js`'s WDWBORDER scenarios and `src/test/dspfWebview.test.js`'s `runWindowBorderAndDefaultColorScenario`/`runDefaultWindowBorderScenario`'s own explicit-WDWBORDER guard), plus two brand-new `dspfEngine.test.js` cases covering "*COLOR only" and "*DSPATR only" partial keywords directly against the resolved `border` object. |
| **L33** | **Field-dragging has the same absolute-snap-to-cursor pattern that caused the L30 window-drag jump bug** — deferred sliver flagged during Task L30. `startDrag`'s own `onMove` (used for dragging fields/constants on the canvas) computes the field's new position directly from the raw mouse coordinates, the same pattern `startWindowMove` had before L30's fix - grabbing a field anywhere other than its own top-left cell would jump it to align that grabbed point with the cursor, rather than preserving the click offset. Not fixed alongside L30 since fields weren't part of that report, and field-dragging has extensive existing test coverage (`runClickToPlaceScenario` and others in `src/test/dspfWebview.test.js`) keyed to the CURRENT exact-snap behavior at specific pixel coordinates - fixing this would need those tests re-verified/updated too, a bigger change than the literal report needed. | L30 (same root-cause pattern, already fixed there) | **done** — `startDrag` and `startGroupDrag` (`buildWebviewTemplate.js`) both now thread the mousedown event through from their own mousedown listeners (the three call sites - lone-field drag, SFLPAG-preview-row group drag, and multi-select block group drag - all updated to pass it), capture the mouse's own starting point at drag-start, and every subsequent `onMove` applies how far the MOUSE has moved as a delta onto the field's/group's original rendered position - the exact same fix `startWindowMove` already got in L30, extended to the two field-drag paths that shared its root cause. Re-checked every EXISTING drag test that exercises a relative mouse delta (all of them, per this task's own written concern about needing them "re-verified") - all passed unchanged, since they only ever asserted the DELTA the mouse moved, never an absolute snap-to-cursor position, so none needed updating. New `runFieldDragOffOriginScenario` in `src/test/dspfWebview.test.js` (modeled directly on L30's own `runWindowMoveOffOriginScenario`) grabs a multi-column field away from its own top-left cell and asserts two things the old bug got wrong: zero subsequent mouse movement posts no edit at all (the old code would have already snapped/jumped on the very first `onMove`, even with no real movement), and a genuine one-cell drag from that same off-origin grab point moves the field by exactly one row/column from its ORIGINAL position, not to wherever it was grabbed. Verified the new test actually catches the bug by temporarily reverting the fix and confirming it fails, then restoring it. |
| **L34** | **Watch for other legacy-keyword-synonym gaps beyond ROLLUP/ROLLDOWN.** Task L22's ROLLUP/ROLLDOWN fix (legacy alternate spellings of `PAGEDOWN`/`PAGEUP` not being recognized as equivalent by the checkbox UI) revealed a general pattern: DDS has other keywords with documented legacy/alternate spellings too. Checked via web research against IBM's own DDS Reference "Conventions and terminology" page (the canonical glossary for the whole DDS-for-display-files documentation set) - it declares exactly one other such pair: `CMP` is "equivalent to the COMP keyword... The COMP keyword is preferred" (field-level validity check). No other "keyword X is the same as keyword Y" statement appears anywhere in that reference page or in the individual per-keyword DDS Reference pages checked alongside it. | L22 (source of the pattern) | **done** — `getValidityCheck`/`getValidityCheckInstances` in `dspfWriter.js` now recognize `CMP` alongside `RANGE`/`COMP`/`VALUES` on read (normalized to `COMP` for the picker's own `kind` field, since `CMP` was previously invisible to the Validity Check picker entirely - unlike the ROLLUP case, validity check has no separate on/off flag to hide behind). `setValidityCheckInstances`/`setValidityCheck` never re-emit `CMP` (the kind dropdown only ever offers `RANGE`/`COMP`/`VALUES`), so editing a `CMP`-sourced field through the picker at all normalizes it to the preferred `COMP` spelling - same "read both, always write the modern name" rule L22's own ROLLUP/ROLLDOWN fix already established. New coverage in `src/test/validityCheckInstances.test.js`. No other synonym pair was found in this pass; the row stays open in spirit as a standing reminder for future audits, but the concrete finding it produced is fixed. |
| **L35** | **Reported: "code for ibm i runcommand is not valid, it should be code for ibm i runaction."** Investigated against Code for IBM i's own official developer documentation (`codefori.github.io/docs/dev/examples/`, "Running commands with the user library list" and "Right click options" sections) rather than changing the API call on the strength of the report alone, since a wrong Code for IBM i API call would break Compile/DSPFFD entirely, not just cosmetically. Finding: `code-for-ibmi.runCommand` **is correct and is not the same thing as "Run Action."** The docs show it twice - once as `instance.getConnection().runCommand({command, environment})` (a method on the connection object, exactly this codebase's own `getConnectedCodeForIBMi()`/`connection.runCommand(...)` usage) and once as `vscode.commands.executeCommand('code-for-ibmi.runCommand', {command, environment})` (a registered VS Code command with the identical shape, exactly this codebase's own `extension.ts` usage for `CRTDSPF`/`CRTMNU`/`DSPFFD`) - both run an ARBITRARY CL/PASE command string supplied by the caller. "Run Action" (`code-for-ibmi.runAction` per the extension's own changelog/release notes, e.g. "Fixed 'run action' command when triggered via the command palette") is a DIFFERENT, end-user-facing feature entirely - it runs a PROJECT-CONFIGURED action from a workspace's own `actions.json` (bound to Ctrl+E, right-click, or the command palette's "Run Action" entry), not something a caller hands an arbitrary command string to. Swapping to `runAction` here would break every `CRTDSPF`/`CRTMNU`/`DSPFFD` call, since there's no `actions.json`-defined action for those in this extension's own workflow - the two commands solve different problems and aren't interchangeable. | — (verification only; no code change) | **not applicable** — current usage confirmed correct against official documentation; no change made |
| **L36** | **"Copy field"/"Copy constant" (the `p-copy` button in the field properties panel) always drops the copy one row directly below the original, same column — reported as "when clicked ask user where to place the copied field... currently it is pasted on next line and overlaps."** `commitCopy` calls `DspfWriter.copyField(record, lines, field, {})` with no `options.location`, so `copyField`'s own default (`field.location.line + 1`, same column) always fires — for a single-line-height field this lands the copy's top row directly on/adjacent to the original's own footprint, visibly overlapping it in the design canvas rather than landing somewhere free. The existing "+ Field"/"+ Constant" click-to-place flow (`placementMode`/`pendingPlacement`/`renderPlacementProps`) already solves exactly this for brand-new fields — click the button, then click the screen preview to choose a starting line/column (still editable as numbers) before it's actually inserted. | L9/placement-mode machinery (reused, not duplicated) | **done** — the `p-copy` button now calls a new `beginCopyPlacement(recordName, field)` instead of committing immediately: it snapshots the field/constant being copied into a new `pendingCopySource` and enters a third `placementMode` value, `'COPY'`, reusing the SAME `screenOutput` click handler and `pendingPlacement` state the "+ Field"/"+ Constant" flow already has (no new click-capture code needed). A new `renderCopyPlacementProps` panel (branched off `renderPlacementProps` for the `'COPY'` kind, since it needs none of the name/length/type/decimals inputs FIELD/CONSTANT placement asks for — those all come from the snapshot unchanged) shows "Copy of <name/text>" plus the same editable Line/Column inputs, defaulting to wherever the canvas was clicked. Confirming calls `DspfWriter.copyField(rec, lines, field, { location: { line, column } })` — the exact same primitive `commitCopy` always used, just now with an explicit `options.location` overriding `copyField`'s own one-row-below default instead of relying on it, which is what was causing the reported overlap. Copying into a DIFFERENT record than the source (cross-record, same as Ctrl+X/C/V's own paste) still works, since `recordName` is whichever record is currently shown. `pendingCopySource` is cleared alongside `pendingPlacement` on cancel/Escape/breadcrumb-navigation so it can never go stale across an unrelated later placement. Scope: only the single-field/-constant "Copy" button in the properties panel — Ctrl+D (duplicate in place), Ctrl+X/C/V (cut/copy/paste), and the multi-select "Duplicate selection" button are UNCHANGED, since the report was specifically about this one button and those other paths have their own established "land somewhere sensible, then drag it" conventions already covered by existing tests. Updated `runCopyFieldScenario` in `src/test/dspfWebview.test.js` to exercise the new click-to-place flow end to end (Copy button activates placement mode without posting an edit, a canvas click opens the pre-filled placement form, confirming posts the edit at the clicked line/column) rather than asserting the old immediate-commit behavior; every other check in that scenario (auto-generated distinct name, DSPATR preserved, original untouched, new copy selected) is unchanged. |
| **L37** | **Left-panel cleanup + a "Find keyword" quick-nav in the properties panel** — reported directly: (1) the "File" section-label sat stranded near the bottom of the left panel, well below the filename it should be labeling (moved up by L28), instead of right next to it; (2) the standalone "File attributes" button duplicated the always-visible "File" crumb at the top of the right-side properties panel, one click away already; (3) the properties panel had no way to jump straight to a specific keyword once its General/Indicator/Print/etc. tabs and the Advanced/raw-keywords accordion held more than a handful of entries. | L28 (repositions what it moved) | **done** — the `section-label` "File" now sits directly under the "Screen Design" `<h2>`, immediately before the filename, styled bold (`font-weight:700`); it also doubles as a click target that opens the file-level properties panel (same `showFileProps = true` path the removed button used), so there's still a one-click way in from the left side without a redundant second button. `fileAttrsBtn` and its listener are gone entirely. A new "Find keyword" search box now sits at the top of the properties panel (`buildWebviewTemplate.js`): rather than a separate keyword→tab lookup table that would drift out of sync with `fileKeywordsPanelsHtml`/`recordKeywordsPanelsHtml`'s own category choices, it searches the ALREADY-RENDERED `propsBody` DOM directly (every `tabsHtml()`/`subtabsHtml()` panel exists in the DOM at once, just CSS-hidden via the missing `.active` class), matching against row labels/section-labels/keyword-chip text. Picking a match auto-switches to its tab/subtab, opens any collapsed `<details>` accordion it's inside, scrolls it into view, and briefly flashes it (`kwJumpFlash` highlight animation) - same arrow-key/Enter/Escape handling as the existing "Find field" box. Updated `runFileNamePositionScenario` (renamed assertions for the new File-label→filename→badge order, confirms the label is bold, confirms `fileAttrsBtn` no longer exists) and the file-level-command-keys/file-attrs-toggle scenarios in `src/test/dspfWebview.test.js` to open file properties via the `crumb-file` breadcrumb instead of the removed button. Scope: DSPF designer only - the Menu designer's own `buildMenuWebviewTemplate.js` has a similarly-named but structurally different `fileAttrsToggle` (an in-page collapsible, not a button that switches the right panel) and wasn't touched. |
| **L38** | **Source modification tracking** — requested directly: an option to tag added/changed source lines with a modification marker, and to comment out an existing line instead of overwriting it when it changes, so a line's edit history stays visible in the source rather than being silently overwritten. Design agreed with the person before implementation: the tag is an inline marker written in columns 81-90 (past DDS's own 80-column compiled area, so the compiler never sees it) rather than a separate comment line; its 10-character content is freely typed into a session-only text box in the properties panel (no fixed format imposed); on/off is controlled by both a global `isda.trackSourceModifications` VS Code setting (default off) and a per-session checkbox in the same panel that starts from the setting's value but doesn't write back to it - same "session-only, never persisted" convention Task L11's ruler toggle already established. Scoped to the DSPF designer only for this pass (per the `L`-prefix convention) - the Menu designer has no single "commit" choke point the way the DSPF designer's `commitSourceChange` does, so wiring it there is a larger, separate lift; raise a new `M`-prefixed task if that's wanted. (Done — see **M8**, v0.10.33.) **Implementation:** `dspfWriter.js` gained four small primitives - `commentOutLine` (flips column 7 to `*`, preserves the rest of the original line verbatim), `buildModTag` (normalizes the typed tag to ≤10 chars, no newlines), `appendModTag` (pads to column 80 then appends the tag at 81-90), and `applyModificationTracking(oldLines, newLines, options)`, the one function that actually does the work - rather than threading tracking options through every individual `apply*`/`insert*`/`delete*` function in the file (dozens of call sites), it's a single post-processing step wrapping the (before, after) line-array pair every edit already produces. It trims the common prefix/suffix between the two arrays (every edit function in this file already replaces exactly one contiguous range and leaves everything else byte-for-byte alone, so this - not a general LCS diff - is enough to isolate what changed), then per position: unchanged -> left alone; changed -> old commented out immediately before the new, tagged, line; pure insertion -> tagged, nothing to comment; pure deletion -> the old line kept as a comment (a genuinely blank deleted line is dropped, not preserved as an empty comment) so a shrinking edit's history survives too. `buildWebviewTemplate.js`'s `commitSourceChange` - the single choke point nearly every DSPF designer edit already funnels through - calls `DspfWriter.applyModificationTracking(lines, newLines, { enabled: modTrackingEnabled, tag: modTrackingTag })` right after `transform()` runs, so this needed no changes to any individual picker/apply function. Properties panel gained a "Track modifications" checkbox + 10-char tag box (session state, initialized from the `modTrackingConfig` message, never written back to settings once the person touches either control directly). `extension.ts`'s `DspfDesignerEditorProvider` reads `isda.trackSourceModifications`/`isda.modificationTag` and posts `modTrackingConfig` on `ready` and again on any live `settings.json` change while the panel is open (via `onDidChangeConfiguration`, disposed on panel close). `vscode-mock.js` gained `onDidChangeConfiguration`/`__fireConfigChange` support (previously untested surface). New dedicated test files `modificationTracking.test.js` (writer primitives) and `modTrackingWebview.test.js` (full jsdom scenario: edit with tracking on, verify comment-out + tag; toggle off, verify plain in-place edits resume), plus a new scenario in `extension.test.js` covering the config push/live-change/disposal. All 28 test files, zero failures. | — (standalone) | **done (v0.10.18)** |
| **L39** | **Dragging a field/constant inside a `WINDOW` record has no boundary at all today — it can be dragged (or nudged) clean outside the window's own box, real SDA's own Design Image screen refuses to let that happen.** Reported directly: "when moving any field/constant within windows record formats, it should not allow it to be moved outside windows boundary same as IBM i SDA." Confirmed against the current code: `startDrag`'s `onMove` (`buildWebviewTemplate.js`, ~line 2388) only clamps `newCol`/`newLine` with `Math.max(1, ...)` — a floor at column/row 1 of the WHOLE screen, not the window's own client area — and has no upper bound and no window-awareness whatsoever; `startGroupDrag` (multi-select block drag, ~line 2535) and the arrow-key nudge path have the identical gap. `resolveWindow(record, dspfFile)` (`dspfEngine.js`, ~line 645) already resolves a window record's own `{line, col, height, width}` box from its `WINDOW` keyword (border thickness is a separate, additive concern — see `resolveWdwBorder`/Task L29/L32 — not folded into this box today), so the geometry needed to compute a client-area boundary already exists; it's just never consulted at drag/nudge time. Real SDA's own Design Image screen keeps every field move/nudge inside a window's frame — a field can't be dropped or nudged so its own cell(s) land on or outside the window's border. | L29/L32 (window border geometry this would need to account for), L30/L33 (the delta-drag mechanism this constrains) | **done** — `startDrag`/`startGroupDrag`'s `onMove` now consult a new shared `computeDragBounds(recordName, footprintLength, footprintHeight)` helper instead of the old bare `Math.max(1, ...)` floor. It reads the CURRENT window's own `{line, col, height, width}` box via `lastScreen.window` and clamps the field's (or, for `startGroupDrag`, the whole selected group's combined bounding box's) own top-left corner to `[windowBox.col+1, windowBox.col+windowBox.width-2-(footprintLength-1)]` / the equivalent for lines — i.e. one cell in from every edge, since the border ring is ALWAYS reserved space (default period/colon chars even with no `WDWBORDER` — L29/L32), never usable field space. `resolveWindow` is now exported from `dspfEngine.js`'s own module surface (previously internal-only) so nothing needed duplicating client-side. New coverage: `runWindowFieldDragBoundaryScenario` in `dspfWebview.test.js` — drags a field from well inside a window's interior toward its top-left corner, far enough to aim past the border if nothing stopped it, and confirms it lands clamped to the interior instead. Deliberately scoped to MOUSE DRAG only (`startDrag`/`startGroupDrag`) in the first pass — the arrow-key nudge path (`nudgeSelected`) works in SOURCE-relative coordinates rather than `startDrag`'s absolute render coordinates (window fields are addressed relative to the window's own origin in real DDS), so the exact same numeric bounds didn't directly translate; **follow-up (same day): now covered too** — a new `computeNudgeBounds(recordName, footprintLength, footprintHeight)` helper (source-coordinate sibling of `computeDragBounds`, NOT a thin wrapper around it, since the window-interior math is expressed directly in the window's own relative frame — source line `2..height-1`, column `2..width-1` — rather than `computeDragBounds`' absolute-screen numbers) is now wired into both the single-field and multi-select branches of `nudgeSelected`. New coverage: `runWindowNudgeBoundaryScenario` — Shift+ArrowUp (nudge-by-5) aimed well past the window's top edge lands clamped at the interior boundary instead, and a further nudge from there posts no edit at all. |
| **L40** | **Dragging a field within a paired `SFL`/`SFLCTL` subfile pair has no region boundary either — a field can be dragged off the SFLCTL's own header rows into the repeating subfile detail band, or off the SFL detail record's own row, with nothing stopping it, same underlying gap as L39.** Reported alongside L39: "when working with sfl or sfltctl, fields in their respective region should be allowed with in their range same as IBM i SDA." Real SDA draws a firm line between an SFLCTL record's own fields (headers, column captions, command-key text — laid out on whatever fixed rows precede the subfile) and the paired SFL record's own fields (the single repeating detail row, laid out on that record's own one or few source lines and then replayed `SFLPAG` times at render time) — neither can be dragged into the other's territory on the Design Image screen. iSDA already treats the two specially at RENDER time (`isSflRecord`/`findSflPairing`/`resolveSubfilePreview` in `dspfEngine.js`, ~line 900-1108; the `subfile-edit-row-` tag and its dedicated drag-writes-back-to-the-SFL-record handling in `buildWebviewTemplate.js`, ~line 2170, which Task L33's own test coverage already exercises for the delta-drag mechanism) but has no equivalent boundary check at DRAG time — a plain `startDrag` on an SFLCTL field or an editable subfile-preview row uses the exact same unbounded `Math.max(1, ...)` floor L39 describes, with no concept of "stay within my own record's line range" at all. Needs its own investigation into what the boundary actually IS for each side before implementation: for SFLCTL, likely the rows NOT occupied by the rendered subfile band (`lineOffset`/`declaredSflPag` math `resolveSubfilePreview` already computes); for the SFL detail record, likely confined to its own record's declared field-line span (checking `dist/` fixtures for whether a real-world SFL record ever legitimately spans more than one source line before assuming a single-row band). | L39 (same root-cause gap, same delta-drag mechanism to constrain), the existing SFL/SFLCTL pairing and subfile-preview-row-drag logic this needs to read geometry from (no task ID — see `resolveSubfilePreview`/`findSflPairing` in `dspfEngine.js` directly) | **done** — implemented as a MUTUAL-EXCLUSION model rather than the fuller "reserved band" idea this row originally speculated about: `computeDragBounds` also reads, straight from the model (not the rendered screen — the paired record's own fields aren't part of `lastScreen.fields` at all), the OTHER half's own declared field lines via `DspfEngine.findSflPairing(model, recordName)`, offset by the same window `lineOffset` both sides share when a window is also in effect. If the field being dragged would land its own occupied line(s) on any line the OTHER record's own fields sit on, the move is rejected and the field HOLDS at its last valid position for that step (not re-clamped to some computed nearest-legal line) — a resist-at-the-boundary feel, same idea `startDrag`'s own comment on `lastValidLine` explains. Works symmetrically in both directions (SFLCTL field dragged toward the SFL record's own line, and the SFL record's own field dragged toward an SFLCTL field's line), and for both `startDrag` and `startGroupDrag` (the SFLPAG-preview group-drag / arbitrary multi-select paths). Consciously narrower than the "reserved band" framing this row's own description first reached for: this checks against the OTHER record's ACTUAL declared field lines, not the full runtime-replayed `SFLPAG` band a real subfile occupies once rendered — a field could still be dragged onto a line that's part of the replayed band but happens to have no field of its own declared there today. Left as-is rather than building the fuller band-exclusion model sight-unseen: the reported problem (fields from one record landing exactly on the other's own fields, an immediate visible collision) is what this fixes, and a real gap beyond that can be its own follow-up once someone hits it in practice, same "fix what's reported, track the rest" discipline the rest of this table already follows. New coverage: `runSflRegionDragBoundaryScenario` in `dspfWebview.test.js` — covers "blocked" (landing exactly on the other record's own line, no edit posted at all) and "allowed" (an unoccupied line goes through normally) in BOTH directions. **Follow-up (same day):** the arrow-key nudge path now enforces the same mutual-exclusion rule too — `computeNudgeBounds` (source-coordinate sibling of `computeDragBounds` added for L39's own nudge follow-up) compares the OTHER record's declared field lines directly, with NO `lineOffset` adjustment needed this time (both halves of a pair already declare their own fields in the same relative frame, so nothing needs re-basing the way `computeDragBounds`' render-coordinate comparison does). New coverage: `runSflRegionNudgeBoundaryScenario`. |
| **L41** | **WDWBORDER with a real-world single-quoted `*CHAR` string rendered no border at all** — reported with three screenshots: iSDA's own preview showing the window with no visible border whatsoever, real SDA/5250 showing a solid blue box border around the identical window, and the DDS source itself (`WDWBORDER((*COLOR BLU) (*DSPATR RI) (*CHAR '        '))`, all 8 border-position characters given as ONE combined blank string, continued across two physical lines). Root cause: both `resolveWdwBorder` (`dspfEngine.js`) and `getWdwBorder` (`dspfWriter.js`) assumed `*CHAR` is always written as 8 SEPARATE quoted single-character literals (`'.' '.' '.' ...`) - which is what this codebase's OWN writer (`setWdwBorder`) always emits, so round-tripping iSDA's own output always worked - but real-world DDS commonly writes `*CHAR` as ONE combined character-string value instead (confirmed against IBM's own WDWBORDER documentation: "the parameter is coded as WDWBORDER(*CHAR followed by a string containing the top left, top, top right, left, right, bottom left, bottom, and bottom right characters, in that order" - and real published DDS examples like `(*CHAR ' ')` and `(*CHAR '...:- ::.:')`). With a single combined string, the old regex put the WHOLE string into position 0 (top-left corner) and left positions 1-7 empty. For this report's all-blank string, `hasWdwBorderChars` then saw position 0 as "not blank" (an 8-character string isn't `=== ' '`, even though every individual character in it WAS blank) and wrongly switched the window into "char mode" (which suppresses the plain colored CSS box border in favor of rendering the 8 individual character cells) - but positions 1-7 were empty and position 0's own cell only ever shows ONE character width, so nothing was actually visible in any of the 8 cells. Net effect: no border at all, in either form. | — (standalone; found via a direct user report with reference screenshots) | **done** — both `resolveWdwBorder` and `getWdwBorder` now disambiguate: if the `*CHAR` argument contains exactly ONE quoted group, its own characters are split across the 8 positions (real DDS's actual, documented syntax); if it contains multiple quoted groups (this codebase's own written format), each group still maps to one position, unchanged. With this fix, the reported all-blank string correctly resolves to 8 individual blank positions, `hasWdwBorderChars` correctly reports "no border characters" (not "char mode"), and the window falls through to the plain CSS box border carrying `*COLOR` as its own inline `border-color` - matching real SDA's solid blue box. New coverage: two new cases in `src/test/dspfEngine.test.js` (a non-blank single combined string splitting correctly, and the exact reported all-blank case rendering the plain colored box instead of empty char-mode) and two new cases in `src/test/fileKeywordsPicker.test.js`'s existing `getWdwBorder`/`setWdwBorder` block. |
| **L42** | **File-level comment rows show only their text, with no way to see or choose the source line they live at** — requested directly: "Comment properties in file level currently displayed in text alone. Would be great to show the line number from source. So when I want to add a comment I can also specify the line number where I want to add comment." Task L13's own comments list (`commentsListHtml`/`wireCommentsSection` in `buildWebviewTemplate.js`, shared by both the file-level Comments tab and each record's own Structure-tab Comments section) already tracks each comment's `data-source-line` internally for click/update/delete wiring, but never surfaced it visually, and `DspfWriter.addComment` only ever appends a new comment after the last existing one (or a fixed fallback line) - there was no way to choose where a new comment lands. Scoped to the file-level Comments tab, per how this was asked. **Implementation:** `DspfWriter.addComment(sourceLines, existingComments, fallbackAfterLine, text, desiredLine)` gained a 5th, optional `desiredLine` parameter - the 1-based physical line the NEW comment itself should end up as (clamped to `[1, sourceLines.length + 1]`, so an out-of-range typed value can never throw); everything else already at/after that line is simply pushed down by one, same as any other insertion in this file. Omitting it (every existing call site except the file-level one) preserves the exact old append-after-last-comment/fallback behavior unchanged. `commentsListHtml` now renders a small non-editable "L{n}" line-number badge on every comment row in BOTH scopes (display-only - a comment's line is a consequence of where it sits, not a property of the row), and gained a new `allowCustomLine` parameter (true only for the file-level call site) that additionally renders a "Line #" number input next to the Add-comment button; `wireCommentsSection` reads it at add-click time and forwards it through as `desiredLine`, left as `null` when blank (falls back to the old behavior) or when the input doesn't exist at all (the record-level scope). No parser/model changes needed - `comment.line` was already tracked, just never rendered. New coverage: a `desiredLine` block in `dspfWriter.test.js` (top/middle/clamped-high/clamped-low/omitted cases) and new checks in `dspfWebview.test.js`'s existing `runCommentsScenario` (badge rendering in both scopes, the line-number input existing on file-level only, an explicit-line add landing exactly where typed, and a blank-input add still falling back to append-at-end). | L13 (the comments feature this extends) | **done (v0.10.20)** |
| **L43** | **File-level conditioning indicators missing from the left-panel indicator list, and no way to rename a copy while placing it** — reported directly: (1) an indicator used ONLY to condition a file-level keyword (a `CAxx`/`CFxx` command key, `ALARM`, etc. - see `fileKeywordsPanelsHtml`) never appeared in the "Conditioning indicators (preview)" list, since `indicatorsForContext()` only ever walked record/field-level conditions, never `model.fileKeywords`; (2) the Task L36 copy-placement flow copied a field/constant's name/text unchanged (only line/column were editable), so renaming a field copy or rewording a constant's copy meant a second edit right after placing it. | L36 (extends its placement panel) | **done** — `indicatorsForContext()` (`buildWebviewTemplate.js`) now also collects every file-level keyword's own `conditions`, unconditionally (unlike the record-scoped collection around it, a file-level indicator applies to every record, so it's always included regardless of which record is currently previewed - see the SFL-pairing scoping right above it in the same function for the contrasting, record-scoped case). `renderCopyPlacementProps` gained an editable "Name" input (for a named field, pre-filled with the same auto-generated distinct name `DspfWriter.copyField` would otherwise pick via `nextAvailableFieldName`, validated the same way the other placement panels validate a new name: non-empty, a valid DDS name, and not already used in the target record) and an editable "Text" input (for a literal constant, pre-filled with its own text - skipped for a system-value constant, which has no literal text to override, per L16's own literal-vs-system-value distinction). `DspfWriter.copyField` gained `options.constantValue` alongside its existing `options.name`/`options.location`. New/updated scenarios in `src/test/dspfWebview.test.js`: `runFileIndicatorScenario` (indicator used only on a file-level `CA03` now listed) and three new checks inside `runCopyFieldScenario` (renaming a field copy; rejecting a colliding name; editing a literal constant's copy text). |
| **L44** | **`Ctrl+D` (duplicate) and `Ctrl+V` (paste) never ask where the copy should land, unlike the "Copy" button in the field props panel** — reported directly: "ctrl+d and ctrl+v is not asking for the field/constant placement positions similar to copy field button." Confirmed: the Copy button (`beginCopyPlacement`, Task L36) already enters the same click-to-place flow "+ Field"/"+ Constant" use, letting the person click the canvas to choose exactly where the copy lands (and, since Task L43, rename it there too) - but `commitCopy` (single-field Ctrl+D) and `commitPaste` (single-field Ctrl+V) both instead called `DspfWriter.copyField` directly with no `options.location`, landing the copy at its own hardcoded default (one row below the original, same column) with no placement step at all. | L36/L43 (the placement flow this now reuses) | **done (v0.10.23)** — single-field Ctrl+D (`commitCopySelection`'s own single-selection branch) and single-field Ctrl+V (`commitPaste`) now both call `beginCopyPlacement` - the exact same function the Copy button calls - instead of inserting immediately; `commitCopy`, whose only caller this replaced, was removed as dead code. `renderCopyPlacementProps`'s own `targetRec` was already "whichever record the placement click happened over" rather than hardcoded to the copy's origin record, which is exactly Ctrl+V's own cross-record paste case, so no changes were needed there. The global keydown handler also gained a guard (`if (placementMode || pendingPlacement) return;`) so Ctrl+D/Ctrl+V/Ctrl+X/Ctrl+C/Delete/arrow-nudge can't fire a second time and clobber a placement that's already pending - Escape already existed to back out of one. **Deliberately out of scope:** multi-select (2+ fields) Ctrl+D/Ctrl+V - there is no "click to place a whole block" UI anywhere in this codebase today (the Copy button, and so this shortcut's own fix, only ever place ONE field/constant at a time), so the existing uniform-delta immediate-placement block-duplicate/paste behavior (`pasteFieldsBlock`, Task L10) is unchanged; raise a new task if block placement is wanted too. Updated `runCopyFieldScenario`'s own Ctrl+D-on-a-constant block and both Ctrl+V cases in `runNudgeCutCopyPasteScenario` (`src/test/dspfWebview.test.js`) for the new click-to-place flow, plus a new check that a second Ctrl+D fired mid-placement doesn't clobber the pending one; `multiSelect.test.js`'s own Ctrl+X/Ctrl+V coverage (a 2-field block) needed no changes, confirming the scoping boundary held. |
| **L45** | **Record-level comments had no "insert at this line #" option, only file-level did - the machinery existed, it just was never turned on there** — reported directly: "Comment line new insert line option or update apply option is not yet build[,] new line sequence is build but not used yet." Confirmed: Task L42 built `DspfWriter.addComment`'s `desiredLine` parameter and `wireCommentsSection`'s matching 3rd `commitInsert` argument generically, and wired both all the way through for the file-level Comments tab (`commentsListHtml(fileComments, 'filecomments', true)`) - but the record-level Structure tab's own Comments section called `commentsListHtml(..., recPrefix)` with `allowCustomLine` left `undefined` (so its "Line #" box never rendered at all) AND its own `commitInsert` callback was still the pre-L42 two-argument shape (`(comments, fallbackAfterLine) => ...`), silently dropping `wireCommentsSection`'s 3rd argument even if a line number had somehow been supplied. | L13 (introduced the comments sections), L42 (built the `desiredLine` machinery this turns on) | **done** — `commentsListHtml(DspfWriter.getRecordComments(model, rec), recPrefix, true)` now passes `allowCustomLine`, and the record-level `wireCommentsSection` call's `commitInsert` callback now accepts and forwards `desiredLine` to `DspfWriter.addComment`, identically to the file-level call. No changes needed to `DspfWriter.addComment`/`wireCommentsSection` themselves - both already supported this; only the record-level call site hadn't been updated to use it. New tests in `src/test/dspfWebview.test.js`'s `runCommentsScenario`: a record-level add-comment-line input existence check (replacing the old check that asserted it was ABSENT) and a full round-trip (typing a target line, clicking Add, confirming the new comment lands exactly there and the following field is pushed down by one line, mirroring the existing file-level line-number test right above it). |
| **L46** | **"+ Add comment" always inserted a blank comment line, even once L45 let you choose WHERE it landed - the wording still had to be typed in as a separate edit right after.** Asked directly once L45 shipped: "Do I have option to enter the comment line text option in this build?" | L42 (introduced the add-row), L45 (the line-number half of the same add-row) | **done** — `commentsListHtml`'s add-row gains its own text input (`comment-add-text-input` - a distinct class from each existing row's own `comment-text-input`, visually identical via a shared CSS rule, but kept separate so the several existing `.comment-text-input` queries elsewhere in this file don't also pick up the add-row's), read by `wireCommentsSection` alongside the existing line-number input and forwarded as `commitInsert`'s new 4th argument through to `DspfWriter.addComment`'s existing `text` parameter (already supported everything needed - only the add-row UI and its wiring were missing). Left blank, adding still inserts an empty comment exactly like before. Both call sites (file-level and record-level, kept in lockstep since L45) updated identically. `.comment-add-row button`'s CSS dropped its own `flex: 1` (now `flex: 0 0 auto`) so the wider text input, not the button, claims the row's spare width. New tests in `runCommentsScenario`: file-level line+text together, and record-level line+text together (extending the existing L45 test rather than duplicating it), plus a blank-text-still-works check. |
| **L47** | **The comment add-row's line-number box, text box, and "+ Add comment" button were cramped enough to force a horizontal scrollbar on the panel** — reported directly with a screenshot: "line number input box is not at appropriate size and comment field is too small and + add comment is also on same line... I would display similar to how existing comments are displayed and instead x symbol to delete comment have + sign to add." Confirmed: the add-row (Tasks L42/L45/L46) had its own bespoke `.comment-add-row` flex layout - a 64px line-number input, a flexible text input, and a wide TEXT button all fixed-width-competing in the same ~280px-wide panel - which didn't fit, forcing the whole row (and so the panel) to scroll horizontally instead of the text input simply shrinking. | L42/L45/L46 (the add-row this restyles, not rebuilds) | **done** — the add-row now reuses the exact same `.field-order-row` class every existing comment row already has, instead of its own layout: the line-number input sits in roughly the same column the read-only "L{n}" badge occupies on rows above it (its own small `.comment-add-line-input` rule, since `.field-order-row` itself has no opinion on an input's width), the text input still gets the row's remaining space, and "+ Add comment" is now a plain "+" in a 22x22 square button - inherited for free from the existing generic `.field-order-row button` rule, identical in size/style to every row's own "x" delete button, no new CSS needed for it. Net effect: the add-row now reads as "the next (blank) row in the same list" rather than a separate control bar bolted on below it, and the width budget comfortably fits without scrolling (fixed overhead - padding + line input + gaps + button - is ~92px, leaving the rest of the panel for the text input, versus the old layout's line input + a much wider text button eating most of the row before the text input got anything). Along the way, found and fixed a LATENT test bug this change exposed rather than caused: `runCopyFieldScenario`'s own "Field order: Up/Down buttons" check queried `.field-order-row` with no scoping, which happened to only ever match the 3 field-reorder rows before because the add-row's old class never carried `.field-order-row` - now that it correctly does (same class, deliberately reused), that query needed the same `[data-idx]` scoping the app's own field-reorder wiring already uses to distinguish reorder rows from comment rows sharing the same visual class. | 
| **L48** | **Adding a keyword to a bare CONSTANT (no existing keywords) collapsed it onto the constant's own literal line instead of giving it a new one - reported directly from the Menu designer with two screenshots: a DDS source snippet showing real SDA's own expected output (each menu-option label fragment's `COLOR(BLU)` on its own dedicated line below the quoted literal, never sharing it), and the Style/Keywords panel used to apply it.** Confirmed by direct reproduction: `serializeFieldEntry` (`dspfWriter.js`) always let the field's FIRST unconditioned keyword ride the SAME physical line as the field's own position columns - a deliberate, correct, already-tested convention for a NAMED field (real SDA's own output legitimately shares a field's declaration line with one keyword, e.g. `FLDA 20I 2O 2 2DSPATR(HI)` - see `keywordLineLayout.test.js`'s existing coverage) - but the SAME rule was also being applied to a bare CONSTANT, which has no name/type/length on its own line at all, just the quoted literal. Real SDA never lets a constant share its line with a keyword, not even the first one added - confirmed against the reported screenshot, where a 5-fragment split menu-option label (Task M7's own multi-constant scenario) shows FIVE literal lines each immediately followed by its OWN separate `COLOR(BLU)` line, ten physical lines total, never combined. Reported together with a second symptom, \"menu color not displayed correctly\" - the same screenshot's DDS snippet was the expected/correct shape being compared against the tool's actual (buggy, single-line) output; no separate rendering-path bug was found on investigation (`DspfEngine.styleFromKeywords`/`resolveScreen` already read a field's keywords correctly regardless of which physical line they're written on, verified by rendering both the buggy and the fixed output through the same pipeline with identical results) - the reported color problem was this same line-collapsing bug, not a second, independent one. | L1 (the multi-instance-keyword foundation `groupKeywordsByCondition` already builds on), M1/M7 (the menu-designer Style picker this was reported from) | **done** — `serializeFieldEntry` now takes a `isBareConstant` branch (`field.nameType === 'CONSTANT' && field.constantValue != null`): for a bare constant, `firstUnconditioned` is always empty (nothing rides the content line) and EVERY keyword, including what would have been the first, flows through the existing dedicated-line `restKeywords`/`groupKeywordsByCondition` path instead - the exact same per-keyword-own-line machinery Task L1's `groupKeywordsByCondition` fix already built for every keyword PAST the first. A named field (`isBareConstant` false) is completely unaffected, keeping its own established, tested "first keyword rides the line" convention. Applies uniformly everywhere `serializeFieldEntry` is used - `applyFieldUpdate` (editing an existing constant's keywords, the exact menu-designer path reported) and `insertField` (a brand-new constant created with keywords already attached) both go through it, so newly-INSERTED constants get the same correct behavior, not just edited ones. Updated the one existing test that had encoded the OLD (buggy) behavior for constants (`keywordLineLayout.test.js`'s own "Constant fields: the literal + its FIRST keyword may share the content line" scenario, now asserting the opposite - neither the first nor second keyword ever shares the literal's line) and added a new scenario reproducing the exact reported case (a single constant with zero keywords, adding COLOR through `applyFieldUpdate` the same way the Style picker's "+ Add" does, asserting the result is two separate lines, not one, and that it round-trips correctly). See `src/test/keywordLineLayout.test.js`. |
| **L49** | **Field data type (character/numeric) and usage (I/O/B) had no distinct visual representation in the design canvas — all named fields showed the same `XXXXXXXX` filler with no way to tell at a glance whether a field was input-capable or display-only.** IBM SDA's own Design Image uses the 5250 terminal's input-field underline attribute to distinguish input (I) and both (B) fields from output-only (O) fields, and uses `X` vs `9` placeholder characters to distinguish character from numeric. iSDA's canvas already had the correct X/9 placeholder text (via `dspfEngine.placeholderChar`), but (1) the resolved field object never forwarded `field.dataType` to the render layer so no CSS class could be emitted for it, and (2) no usage-based CSS class was emitted at all — `field.usage` appeared only in the hover tooltip as a bare `[B]`/`[I]`/`[O]` letter. | — (standalone; engine + CSS only, no parser/writer model changes) | **done** — `candidates.push()` in `resolveRecordFields` now forwards `dataType: field.dataType` alongside the existing `usage` (already forwarded). `renderFieldDiv` emits two new class groups for `nameType === 'FIELD'` divs only (never constants or help specs, which have no usage/dtype of their own): `dspf-usage-i`, `dspf-usage-o`, or `dspf-usage-b` from `f.usage`; and `dspf-dtype-char` or `dspf-dtype-num` from `f.dataType` (using the same S/Y/N/D/F numeric-type set `placeholderChar` already uses). Both `buildWebviewTemplate.js` and `buildMenuWebviewTemplate.js` have matching CSS: `.dspf-field.dspf-usage-i, .dspf-field.dspf-usage-b { text-decoration: underline; text-underline-offset: 3px; }` — the 5250 input-field underline, now matching IBM SDA Design Image exactly. Output (O) fields need no extra rule: the ABSENCE of the underline is the correct visual distinction. The `dspf-dtype-*` classes carry no color override (the X / 9 placeholder text already distinguishes character from numeric at a glance); they are emitted as CSS hooks for future theming or targeted JS queries. The hover tooltip was also improved from the bare `[B]` / `[I]` / `[O]` letter to a human-readable `· Both (B) · Character` / `· Output (O) · Signed numeric` suffix, plus a descriptive data-type label per field. 18 new checks in `src/test/dspfEngine.test.js` (Task L49 scenario). |
| **L50** | **The comment add-row's line-number box was still noticeably wider than the read-only "L{n}" badge it's meant to line up under, in both the file-level Comments tab and record-level Comments — Task L47 fixed the horizontal-scroll problem but left the two boxes visually mismatched.** Reported directly with screenshots of both scopes: "Line number box is too wide compared to existing line box, comment text box too small or too narrow." Confirmed: L47's own `.comment-add-line-input` rule set a flat `width: 46px` / `font-size: 11px` / `padding: 4px 2px`, chosen to roughly fit the row rather than to match the sibling `.comment-line-badge` it sits above (that badge is auto-width off its own shorter "L{n}" text at `font-size: 10px`, `padding: 2px 5px`) — every pixel the number input claimed beyond the badge's own footprint came directly out of `.comment-add-text-input`'s `flex: 1` share, since both boxes live in the same fixed-width `.field-order-row`. | L47 (the row layout this narrows further, not rebuilds) | **done** — `.comment-add-line-input` narrowed to `width: 30px`, `padding: 2px 3px`, `font-size: 10px`, sized to sit in the badge's own column instead of overshooting it; the freed width goes straight to `.comment-add-text-input`. No JS/data changes — CSS-only, same as L47. |
| **L51** | **L50's own fix never actually rendered — reported directly with screenshots: the add-row's "Line #" box was STILL the full row width, cramping the text input and "+" button, even after L50 narrowed `.comment-add-line-input` to `width: 30px`.** Root-caused (not just re-applied L50's numbers, which were already correct on paper): this stylesheet's generic `select, input[type=text], input[type=number] { width: 100%; ... }` rule (near the top of the file) has HIGHER CSS specificity than a bare class selector — a type selector plus an attribute selector outweighs one class, regardless of which rule appears later in the source — so it always won `width: 100%` (and every other overlapping property: background, color, border, padding, font-family, font-size) over `.comment-add-line-input`, independent of L50's edit. Combined with this input's own `flex: 0 0 auto` (flex-basis: `auto`, so the browser falls back to the `width` property to size it in a flex row), the losing `width: 30px` meant the box rendered at the row's full width the whole time — L50 shipped a correct-looking diff that had zero visual effect. Second, independent report in the same message: newly-created fields with Usage `O` wrote a BLANK column 38 instead of the literal letter `O` (`chars[37] = field.usage && field.usage !== 'O' ? field.usage : ' '`) — DDS spec-wise a blank column 38 is a valid synonym for Output and this tool's own parser already reads it back as `'O'` (`parseUsage(usageRaw) or 'O'`), so this was not a round-trip bug, but the person asked directly for the letter to be written explicitly. | L47/L50 (the row/box this actually fixes vs. re-describes), L49 (introduced the `usage`-aware canvas rendering this column feeds) | **done** — (1) rescoped the selector to `.field-order-row .comment-add-line-input` (two classes), which unconditionally outweighs the generic rule's type+attribute selector no matter the source order; verified directly by comparing `getComputedStyle(...).width` before (`100%`) and after (`30px`) the change in a jsdom harness, not just by re-reading the CSS text. (2) `serializePositionalCols` now writes `chars[37] = field.usage or 'O'` — an explicit `'O'` for any field whose usage is `'O'` or unset, while `DspfParser`'s existing blank-defaults-to-`'O'` fallback is untouched, so pre-existing DDS source with a blank column 38 (written by iSDA before this fix, or by any other tool) still parses identically. New coverage: `src/test/dspfWriter.test.js`'s "Task L51" scenario (explicit `'O'` on write for both an explicit and an unset `usage`, round-trip back to `'O'`, and a legacy-blank-column-38 file still parsing as `'O'`). |
| **L52** | **With "Track modifications" enabled, editing a multi-line/multi-fragment value corrupted the field entirely instead of tracking it cleanly — reported directly with screenshots: editing a menu option's label text (spanning two separate CONSTANT fragments, "Vendor Master File" + "Data Maintenance") into one longer piece of text made the field vanish from both the Options panel and the canvas.** Root cause was in the ONE function both designers share for this (`applyModificationTracking` in `dspfWriter.js`, ported to the menu designer verbatim by M8's own "needed zero changes" claim - that claim held for the simple 1-old-line/1-new-line case M8's own test covered, but not this one): its diff-mid loop pushed `comment(old[i])` then `tag(new[i])` **per index**, so whenever the changed region had more than one line on either side, the output interleaved `[comment old0][new0][comment old1][new1]` instead of grouping every comment together ahead of every new line. That's not just cosmetically wrong - DDS requires a continuation line (`-`/`+` in column 80) to immediately follow the line it continues, so an unrelated commented-out line landing between a freshly-written line and its own continuation corrupts the field outright; the parser could no longer reconstruct it, which is exactly why it silently disappeared rather than just looking odd. Confirmed this reaches the DSPF designer too, sharing the exact same function - any field whose old AND new content both span more than one physical line (not just the menu-option-label case) hits the same interleaving. | L38/M8 (the modification-tracking feature this is a correctness bug IN, not a new feature) | **done** — `applyModificationTracking`'s single interleaved loop split into two sequential passes: pass one comments out every OLD mid-region line that changed or was removed, in its own original order, entirely before pass two appends every NEW mid-region line that's new or changed, in its own new order and tagged - so a comment block and a new-content block never end up sandwiching one another regardless of how many lines either side has. A line that's genuinely identical at the same index in both old and new mid (a rare "sandwiched exact match" case, possible even after the existing prefix/suffix trim) is carried through bare, once, in the second pass's own position - correct either way, since it needs neither commenting nor a tag. New coverage in `src/test/dspfWriter.test.js`'s own "Task L52" scenario: the exact reported multi-fragment menu-option-label consolidation (mirroring `writeOptionLabel`'s own delete-then-rewrite shape directly, not just calling the tracking function with contrived arrays), asserting the comment block and new-content block are each contiguous and the comments entirely precede the new lines, that the field survives re-parsing with its full new text (the exact thing that silently broke before), and that no stray duplicate field resurfaces from the deleted fragment; plus a plain 1-old/1-new in-place edit confirming the common case is unaffected by the two-pass split. Full suite, zero failures. |
| **L53** | **"+ Fields from database file" (Task L14) always inserted the whole checked batch immediately, stacked one row below the record's own current last field at a fixed column 2 - no way to choose where it actually lands, unlike "+ Field"/"+ Constant"/"Copy field", which all go through click-to-place.** Asked directly: "when I use fields from database file currently places/add it at last available line, I want it to give me click to place option like we have for add field or copy field." Confirmed: `showDatabaseFieldsPicker`'s "Add fields" button posted `addFieldsFromDatabase` straight to the extension host the moment it was clicked, and `handleAddFieldsFromDatabase` always computed its own starting `{line, column}` from the record's existing fields - there was no click-to-place step anywhere in this flow, unlike every other field-creation entry point. | L14 (the feature this changes), L36/L44 (the existing single-field click-to-place flow this reuses, rather than duplicating) | **done** — a new `'DBFIELDS'` placement mode alongside `'FIELD'`/`'CONSTANT'`/`'COPY'`: clicking "Add fields" now calls `beginDbFieldsPlacement(library, file, fields)`, which snapshots the checked fields into a new `pendingDbFieldsSource` and arms `'DBFIELDS'` mode (closing the dialog immediately) instead of posting the edit - the exact same `screenOutput` click handler and `pendingPlacement` state every other placement kind already shares captures the click. A new `renderDbFieldsPlacementProps` panel (branched off `renderPlacementProps` for the `'DBFIELDS'` kind, same pattern as `renderCopyPlacementProps` for `'COPY'`) shows the source library/file, the field names about to be added, and editable Start line/Column inputs pre-filled from the click; confirming posts `addFieldsFromDatabase` with a new `location: {line, column}` payload. `handleAddFieldsFromDatabase` in `extension.ts` now starts its per-field stacking loop from `msg.location` when present, falling back to the original "one row below the last existing field, column 2" default when it's absent (so any other caller of this same message keeps working unchanged). The database-fields picker no longer needs a target record to open with at all (browsing a file's fields was never actually tied to one) - the target is whichever record is currently being previewed when the placement click lands, same "click decides the target" reasoning `renderCopyPlacementProps` already established for Copy/Paste's own cross-record case. `pendingDbFieldsSource` is cleared alongside `pendingPlacement` on cancel/Escape/breadcrumb-navigation, same discipline `pendingCopySource` already follows. Updated `runDatabaseFieldsPickerScenario` in `src/test/dspfWebview.test.js` for the new two-step flow (dialog closes and arms placement mode without posting anything, a canvas click opens the pre-filled placement form naming only the checked fields, confirming posts the edit at the clicked location); added a new `extension.test.js` case covering `handleAddFieldsFromDatabase`'s `location` param directly (confirms the batch starts exactly at the given line/column and each field still stacks one row apart, rather than relying on the record's existing fields). Full suite: 33/33 files, zero failures. |
| **L54** | **Reported directly: after adding a comment WITHOUT a line number it appeared right away, but adding one WITH a line number to insert at did not show up.** Root-caused by direct reproduction rather than guessed at: `DspfWriter.addComment`'s `desiredLine` path (Task L42) splices the new raw comment line in verbatim at whatever physical line number was typed, with no awareness that some physical lines are the MIDDLE of a DDS function-area (+/-) continuation - a keyword's parameter list wrapped onto a second physical line, whose own columns 7-44 must stay blank for `DspfParser`'s own `buildLogicalEntries` to keep resolving it as one continued entry. A comment line's column 7 is always `*`, so splicing one into the middle of a continuation desyncs that resolution: the interrupted keyword's text silently truncates at the dangling `+`/`-`, and what was meant to be ITS continuation instead gets re-parsed as an unrelated new keyword-only line - confirmed by direct reproduction, this doesn't just misplace the new comment, it can truncate the interrupted keyword's own parameter list and spawn a garbage keyword out of what's left over. That corruption, not the comment itself, is almost certainly why a comment inserted this way could appear to vanish along with whatever record/field content sat around it, rather than landing where asked. | L42 (introduced the `desiredLine` input this hardens) | **done** - added `continuationChainStarts`, which walks `sourceLines` with the exact same continuation-tracking `buildLogicalEntries` itself uses, mapping every physical line to the 1-based line number that starts its own continuation chain (itself, if it isn't a continuation line at all). `addComment` now looks up the requested `desiredLine` in that table before splicing, and if it would land in the middle of a chain, pulls the insertion back to the chain's own first line instead - the new comment ends up right before the whole multi-line entry, never inside it. A target line that isn't part of any continuation is spliced in exactly as asked, unaffected. New coverage: `src/test/dspfWriter.test.js`'s "Task L54" scenario, built on a `VALUES` keyword long enough to force a real engine-produced continuation (via `insertField` itself, not hand-typed columns) - confirms the continuation-chain lookup table is correct, confirms a mid-continuation insert gets pulled back to the chain's start with zero parse errors and the interrupted keyword's parameters still fully intact, and confirms an insert outside any continuation is completely unaffected by the guard. |
| **L55** | **Reported directly: the add-row's "Line #" box shows the browser's default up/down spinner arrows, which aren't wanted - it's meant to read as a small line-number badge to type into, not a stepper control.** | L47/L50/L51/L54 (the box this styles further, not rebuilds) | **done** - added `-moz-appearance: textfield` (Firefox's own switch to drop its built-in number spinner outright) plus `::-webkit-outer-spin-button`/`::-webkit-inner-spin-button` rules with `-webkit-appearance: none` (Chrome/Edge/Safari don't respect the `-moz-` property and need their own pseudo-elements hidden instead), both scoped to the same `.field-order-row .comment-add-line-input` selector L51 already had to use to reliably win the cascade. Typing digits and the field's own `min="1"` attribute are unaffected - only the mouse-driven increment/decrement UI is gone. New coverage: `src/test/dspfWebview.test.js`'s "Task L55" checks confirm the stylesheet the webview actually serves carries both suppression rules, scoped correctly. |

---

## Menu designer

Menu designer's own "Known limitations"/"Planned enhancements" items from
README, tracked here (prefixed `M` — see this doc's intro) now that
they're broken into actionable tasks instead of being untagged. None of
these depend on each other or on any `L`-prefixed DSPF designer task, so
all six can start in parallel with each other AND with any open
`L`-prefixed task above. (M6 wasn't sourced from README's own lists —
same "found by auditing the codebase, not just README" situation Task L8
was in — see M6's own row for where it actually came from.)

| Task | Description | Depends on | Status |
| --- | --- | --- | --- |
| **M1** | **Menu designer options get the same dedicated-picker treatment** the DSPF designer's keywords now have. Confirmed relevant: a menu option is just a DDS `CONSTANT` under the hood (see `buildMenuWebviewTemplate.js`'s own `extractMenuOptions`/`updateOptionConditions`), and real SDA's "Menu design" mode is the SAME underlying DSPF-record-design engine with menu-specific conventions layered on top (numbered options, `MNUCMD` companion file) — so it supports styling option text with `COLOR`/`DSPATR` the same as any other constant. Checked the current option row UI (`renderOptions()`) and confirmed the gap is real: it has label text, command mapping, and Conditioning, but NO keyword editor at all - not even the raw generic one - so there's currently no way to add `COLOR`/`DSPATR` (or any other keyword) to an option through the UI, only by hand-editing the DDS source outside iSDA. The per-option Conditioning panel already follows a similar structure to build on — survey it first for what's directly reusable vs. menu-option-specific before designing new UI from scratch. | — (standalone) | **done** — matched against real SDA's own "Set Field Attributes" screen (`docs/sda-reference/screens/menu-designer/option-field-attributes/`, a user-supplied photo — Color/High intensity/Reverse image/Blink/Underline, the same shape as the field-level Color & attributes picker). Each option row's `renderOptions()` gained a new "Style" accordion, built exactly like the existing Conditioning one, reusing `WebviewClientHelpers.colorAttrStatesHtml`/`wireColorAttrStatesEditor` (Tasks D1/D4's own component - COLOR/DSPATR) plus the generic `keywordEditorHtml`/`wireKeywordEditor` underneath for anything else, matching every other picker's "dedicated + raw fallback" shape. New `updateOptionKeywords()` mirrors `updateOptionConditions()`'s own sync logic - writes to `numberField`, then to the separate `labelField` too for the split-constant form, so number marker and label text never drift apart in styling. No new `dspfWriter.js` code needed - the existing `getColorAttrStates`/`setColorAttrStates`/`keywordEditorHtml` primitives already operate on a plain keywords array. See `src/test/menuWebview.test.js`'s `runOptionStyleScenario` and CHANGELOG `[0.9.81]`. |
| **M2** | **`CRTSRCPF` support in "Create New Menu."** Confirmed still open by code inspection: `createRemoteMenuMembers()` in `src/extension.ts` prompts for the source physical file with "must already exist - ADDPFM does not create it" and never calls `ensureSourcePhysicalFileExists()` (the helper `createRemoteMember()`/`createNewDspf`'s own remote path already uses for Task L4). Port L4's fix to this function too — same gap, same pattern. | L4 (done — port its pattern) | **done** — ported `ensureSourcePhysicalFileExists()`'s CHKOBJ/CRTSRCPF fallback into `createRemoteMenuMembers()`, the same as `createRemoteMember()` already had. A single CHKOBJ/CRTSRCPF pass covers both the MNUDDS and MNUCMD companion member up front (they share the same qualified source file), before either member's own `ADDPFM`, rather than checking twice. Prompt text updated to match L4's own wording ("created automatically with CRTSRCPF if it doesn't already exist"). New scenarios in `src/test/createNewMenu.test.js`: CHKOBJ-missing+decline, CHKOBJ-missing+confirm (verifies exactly one CRTSRCPF call covers both members), and CRTSRCPF-itself-fails. |
| **M3** | **Menu option delete reference scan.** Deleting an option doesn't scan for other references to it, unlike rename. Port Task L2's field-deletion reference-check pattern (advisory `findLikelyNameReferences`-style scan, blocking confirmation dialog naming what was found) to menu option deletion. | L2 (done — port its pattern) | **done** — a numbered menu option has no DDS-identifier "name" the way a field/record does (`extractMenuOptions` only ever builds these from bare, unnamed `CONSTANT` fields), so `findLikelyNameReferences` itself (word-boundary matching an identifier) has nothing to search *for* here — there's no keyword that references a menu option's constant by name the way `REFFLD`/`SFLCTL`/`WINDOW` do. The genuinely real, domain-specific analog instead: real SDA menus commonly define the SAME menu twice as separate record formats for different display sizes (`*DS3`/`*DS4` twins), each independently listing the same numbered options — deleting option N from one record while a sibling record still independently defines its OWN option N is the same kind of easy-to-miss duplication L2 protects against, just "another record defines the same option number" standing in for "another keyword references this name." New `findOtherOptionsWithSameNumber` scans every OTHER record (scoped per-record via `extractMenuOptions(model, recordName)` — calling it file-wide without a `recordName` dedupes globally by number, which would silently hide exactly the duplicates this scan needs to find) for a matching option number; `commitDeleteOption` blocks on a ported `showConfirmDialog` (verbatim from the DSPF designer's own, CSS included) naming the record/line found, same "common case deletes immediately, no added click" stance `commitDelete` takes for a field. See the Task M3 scenario appended to `runCrossRecordOptionScopingScenario` in `src/test/menuWebview.test.js`. |
| **M4** | **Companion commands file (`QQ` member, or local/streamfile sibling) sync — done.** Previously all three edit paths (per-option command change, swap, delete-option cleanup) computed the FULL new companion-file text against the webview's own in-memory `commandText` (captured once at `resolveCustomTextEditor()` time) and posted it wholesale via `applyMenuCmdEdit`; two designer instances (different VS Code windows/sessions on the same remote member) each holding a stale copy meant one's write could silently clobber the other's unrelated edit. Fixed with a read-modify-write redesign: the webview now posts a NEW message, `applyMenuCmdOptionEdit`, with structured `{ numberValue, command }[]` edits (one entry for a single-option change or delete cleanup, two for a swap, applied together as one write so a reader never observes a half-swapped state) instead of full text; the extension host ALWAYS re-reads the current base text immediately before applying — from the open document's live buffer if the companion file is open in its own tab, else fresh from disk via `workspace.fs.readFile` — then applies the edit(s) via `MnuCmdEngine.applyOptionCommand` and writes back, closing the actual race (an edit to option 3 from one instance can no longer clobber a concurrent edit to option 7 from another). After a successful write the host echoes the merged text back to the ORIGINATING webview via a new `menuCmdSaved` message (its own write is never echoed via the existing `externalCommandUpdate`/change-listener path, since `applyingCommandFromWebview` suppresses that specifically to avoid a loop), so that webview's own `commandText`/`cmdModel` stay in sync with what's actually on disk. A genuinely concurrent edit to the EXACT SAME option is still last-write-wins (no CRDT/OT) — an accepted, inherent limit for a plain-text companion file; the data-loss risk this task called out was UNRELATED edits stomping each other, which is what's fixed. See the Task M4 scenarios in `src/test/menu.test.js` (including a dedicated test proving an unrelated concurrent edit survives) and `src/test/menuWebview.test.js`. | — (standalone) | **done** |
| ~~**M5**~~ | ~~**Support menu types beyond `TYPE(*DSPF)`** in Compile Menu.~~ **Reclassified as not applicable to iSDA's scope, not a planned enhancement** — verified against IBM's own CRTMNU `TYPE()` reference and independent sources: `TYPE(*PGM)` menus call a program directly with NO display file or screen at all (nothing for a visual DDS screen designer to design); `TYPE(*UIM)` menus are written in UIM's own tag-based panel-group language (`PNLGRP` source, compiled via `CRTPNLGRP`), a completely different, non-DDS markup that would need its own dedicated designer, not an extension of this one — multiple independent sources describe UIM as "totally bypassing SDA" for exactly this reason. `TYPE(*DSPF)` is the ONLY menu type backed by an actual DDS-designed screen, which is why it's the only one real SDA's own "Menu design" mode (and this DSPF/MNUDDS-focused tool) ever produces. Moved to README's Known limitations as an inherent scope boundary instead. | — | **not applicable** |
| **M6** | **Menu designer's left/right panels had no hide/minimize control** — one of the "Common" issues in the source doc (`docs/sda-reference/source/SDA-Issues-and-Enhancement-Screenshots.docx`, "Make the Right side and left side panel hide or minimizable, so that we won't have issue when working with 27x132 *DS4 display size"), meant to apply to BOTH designers. The DSPF designer already had this (`.panel-toggle-btn`/`.panel-collapsed`, `buildWebviewTemplate.js`); the Menu designer never got the matching implementation and this gap was never captured as a tracked task or a README Known limitation - found by auditing the codebase directly against the source doc rather than against README's own (incomplete, on this point) lists. Done — ported the identical pattern into `buildMenuWebviewTemplate.js`: same `.panel-toggle-btn`/`.panel-collapsed` CSS, `#leftPanelToggle`/`#rightPanelToggle` buttons wrapping `aside`'s and `.options-panel`'s content in a `.panel-body` div each, and the same session-only (not persisted) `applyPanelCollapse()` JS, just with the menu template's own 200px/340px column widths instead of the DSPF designer's 240px/300px. See the new panel-collapse scenario appended to `src/test/menuWebview.test.js`. | — (ports the DSPF designer's existing pattern) | done |
| **M7** | **A menu option's label split across 3+ separate DDS constants on one line lost every fragment past the first, and every edit that DID reach an option only ever touched ONE label fragment - reported with two screenshots: (1) "11."/"Back"/"to"/"Main"/"Menu" as five separate constants showed as just "Back" in the Options panel; (2) applying COLOR to an option only colored part of its visible text, read as "the color isn't applying at all."** `extractMenuOptions` (see M1's own `updateOptionKeywords`) paired a bare number marker with only the SINGLE next constant on its line - real DDS sources commonly break a multi-word option label into several constants at different columns rather than one long quoted literal (the same split-constant technique `runSplitConstantScenario` already covered for the 2-fragment case, just never generalized past exactly one label fragment). | M1 (introduced `extractMenuOptions`/`updateOptionKeywords`, both extended here) | **done** — `extractMenuOptions` now collects EVERY constant to the right of a number marker on its line, up to (not including) whichever one starts the next option or the end of the line, joining their text with spacing computed from each fragment's own column gap (so "Back"+"to"+"Main"+"Menu" at columns 10/15/18/23 reads "Back to Main Menu", not "BacktoMainMenu"). New `option.labelFields` (array, `option.labelField` kept as `labelFields[0]` for any reader that only needs "a" representative field). Every consumer that used to touch a single `labelField` now loops over the whole array: `updateOptionConditions`/`updateOptionKeywords` (conditioning and COLOR/DSPATR/etc now sync across every fragment, not just the first - this is what actually fixes the color report), `performDeleteOption` (deletes every fragment), and `copyOption` (duplicates every fragment, each to its own original column on the copy's row). `writeOptionLabel` (editing the label text) deliberately does NOT try to preserve a multi-fragment shape through a wording change - it collapses down to a single constant, writing the full new text into the first fragment and deleting the rest, since there's no principled way to redistribute arbitrary new text back across however many fragments there used to be; conditioning/style edits (which don't change the wording) DO keep every fragment in sync via the loop above. New `runMultiFragmentLabelScenario` in `src/test/menuWebview.test.js` (full label read-back, COLOR syncing across all 5 constants, label-edit collapsing to 2 constants, and copy duplicating all 5). |
| **M8** | **Source modification tracking, ported to the Menu designer** — Task L38's own row explicitly deferred this: "the Menu designer has no single 'commit' choke point the way the DSPF designer's `commitSourceChange` does, so wiring it there is a larger, separate lift; raise a new M-prefixed task if that's wanted." Requested directly once that gap was noticed. | L38 (done — reuses its writer primitives verbatim, ports its UI/wiring) | **done (v0.10.33)** — the actual primitives (`commentOutLine`/`buildModTag`/`appendModTag`/`applyModificationTracking` in `dspfWriter.js`) needed zero changes, being generic over any (oldLines, newLines) pair regardless of which designer produced them. The real work was the choke point L38's own row called out as missing: `buildMenuWebviewTemplate.js` had 12 separate call sites (option label/conditioning/keywords edits, add/copy/delete/swap option, file attributes, record rename/add/copy/delete), each duplicating the same split-source/apply-writer-call/join/reparse/`postMessage('applyEdit')`/`renderAll()` skeleton inline rather than funneling through one function the way the DSPF designer's `commitSourceChange` does. All 12 are now refactored through a new `commitMenuSourceChange(transform, afterReparse)`, matching `commitSourceChange`'s own shape and semantics exactly (transform returning null/undefined is a no-op; `afterReparse`, run after the model is reparsed but before `renderAll()`, covers the handful of callers that need the fresh model for more work first - an advisory reference scan, a conditioning-panel Set cleanup, clearing the add-option form fields, or posting the companion `applyMenuCmdOptionEdit` message). `applyModificationTracking` is wrapped in as the same single post-processing step `commitSourceChange` already uses. Properties panel gained the identical "Track modifications" checkbox + 10-char tag box in the Options panel header (same session-only convention: starts from `isda.trackSourceModifications`/`isda.modificationTag`, never written back once touched directly). `extension.ts`'s `MenuDesignerEditorProvider` gained the same `sendModTrackingConfig`/`modTrackingConfigSub` pair `DspfDesignerEditorProvider` already had, reusing the existing shared `getModTrackingConfig()` helper verbatim (the setting is shared across both designers, not duplicated) - posts on `ready` and on any live `settings.json` change while the panel is open, disposed on panel close. New `src/test/modTrackingMenuWebview.test.js` (full jsdom scenario mirroring the DSPF designer's own `modTrackingWebview.test.js`: an option-label edit with tracking on comments out the old line and tags the new one; toggling tracking off mid-session reverts to plain in-place edits), plus a new Task M8 scenario appended to `extension.test.js` covering the config push/live-change/disposal on the menu provider specifically. All test files, zero failures. |

---

## Status

L1 through L9 (including the L5a-d and L5d-i/L5d-ii splits) and M1
through M7 are all `done`, except M5, which was investigated and
reclassified as `not applicable` (see its own row). Each of those row's
own Status cell already carries the full explanation for how and why it
closed, and `CHANGELOG.md`/git history has the exact commits.

**L10 through L15 are all now `done`.** L10 is multi-field select
(shift/ctrl-click + rubber-band drag-select) plus block move/copy/delete/
style, built on the existing `startGroupDrag`/`commitGroupEdit` machinery
generalized to an arbitrary selection (see its own row and
`CHANGELOG.md`'s 0.9.93 entry for the full list of what landed). L11
(ruler overlay) was built as specified, and later gained a crosshair
follow-up (position readout toggle) in the same session. L12 turned out
to already exist as the `p-center` "Center on screen" button in the
field props panel, before this brainstorm even started - reclassified
`done` before any work began on it, though its own row notes that
centering a BLOCK of fields together (now possible in principle since
L10's group-selection state exists) is a small, well-scoped follow-up if
wanted; L13 (Comments panel) was also built as specified. L14 (bulk "Add
fields from database file") was deliberately the lowest priority of this
batch when proposed, since it needed new Code for IBM i round-trips
(listing every field in a file via `DSPFFD`, not just resolving one
already-named field, plus a multi-select picker UI) rather than just UI -
see its own row for the full implementation. All five of L10-L14 came
from the same brainstorm of real-SDA-parity gaps (verified against IBM's
own SDA reference manual, not guessed), not sourced from README's own
lists the way most earlier tasks were. L15 (`MNUBARCHC` picker's text
input collapsing to ~0px in the properties panel's own default width)
was a standalone bug fix found by a real usage report, not part of that
same brainstorm - see its own row. L16 (system-value constants being
corrupted by editing) was likewise a standalone bug fix.

**L17 is also now `done`.** `DSPATR` referencing a hidden
"program-to-system" field (`USAGE(P)`) - real DDS's alternate,
indicator-free way of driving a display attribute from a runtime value -
wasn't shown in the Color & attributes panel and got silently dropped on
the next edit to that panel; see its own row for the full fix. Another
standalone bug fix found by a real usage report, not part of the L10-L14
brainstorm.

**L18 and L19 are also now `done`.** Both were found by direct user
report rather than sourced from README's own lists (same situation L8/M6
were in): L18 is the "IBM i: Connected/Not connected/Not installed"
badge (upfront visibility instead of finding out via a failed
Compile/Resolve/Add-from-DB click); L19 is the "Find field" search box
(live filtering across every record's fields/constants by name, with
cross-record jump). See their own rows for the full implementation.

**L20, L21, L23, L24, L25, and L26 are also now `done`**, all found via
a direct keyword-inventory audit against real SDA's own reference
screenshots (L22 tracks the audit itself): L20 is CNTFLD not being
selectable at all; L21 is CHGINPDFT's missing sub-flag checkboxes; L23
is KEYBRD's wrong value list; L24/L25/L26 are three keywords (MSGLOC,
TEXT, ENTFLDATR) that were entirely missing or only a free-text box,
the last two resolved via web research against IBM's own DDS Reference
rather than the reference screenshots alone.

**L22 is also now fully `done`.** Its own remaining item -
ROLLUP/ROLLDOWN (legacy alternate spellings of PAGEDOWN/PAGEUP)
synonym recognition - is fixed: `getFileFlagKeyword`/`setFileFlagKeyword`
gained an optional `altNames` parameter, and the record-level "Define
Indicator Keywords" screen (`getRecordIndicatorInstances`/
`setRecordIndicatorInstances`) got the equivalent fix; see L22's own row
for the full writeup.

**L27, L28, L29, and L30 are also now `done`**, all found via direct
user report in the same session: L27 is command keys (CAnn/CFnn) not
supporting indicator conditioning; L28 is the open file's own name
sitting too far down the left panel instead of right under the
"Screen Design"/"Menu Design" heading; L29 is windows with no
`WDWBORDER` anywhere rendering with a plain unstyled border instead of
the real DDS-documented period/colon default; L30 is the window move
handle snapping to the cursor instead of preserving the drag's grab
offset, making a window jump right the instant it was grabbed anywhere
but its exact top-left pixel. See each row for the full writeup.

**L32 is now `done`.** It was a deferred sliver flagged (but
deliberately not implemented) while fixing L29: a genuinely partial
`WDWBORDER` (e.g. only `*DSPATR` set) not getting IBM's own
per-sub-parameter defaults the way an entirely-absent one already did
(L29). `resolveWdwBorder`'s `parse()` now backfills the documented
default independently for whichever sub-parameter group(s) (`*COLOR`/
`*CHAR`) a present WDWBORDER keyword leaves unset, without disturbing
any group that IS explicitly set. See L32's own row for the full
writeup, including the existing tests updated to match the new
(visible, intentional) rendering change for partial-`WDWBORDER`
windows.

**L33 is now `done`.** It was the other deferred sliver flagged while
fixing L30: field-dragging (`startDrag`/`startGroupDrag`) had the
identical absolute-snap-to-cursor pattern that caused the L30
window-drag jump bug. Both functions now get the same
mousedown-event-threading, delta-from-grab-point fix L30 already
established, and the concern that flagged this as deferred (existing
pixel-exact test coverage needing re-verification) turned out to be a
non-issue - every existing drag test only ever asserted the mouse's
DELTA, never an absolute snap position, so none needed changes. See
L33's own row for the full writeup, including the new dedicated
off-origin-grab test.

**L31 and L34 are also now `done`.** L31: command keys support multiple
independently-conditioned instances of the same key number now (real
SDA's own F3-reads-"Exit"-vs-"Cancel" pattern) - a new index-based
`setCommandKeyAt`/`removeCommandKeyAt` pair in `dspfWriter.js`
addresses one specific instance without disturbing a sibling instance
of the same number, and `allCommandKeyNumbers()` replaces the old
already-used-number exclusion in the "+ Add command key" picker. L34:
its own speculative question (other legacy-keyword-synonym gaps beyond
ROLLUP/ROLLDOWN) turned up one confirmed pair, `CMP`/`COMP`, now
recognized and normalized the same way. See each row for the full
writeup.

**L35 is `not applicable`** - reported as "code for ibm i runcommand is
not valid, it should be code for ibm i runaction." Investigated against
Code for IBM i's own official developer docs rather than changed on the
strength of the report alone: `code-for-ibmi.runCommand` is confirmed
correct (it runs an arbitrary CL/PASE command, exactly what this
codebase needs for `CRTDSPF`/`CRTMNU`/`DSPFFD`); "Run Action" is a
different, unrelated end-user feature (runs a project-configured
`actions.json` entry) that would break every compile/DSPFFD call here
if swapped in. See L35's own row for the full citation.

**L37 is also now `done`.** A left-panel/properties-panel cleanup
reported directly: the "File" section-label moved up next to the
filename (and made bold), the redundant standalone "File attributes"
button was removed (the "File" crumb in the properties panel already
did the same thing one click away), and a new "Find keyword" quick-nav
was added to the top of the properties panel to jump straight to a
keyword no matter which tab it's under. See its own row for the full
implementation.

**L41 is now `done`** - reported with three screenshots (iSDA showing
no border at all, real SDA/5250 showing a solid blue box, and the DDS
source itself). Root cause: `*CHAR` was assumed to always be 8 separate
quoted single-character literals (this codebase's own written format),
but real-world DDS commonly writes it as ONE combined character-string
value instead - with the reported file's all-blank string, that put a
non-blank 8-character blob in position 0, wrongly triggering "char
mode" (which suppresses the plain colored box border) while rendering
nothing visible anywhere. See L41's own row for the full fix.

**L43 is also now `done`.** Two more directly-reported gaps: a
conditioning indicator used only on a file-level keyword never showed
up in the left-panel indicator list (`indicatorsForContext()` never
walked `model.fileKeywords`), and the copy-placement flow from L36 had
no way to rename a field copy or reword a constant copy - both had to
be fixed as a second edit right after placing. See its own row for the
full implementation.

**L39, L40, and L44 are also now `done`.** L39/L40 gave dragging (mouse
and arrow-key nudge, single-field and multi-select block) a real
boundary for the first time when the record being edited is itself a
`WINDOW`/`WNDSFL`/etc. record, or an `SFL`/`SFLCTL` pair - previously a
field could be dragged or nudged clean outside its own window/subfile
frame with nothing stopping it. L44 extended the click-to-place flow
`beginCopyPlacement` already gave the "Copy" button (Task L36) to
single-field `Ctrl+D`/`Ctrl+V` too, which had both still been
inserting immediately at a hardcoded offset with no placement step at
all. See each task's own row for the full implementation.

**L45 is also now `done`.** The record-level Comments section had no
"insert at this line #" option - not because it needed building, but
because the `desiredLine` machinery L42 built generically for exactly
this was only ever turned on for the file-level Comments tab, not the
record-level one right below it. See its own row for the full fix.

**L46 is also now `done`.** Once L45 made choosing WHERE a new comment
lands possible, the natural next question came right back: could the
wording be typed in at the same time too, instead of always landing
blank? It now can - the add-row gained its own text input alongside
the line-number one. See its own row for the full implementation.

**L48 is also now `done`.** Reported directly from the Menu designer
with two screenshots: adding a keyword to a bare CONSTANT with no
existing keywords collapsed it onto the constant's own literal line
instead of giving it a dedicated new one, the opposite of real SDA's
own output. The same report's second symptom ("menu color not
displayed correctly") turned out to be the same bug, not an
independent rendering-path issue - see L48's own row for the full
root-cause writeup and fix.

**L50 is also now `done`.** A follow-up to L47's own comment add-row
fix, reported directly with screenshots of both the file-level and
record-level Comments scopes: the add-row's line-number box was still
visibly wider than the read-only "L{n}" badge above it, at the text
input's expense. Narrowed to match the badge's own column width. See
its own row for the full fix.

**L51 is also now `done`.** Two independent reports in the same
message, both confirmed by direct reproduction: (1) L50's narrowing
never actually rendered - a higher-specificity generic `input[type=
number]` rule elsewhere in the stylesheet always won the width (and
every other overlapping property) over `.comment-add-line-input`'s
plain class selector, so the box stayed full-width the whole time
despite L50's own numbers being correct on paper; rescoping the
selector to two classes fixes it unconditionally, confirmed via a
before/after `getComputedStyle` check rather than just re-reading the
CSS. (2) newly-created fields with usage `O` wrote a blank column 38
instead of the literal letter - valid per the DDS spec and this tool's
own parser already round-tripped it correctly, but written explicitly
now per direct request. See its own row for the full writeup.

**Standalone bug fix (2026-09-03, found by a real screenshot report,
not part of any brainstorm above):** "Add fields from database file"
(L14) was failing with `DSPFFD failed for .../...: Error: command
'code-for-ibmi.runCommand' not found` even though Code for i showed as
installed. Root cause: `fetchReferencedFieldAttributes`,
`fetchDatabaseFileFields`, `compileMenu`, and `compileDspf` all ran
their CL commands through
`vscode.commands.executeCommand('code-for-ibmi.runCommand', ...)` - a
command Code for i registers at its OWN activation time, not declared
in `contributes.commands`, so VS Code's usual auto-activate-on-command
mechanism doesn't cover it. The existing `ext.activate()` guard (added
for the L18/createNewDspf "installed but not yet active" bug) only
narrows that race, it doesn't close it. All four now call
`.runCommand()` directly on the connection object from
`instance.getConnection()` instead - the same object
`fetchReferencedFieldAttributes`/`fetchDatabaseFileFields` already
trusted for `runSQL()` without ever hitting this problem, since that
path never goes through the VS Code command registry at all.
Follow-up UX improvement in the same pass: Compile Display File,
Compile Menu, and "+ Fields from database file" are now hidden
outright (not just left clickable and doomed to fail) whenever the L18
connection badge reports anything other than connected - they
reappear the moment a fresh `codeForIStatus` reports `connected:
true`.

**M8 is also now `done`.** L38's own row explicitly deferred porting
source modification tracking to the Menu designer, since (unlike the
DSPF designer's `commitSourceChange`) it had no single "commit" choke
point every edit funneled through. Requested directly once that gap
was noticed. All 12 of the Menu designer's scattered
split/write/postMessage-`applyEdit` call sites are now refactored
through a new `commitMenuSourceChange`, matching `commitSourceChange`'s
own shape - the same "Track modifications" checkbox + tag box, the
same `modTrackingConfig` push wired into `MenuDesignerEditorProvider`.
No changes needed to the underlying writer primitives themselves. See
M8's own row for the full writeup.

**L52 is also now `done`, and corrects part of M8's own claim above.**
M8 said the shared `applyModificationTracking` primitive "needed zero
changes" to work for the Menu designer - true for the simple case its
own test covered, but reported directly (with screenshots) once a
multi-fragment menu option label edit hit a real bug in that same
primitive: its diff loop interleaved comment/new-line pairs per index
instead of grouping them, which corrupts DDS continuation lines and
silently loses the field. Fixed once in the shared function, so both
designers get the fix together. See L52's own row for the full
root-cause writeup.

**L53 is also now `done`.** Asked directly: "+ Fields from database
file" (L14) always dropped the whole checked batch immediately, one
row below the record's own last field at a fixed column - no
click-to-place step at all, unlike "+ Field"/"+ Constant"/"Copy field".
Now reuses that same click-to-place flow (`beginCopyPlacement`'s own
`placementMode`/`pendingPlacement` machinery, extended with a new
`'DBFIELDS'` mode) so the batch's starting position comes from wherever
the canvas is clicked instead of always landing at the same fixed spot.
See L53's own row for the full writeup.

**L54 is also now `done`.** Reported directly: a comment added without
a line number appeared right away, but one added WITH a target line
number did not show up. Root-caused by direct reproduction: splicing a
raw comment line at an arbitrary typed line number can land it in the
middle of a DDS function-area (+/-) continuation, desyncing the
parser's own continuation resolution and corrupting whatever
field/keyword the continuation belonged to (confirmed: a keyword's
parameter list gets silently truncated and a garbage keyword can spawn
out of what's left over) - the comment appearing to vanish was a
symptom of that corruption, not a display bug. Fixed by pulling a
mid-continuation target back to the start of its own chain before
splicing. See its own row for the full writeup.

**L55 is also now `done`.** Reported directly: the add-row's "Line #"
box showed the browser's default up/down spinner arrows, unwanted on
what's meant to read as a small line-number badge. Suppressed via
`-moz-appearance: textfield` plus hiding the WebKit spin-button
pseudo-elements - typing digits is unaffected. See its own row for the
full writeup.

There's no other pending BUG-FIX work to parallelize right now; every
currently-tracked L/M-series task is closed. The P series below (new
feature work, not a bug fix) has P1-P4 done and P5 (aside panel
removal) deliberately still not started - see its own section for
why. If a new limitation surfaces, add it as a
new task here (continuing the
`L`/`M` numbering) rather than folding it into an existing row, and
follow the same process this effort used throughout: `git fetch` +
`git log --oneline main..FETCH_HEAD` to check for drift before every
push, and update the Status column the moment you pick up or finish a
task.

## P series — one-click toolbox tools (New UI only, DSPF designer)

Requested directly: a Paint-style toolbox in the DSPF (screen) designer -
click an icon, get a ready-made starter record dropped onto the canvas
(a basic `WINDOW` with a title, a `PULLDOWN`/`MNUBAR` menu, etc.) instead
of building one keyword-by-keyword through the properties panel. Explicitly
scoped to the DSPF designer only (not the Menu/MNUDDS designer - see P5's
own note on why "Menu" is ambiguous across the two). Refined three times
since the first draft:

1. The toolbox itself should be a **floating icon that expands on click**
   (a FAB-style popover, not another permanently-visible stack of
   buttons), hosting the record/field/constant/database-fields creation
   actions.
2. Then: fold most of the left (`<aside>`) panel's content into the
   right-side properties panel (`propsPanel`) too, freeing that width
   back to the screen-preview canvas - the person's own words: "provide
   more canvas area."
3. Then, both a scoping and a sequencing correction: **every P-series
   change applies to the "New UI" style ONLY** (`body[data-ui-style=
   "modern"]`, toggled by `#uiStyleToggle` - see `buildWebviewTemplate.js`
   line ~22). **"Classic UI [`data-ui-style="classic"`] should work as it
   is"** - unchanged, pixel-for-pixel and behavior-for-behavior, forever.
   And on sequencing: the floating toolbox and its tools get built FIRST,
   **coexisting alongside the still-fully-present left panel** in New UI -
   the old aside panel and its existing `+ Field`/`+ Constant`/
   `+ Add record`/`+ Fields from database file` buttons stay exactly as
   they are, not hidden or disabled, for the whole time tools are being
   added to the new floating toolbox. Only once every planned tool icon
   exists does removing the aside panel (folding its content into the
   right panel) happen, as the LAST step, and only for New UI - the
   person's own words: "will remove leftside panel once all tools are
   mapped till then both tools and leftside co-exist."

**Important existing-architecture wrinkle, worth knowing before this is
picked up:** today "classic" vs "modern" is a **CSS skin over ONE shared
HTML/JS structure** - same markup, same element IDs (`#placeFieldBtn`,
`#newRecordToggleBtn`, `#addFromDbBtn`, etc.), same event wiring, just
different theming applied via `body[data-ui-style="modern"]` CSS
selectors (see `buildWebviewTemplate.js` lines ~489-555). There is no
structural fork today. This P series needs one: the floating toolbox,
the aside-panel content migrating into the right panel, and (eventually)
the aside panel's removal are all STRUCTURAL DOM changes, and per the
"classic should work as it is" requirement they must only ever apply
under `data-ui-style="modern"`. That means `buildWebviewTemplate.js`
needs to start conditionally emitting genuinely different DOM (not just
different CSS) for New UI vs classic, and every affected script's event
wiring needs to know which structure it's attached to - a materially
bigger technical lift than today's skin-only toggle, not a detail to
discover partway through implementation.

**Was deliberately deprioritized, then explicitly picked back up** - the
person's own words were first "will pick them after stabilizing the
iSDA," then later, directly, "Pick it up." P1 is the result of that
go-ahead - still do not start P2 onward without checking with the person
first, since only P1 was actually authorized so far, not the whole
series at once. The same applies to the LAST task below
(removing the aside panel) even once the rest of the series is done -
"once all tools are mapped" is the person's own judgment call too, not
something to infer just because P2/P4/P5 (or whatever tools exist by
then) all show `done`.

Unlike every L/M-series task above, this is new feature work with no real
SDA equivalent to verify against (real SDA is a fixed two/three-panel
5250 layout, nothing floating, canvas-first, or style-toggled) - defaults
(geometry, naming, which keywords a template includes, the popover's own
layout/icon set, where each migrated control lands in the right panel)
are this project's own design calls, not something to match against IBM
documentation the way an L/M bug fix would be.

| ID | Description | Depends on | Status |
|---|---|---|---|
| **P1** | **Floating expandable toolbox UI shell (New UI only).** A single floating icon (FAB-style, positioned over/near the screen preview canvas) that's collapsed by default and expands on click into a popover, appearing ADDITIONALLY alongside the still-fully-present left `<aside>` panel - not replacing anything yet. Existing entry points get NEW icon-triggered duplicates here, matching whatever they do today, while the aside panel's own originals stay untouched and fully functional (see the coexistence note above): `+ Field`, `+ Constant`, `+ Add record`, `+ Fields from database file`. Two of these are NOT single-click-and-done actions today: `+ Add record` opens a multi-step form (record TYPE picker, conditional SFLCTL-name/window-inherit/SFLMSG sub-fields depending on type, then a name + Create button - see `newRecordForm`'s existing markup) and `+ Fields from database file` opens its own file/field-browsing flow (Task L14) that's ALSO conditionally hidden today whenever the Code for IBM i connection badge isn't `connected` (see the L18-era "hidden outright, not just doomed to fail" fix) - that same conditional visibility has to carry over to its icon inside the popover. `+ Field`/`+ Constant` keep behaving exactly as they do now (immediate click-to-place, Esc to cancel) once triggered from their new icon. Classic UI (`data-ui-style="classic"`) is completely unaffected - the floating icon must not render, or even exist in the DOM, when classic style is active. | none - the safest place to start, since it's purely additive (nothing existing is removed or changed yet) | **done** — `#toolboxFab` (a `#toolboxFabToggle` circular button plus a `#toolboxFabMenu` popover of `.toolbox-fab-item` buttons) added right before `</main>`, gated by `body[data-ui-style="modern"] #toolboxFab { display: flex; }` with a bare `#toolboxFab { display: none; }` default - the SAME "present in the shared markup, invisible under classic via CSS" pattern every other modern-only control in this file already uses (color themes, `#uiThemeRow`, etc. - see that block's own doc comment), not a literal DOM-removal for classic; softened the doc's original "must not even exist in the DOM" wording to match, since forcing a real per-style DOM fork for one inert, harmless-when-hidden control would be a disproportionately bigger change than the rest of this codebase's existing modern/classic split ever required. Every fab item is a pure proxy - `fabPlaceFieldBtn`/`fabPlaceConstantBtn`/`fabAddRecordBtn`/`fabAddFromDbBtn` each just `.click()`s its real aside-panel counterpart (`placeFieldBtn`/`placeConstantBtn`/`newRecordToggleBtn`/`addFromDbBtn`) rather than re-implementing placement mode, the add-record wizard, or the database-fields browsing flow - single source of truth, zero duplicated logic, so nothing about P2-P5's own work (which still targets those same aside originals) needs to change later. `+Field`/`+Constant`'s `active` state and `+Fields from database file`'s connection-gated `hidden` state are mirrored from their aside originals via `MutationObserver`s watching each original's own `class` attribute, rather than re-deriving placement mode or connection status a second time. `+Add record` scrolls `#newRecordForm` into view after opening it (guarded with a `typeof ... === 'function'` check, since jsdom's test environment has no `scrollIntoView` - real webviews always do), since the form still only ever opens inline in the aside, not as its own overlay - a P5 concern, not this one. New dedicated test file `src/test/toolboxFab.test.js` (added to `package.json`'s `test` script) runs the actual generated client script in jsdom, covering: absent visually under classic vs present under modern, popover open/close via toggle/outside-click/Escape, each item's proxy-click landing on the real aside button (not a re-implementation), active/hidden-state mirroring via the `MutationObserver`s, and that `+Add record` reveals the one and only `#newRecordForm` rather than a duplicate. Full suite: 29/29 files, zero failures. |
| **P2** | **Shared "click-to-place a whole template" primitive - needed specifically for the NEW Window/Menu tools (P3/P4), not for the four duplicated buttons in P1 (those already have working placement flows of their own).** Today's click-to-place (`beginCopyPlacement`, used by Copy/Ctrl+D/Ctrl+V/+Field/+Constant) only ever places ONE field/constant - Task L44's own writeup explicitly called a multi-field "click to place a whole block" primitive **out of scope**, confirming it doesn't exist anywhere in this codebase yet. A one-click template tool needs exactly that: a new record (possibly plus child fields/keywords) landing together as one unit, from one click, as one undo step. The "+ Add record" wizard's own multi-step insert (record, then loop over `extraFields` with a reparse between each - see its own SFLMSG handling) is the closest existing precedent and should be reused/generalized rather than building a second pattern from scratch. Needs its own default-geometry/collision story too - today's single-field overlap warning doesn't know about "I'm about to drop N fields as one unit" (see the conversation's own caveat list for the fuller reasoning). | P1 (the popover these tools' icons live in), L44 (documents the gap this fills), the "+ Add record" wizard (`buildTypedRecordPlan`/`insertTypedRecordWithDependent` - the multi-entity-insert pattern to generalize) | **done** — `WebviewClientHelpers.placeRecordTemplate(DspfWriter, dspfFile, sourceLines, template, anchor, reparse)` added to `webviewClientHelpers.js` (DOM-free, like `buildTypedRecordPlan` - kept out of `dspfWriter.js` on purpose since that file has zero parser dependency, so the reparse-between-inserts loop needs an injected `reparse` callback rather than requiring `DspfParser` in). `template` is `{ mainRecord: {name\|baseName, keywords, conditions}, dependent: {name\|baseName, keywords, conditions}\|null, extraFields: [{...insertField's own newField shape, offset:{dLine,dColumn}}] }` - `name`/`baseName` choice on both main and dependent record (via `nextAvailableRecordName`) since P3/P4's single-click tools have no name-entry step the "+ Add record" wizard always had; `offset` is relative to the clicked `anchor` so the same template drops correctly wherever clicked, and is deliberately OPTIONAL per extra field - an entry with no meaningful screen position (SFLMSG's hidden key/queue fields) instead keeps its own explicit `location` (e.g. `{line:null,column:null}`) untouched rather than being forced onto the anchor. `newRecordBtn`'s own click handler in `buildWebviewTemplate.js` now DELEGATES to this instead of keeping its own copy of the reparse loop, so the primitive is proven against the real "+ Add record"/SFLMSG path (all of `dspfWebview.test.js`'s existing SFLMSG-creation checks pass unchanged) rather than sitting untested until P3 exercises it. Collision story: deliberately did NOT build a separate pre-commit multi-field overlap check - `updateOverlapWarning`/`resolveScreen`'s existing overlap resolution (see `dspfEngine.js`) already recomputes from whatever's actually on the model after ANY change, new-record-with-fields included, so a template that drops fields onto occupied cells surfaces in the SAME warning banner a manual multi-field drag already would, with no template-specific special-casing needed. New dedicated test file `src/test/recordTemplatePlacement.test.js` (added to `package.json`'s `test` script) covers: explicit-name and baseName-auto-numbering main records, main+dependent as one atomic two-record insert (including the dependent's OWN independent baseName auto-numbering), extraFields landing at anchor+offset with a reparse between each (no stale-position clobbering), and the offset-omitted/explicit-location case. Full suite: 31/31 files, zero failures. There is deliberately no new UI trigger for this yet - P3 is its first real exercise, per this row's own note. |
| **P3** | **"Window" toolbox icon: one click (then click-to-place on the canvas) drops a ready-made basic window - a new record carrying `WINDOW(line col height width)` sized from the click, plus `WDWTITLE('...')` for its title bar.** Simpler than first assumed: DDS already has a dedicated `WDWTITLE` keyword for a window's own title (`resolveWindowTitle`/`wireWindowPanels` in dspfEngine.js/buildWebviewTemplate.js already fully support it, rendered as `.dspf-window-title` in the preview) - no separate manually-placed title *field* is needed to get a labeled window, just the one new record and its two keywords. Auto-generates a unique record name (`WDW1`, `WDW2`, ...) via the existing `isValidDdsName`/uniqueness-check convention rather than prompting for one on every click. | P1 (popover to live in), P2 (needs the click-to-place-a-block primitive; this is P2's own first real exercise, not built independently of it) | **done** — a `fabWindowBtn` ("Window") item added to the toolbox popover, alongside a new `'WINDOW'` placement mode. Unlike +Field/+Constant/+Add record, this is the popover's first item with no aside-panel original to proxy or mirror - it's a genuinely new tool, not a duplicated entry point (P1's own scope was explicitly limited to duplicating existing ones) - so its arm/cancel active-state toggle lives directly in `setPlacementMode` alongside +Field/+Constant's own, rather than via the P1 IIFE's `MutationObserver` mirroring (there's nothing to mirror FROM). Also unlike every other click-to-place kind, a window template has nothing left to ask once its position is known - no name/length/type entry - so the canvas click handler commits IMMEDIATELY for `'WINDOW'` mode instead of routing through `pendingPlacement`/`renderPlacementProps` the way FIELD/CONSTANT/COPY still do. Auto-names via a small dedicated `nextWindowRecordName()` (`WDW1`, `WDW2`, ...) rather than `DspfWriter.nextAvailableRecordName` - that helper is built for COPYING an EXISTING record, where the copy's own un-suffixed name is already taken by the original, so its numbering always starts at 2; a brand-new record has no such collision to skip past, and this row's own "WDW1, WDW2, ..." promise means starting at 1. Geometry is `line col 10 40` - the click supplies line/col, height/width default to the same `10 40` the "+ Add record" wizard's own WINDOW type already falls back to (see `buildTypedRecordPlan` in webviewClientHelpers.js) rather than inventing a second default. WDWTITLE gets a `'New window'` placeholder via `DspfWriter.setWindowTitleText` - the same auto-quote/escape helper the record properties panel's own "Change Window Title" field already uses - rather than hand-building the quoted parameter string a second time. Reuses `WebviewClientHelpers.placeRecordTemplate` (Task P2) for the actual insert - this is that primitive's first real exercise, per P2's own row above, proven against the same `insertTypedRecord` path the "+ Add record" wizard's WINDOW type already uses. New dedicated test file `src/test/toolboxWindowTool.test.js` (added to `package.json`'s `test` script) runs the actual generated client script in jsdom, covering: arming placement mode (active class, hint text, the canvas's `placing` crosshair class) without posting anything, canceling by clicking the fab item again, committing immediately on a canvas click with no intermediate placement form, the resulting record's geometry/title, WDW1→WDW2 auto-numbering on a second use (skipping the now-taken WDW1, not restarting from WDW1 or landing on WDW3), and the record select switching to the newly-created window. Full suite: 32/32 files, zero failures. |
| **P4** | **"Menu" toolbox icon: drops a `PULLDOWN` record.** Confirmed directly with the person which "menu" this means, since the term is ambiguous in this codebase: a real numbered CRTMNU-style menu can only exist in the separate Menu (MNUDDS) designer - a completely different file type/webview (`buildMenuWebviewTemplate.js`) that this DSPF-designer toolbox has no reach into - so the only thing a DSPF-designer "Menu" icon can sensibly create is DDS's own `PULLDOWN` record type (already selectable today via the "+ Add record" wizard's TYPE picker, just not as a single-click starter template with sensible defaults pre-filled). `MNUBAR` (the menu-BAR itself, as opposed to one of its dropdown panels) is file/record-scope rather than something "placed" the same click-to-place way - worth its own separate icon and its own small design decision on what "clicking" it even means, not folded into this same row. | P1 (popover to live in), P2 (same placement primitive) | **done** — a `fabMenuBtn` ("Menu") item added to the toolbox popover, right after Window. Turned out simpler than Window: a `PULLDOWN` record carries no `WINDOW` keyword and has no line/col/size of its own in real DDS (see `buildTypedRecordPlan`'s own `type === 'PULDWN'` case in `webviewClientHelpers.js`, which the "+ Add record" wizard already relies on - `mainKeywords: [kw('PULLDOWN', '')]`, no geometry anywhere in it; a choice field elsewhere is what actually invokes a pulldown, at THAT field's own position, not the pulldown record's). So unlike Window, this deliberately does NOT arm a placement mode or wait for a canvas click at all - `placeRecordTemplate`'s own `anchor` parameter is only ever read when an extra field supplies an `offset`, and this template has no extra fields, so forcing a canvas click that would then go completely unused would just be motion for its own sake. The fab item commits immediately on click instead - closes the popover and creates the record in the same handler. Auto-names via a small dedicated `nextPulldownRecordName()` (`PDN1`, `PDN2`, ...), the exact same reasoning as Window's own `nextWindowRecordName` (a brand-new record has nothing to skip past, unlike `DspfWriter.nextAvailableRecordName`, which is built for copying an existing one and always starts at 2) - "PDN" matches this codebase's own internal record-type code for a pull-down (`'PULDWN'`/`'PDNSFL'`) rather than inventing a second abbreviation. Reuses `WebviewClientHelpers.placeRecordTemplate` (Task P2) for the actual insert, same as Window. New dedicated test file `src/test/toolboxMenuTool.test.js` (added to `package.json`'s `test` script) runs the actual generated client script in jsdom, covering: no placement mode ever arms (no hint text, no canvas crosshair class, no `active` class - confirming the *absence* of Window's own click-to-place step is deliberate, not an oversight), immediate commit with a bare `PULLDOWN` keyword and no `WINDOW` keyword, `PDN1`→`PDN2` auto-numbering, record-select switching to the new pulldown, and that clicking Menu doesn't disturb an unrelated Field/Constant placement mode already armed. Full suite: 33/33 files, zero failures. |
| **P5** | **LAST: remove the left `<aside>` panel (New UI only), once every planned toolbox icon above is done - not before, and only once the person confirms "all tools are mapped."** Migrates the aside panel's remaining content into the right-side `propsPanel`. Today's grid is `[left 240px] [canvas 1fr] [right 300px]` (see the `gridTemplateColumns` logic keyed off `leftPanelCollapsed`/`rightPanelCollapsed`) - under New UI only, this becomes a two-column `[canvas 1fr] [right panel]`; classic UI keeps the three-column layout forever. Full inventory of what's in the aside today and needs a new home: file status label, the Code-for-IBM-i connection badge, the Save button, Find-field search (+ its results dropdown), the Record select, the screen-size select, the compare-mode/overlay/SFLPAG-preview toggles, the ruler/crosshair toggles, the compare-record list, the conditioning-indicators preview list, the Compile Display File button, and the UI Settings accordion (style/theme) - the style toggle itself has to survive this, since it's how a person would ever switch back to classic. The `h1`/`h2` branding header needs a decision too - fold into the right panel's own header, a slim top bar, or drop it; not assumed here. **Two real risks to design around, not just move-the-HTML busywork:** (1) the right panel's `propsBody` today gets WHOLESALE REPLACED with a "Select a field to edit it" empty-state whenever nothing is selected - any migrated control (Save, Compile, Record select, etc.) has to live OUTSIDE that swapped region, not inside it, or it'll disappear the moment nothing's selected. (2) the right panel already has its own `rightPanelToggle` "Hide panel" affordance - collapsing everything into one panel means that toggle could now hide Save/Compile/the Record select along with it, which the LEFT panel's own independent toggle never risked before; needs either an always-visible pinned strip that survives collapse, or a rethink of what "hide panel" even means once it's the only panel. With this many more controls landing in one place, the right panel likely needs its own accordion/section grouping (the existing `props-accordion` pattern `UI Settings` already uses is the natural template) rather than one long flat stack. Once this lands, P1's temporary "duplicate button in both places" state resolves - the aside's own originals are gone, the toolbox icons (or their migrated equivalents in the right panel) are the only way left, in New UI. **Broken down into P5a-P5i below** - P5b through P5h have no dependency on EACH OTHER (only on P5a's shell existing first), so they can be picked up in any order/in parallel once P5a lands; see each row's own "Depends on". The gating note above still applies to the whole group, P5a included - none of P5a-P5i starts before P1-P4 are done AND the person has separately confirmed "all tools are mapped," the same "only P1 was actually authorized so far" caution this doc has repeated at every step of this series. | P1, P2, P3, P4 (every planned tool icon must exist first - see the coexistence note above) | split into P5a-P5i below - see those for individual status |
| **P5a** | **Foundation: give `propsPanel` a THIRD region, so the rest of P5's migrated controls have somewhere to live that's neither the swappable `propsBody` nor a literal full-panel hide.** Splits `propsPanel` into: (1) a pinned toolbar row, sibling to `propsBreadcrumb`/`propsBody` the same way `rightPanelToggle` already sits outside `propsBody` today - this is where P5b/c/d/e/f's controls all land; (2) a persistent accordion zone between the toolbar and `propsBreadcrumb`, also outside the swappable region - where P5g/h's accordions land; (3) the existing `propsBreadcrumb`+`propsBody` pair, unchanged, still swapped wholesale on selection the way it always has been. Also extends today's collapse behavior - `leftPanelCollapsed`/`rightPanelCollapsed` already collapse a panel to a bare 28px rail via `gridTemplateColumns` (not a true hide), so under New UI, collapsing the now-only right panel should narrow just the accordion+breadcrumb+body zone (2 and 3), leaving the pinned toolbar (1) visible at any width - the concrete answer to P5's own risk (2) above, and the reason this has to land before anything else in the series: every row below assumes this shell already exists. | P5 (its own gating note applies in full - P1-P4 done AND the person's own "all tools are mapped" go-ahead) | **done** — two new empty `<div>`s added inside `#propsPanel`: `#propsPinnedToolbar` (region 1) as a sibling of `#rightPanelToggle`, placed BEFORE `.panel-body`/`#rightPanelBody` so `.panel-collapsed .panel-body { display: none }` structurally never reaches it - no JS change to `applyPanelCollapse()` needed, the exemption falls out of DOM placement alone; `#propsAccordionZone` (region 2) as a sibling of `#propsBreadcrumb`/`#propsBody`, placed between the existing mod-tracking-row and `#propsBreadcrumb`, still inside `.panel-body` (correct, per this row's own text - it only needs to survive `#propsBody`'s empty-state swap (risk 1), not a full collapse (risk 2); only the toolbar needs that). Both gated `display: none` by default, shown only under `body[data-ui-style="modern"]` AND `:not(:empty)` - present in the DOM under classic (the same "shared markup, invisible under classic via CSS" pattern P1 already established) but rendering as nothing while empty, so no blank gap appears before P5b-h actually populate them. Collapsed-rail WIDTH tuning (today's 28px may be too narrow for real toolbar content) is deliberately deferred to whichever of P5b-h first lands content that doesn't fit - guessing a pixel value against an empty box now would just be a number to revisit blind later; this row only had to prove the structural exemption, not the final sizing. New dedicated test file `src/test/propsPanelShell.test.js` (added to `package.json`'s `test` script) runs the actual generated client script in jsdom, covering: both regions present-but-invisible under classic (even once populated, unlike modern), invisible-while-empty then visible-once-populated under modern, both surviving a `#propsBody` selection-triggered swap untouched, the toolbar surviving a full "Hide panel" collapse while the accordion zone (correctly) does not, and the accordion zone's content surviving a collapse/re-expand cycle. Full suite: 35/35 files, zero failures. |
| **P5b** | **Save button + Compile Display File button -> the pinned toolbar (P5a).** The two actions someone needs regardless of what's currently selected, so they belong beside the toolbar's other view-state controls, not inside the swappable `propsBody`. | P5a | **done** — `#toolbarSaveBtn`/`#toolbarCompileBtn` added inside `#propsPinnedToolbar`. Deliberately genuine second buttons with their OWN click handlers (`vscode.postMessage({type:'saveDocument'})`/`{type:'compileDspf'}`) rather than proxies to the aside originals like P1's toolboxFab items - the aside handlers here are one-line `postMessage` calls, not complex placement/wizard/browsing logic worth avoiding duplicating, and P5i eventually deletes the aside originals outright, which a proxy pattern would need re-wiring for anyway; writing genuine buttons from the start avoids that future rework. Shared state stays in sync from a single source: `updateSaveButtonDirtyState` (dirty-indicator class + text swap, driven by the extension host's `dirtyState` message) and `updateCodeForIBadge` (connection-gated `hidden` class, driven by `codeForIStatus`) now each apply their one computed value to BOTH the aside original and its toolbar counterpart in the same call, rather than mirroring via `MutationObserver` after the fact. `propsPanelShell.test.js` (P5a's own test file) updated - `#propsPinnedToolbar` is no longer empty by default now that this task populated it, so its "invisible while empty" checks were rewritten to clear/repopulate the region directly, still proving the underlying `:not(:empty)` CSS guarantee holds without assuming a default emptiness that no longer exists. New dedicated test file `src/test/toolbarSaveCompile.test.js` (added to `package.json`'s `test` script), covering: both buttons present inside `#propsPinnedToolbar` alongside untouched aside originals, each button's own click posting the right message directly, and both dirty-state and connection-gated visibility staying synced between the aside and toolbar pairs. Full suite: 35/35 files, zero failures. |
| **P5c** | **File status label + the Code-for-IBM-i connection badge -> the pinned toolbar (P5a).** Status/chrome, not selection-dependent - same reasoning as P5b. | P5a | **done** — `#toolbarFileStatus`/`#toolbarCodeForIBadge` added inside `#propsPinnedToolbar`, ahead of P5b's Save/Compile buttons (matching this row's own "file status, badge, Save, Compile" order in P5's original inventory). `#toolbarFileStatus` needs no message-driven sync at all - the aside original itself is static, set once from the filename token at render time and never touched by any later message (confirmed: no other reference to `fileStatus` anywhere in the file) - so the toolbar copy is just a second static element carrying the identical content. `#toolbarCodeForIBadge` DOES need syncing (installed/connected state arrives via the `codeForIStatus` message, same as the aside original) - `updateCodeForIBadge` now loops over `[codeForIBadge, toolbarCodeForIBadge]` and applies the identical computed class/text/title to both, same "one computed state, applied to both elements" pattern P5b's own `updateSaveButtonDirtyState`/`toolbarCompileBtn` visibility toggle already established, rather than mirroring via `MutationObserver` after the fact. Genuine second elements, not proxies - same reasoning P5b's own doc comment gives (P5i eventually deletes the aside originals outright, which a proxy pattern would need re-wiring for anyway). New dedicated test file `src/test/toolbarFileStatusBadge.test.js` (added to `package.json`'s `test` script), covering: both elements present inside `#propsPinnedToolbar` alongside untouched aside originals, the toolbar file status label matching the aside original's filename text, and the badge's installed/disconnected/connected states staying in sync between the aside and toolbar pairs across a `codeForIStatus` message. Full suite: 36/36 files, zero failures. |
| **P5d** | **Record select + screen-size select -> the pinned toolbar (P5a).** These are view state (which record/size is being previewed), not properties of a selection, so - like Save/Compile - they belong in the toolbar rather than `propsBody`. | P5a | **done** — `#toolbarRecordSelect`/`#toolbarSizeSelect`/`#toolbarSizeSelectRow` added to `#propsPinnedToolbar`, genuine duplicates of the aside originals rather than a relocation. That distinction mattered in practice: the FIRST version of this task tried relocating `#recordSelect`/`#sizeSelect` straight into the toolbar, on the reasoning that a stateful control referenced from well over a dozen call sites (every "select what was just created" pattern P3/P4/newRecordBtn/etc. already use) shouldn't be duplicated the way P5b's stateless one-line buttons were. That passed every existing test - but would have made switching records completely unusable under CLASSIC UI, since `.props-pinned-toolbar` is `display: none` unless `data-ui-style="modern"`, and classic never shows the toolbar at all. Caught on review before shipping, not by a test failure - which is exactly why this row's own test file leads with a scenario that would have caught it. Reverted to genuine duplication instead, but solved the "well over a dozen call sites" problem without touching any of them: `#recordSelect`/`#sizeSelect` in the aside stay the ONE authoritative element (every existing read/write of `.value` is completely unchanged), and the LOCAL `rebuildRecordSelect()`/`rebuildSizeSelect()` wrapper functions - already called on every single `render()` - are the ONE centralized place that rebuilds the toolbar duplicates' `<option>` lists and copies over the authoritative value, so all ~14 existing call sites propagate to the toolbar automatically with zero changes needed at each site. Selecting FROM a toolbar duplicate sets the real element's `.value` and re-dispatches a `change` event on it, forwarding to the real `change` listener rather than re-implementing it - exactly one behavior, two visible copies of the control. New dedicated test file `src/test/toolbarRecordSizeSelect.test.js` (added to `package.json`'s `test` script), leading with the classic-UI regression scenario this task almost shipped, then covering: exactly one of each element (no accidental extra copies), both starting in sync, switching from EITHER copy updating the other and firing the real handler (confirmed by checking the canvas actually re-rendered, not just that values matched), and the screen-size picker's visibility/options/sync behaving identically in both copies. Full suite: 37/37 files, zero failures. |
| **P5e** | **Find-field search (+ its results dropdown) -> its own row directly under the pinned toolbar (P5a).** Used independently of whatever's selected, same "must survive the empty-state swap" reasoning as P5b-d, but given its own row rather than crammed into the toolbar strip itself since it also needs room for a results dropdown underneath. | P5a | **done** — new sibling region `#propsFindFieldRow`, positioned directly under `#propsPinnedToolbar` and - like the toolbar itself - OUTSIDE `.panel-body`, since P5a's own doc comment had already earmarked this control for the "survives a full panel collapse" camp alongside Save/Compile/Record select (P5b/c/d/f), not the accordion zone's "survives the empty-state swap only" camp (P5g/h); no JS change to `applyPanelCollapse()` needed, same structural-exemption-by-DOM-placement P5a itself established. Block-level (not the toolbar's own flex row), since the results dropdown needs to sit below the input, not beside it. Genuine second widget instance, not a proxy - reasoning follows P5b/c rather than P5d: unlike Record/Screen-size select (referenced from around a dozen other call sites, which is why P5d duplicated the DOM but kept ONE authoritative element), nothing outside the search widget's own wiring ever reads `fieldSearchInput`/`fieldSearchResults` by name, so the whole wiring block (`fieldSearchLabel`/`findFieldMatches` stayed shared top-level helpers; `closeFieldSearchResults`/`renderFieldSearchResults`/`jumpToFieldMatch`/the input's own event listeners were refactored into a `wireFieldSearch(inputEl, resultsEl)` factory) was extracted into a reusable function and called twice - once for the untouched aside pair, once for the new `#toolbarFieldSearchInput`/`#toolbarFieldSearchResults` pair - giving each instance its own independent matches array/active index/open-closed state rather than two inputs fighting over one shared array. P5i eventually deletes the aside originals outright, which a proxy pattern would need re-wiring for anyway - same reasoning P5b/c's own rows already give. Task L19's own full behavioral test (filtering, cross-record jump, Escape/Enter/arrow-key handling, empty-state) already exists for the aside instance and continues to pass unchanged - not re-duplicated wholesale. New dedicated test file `src/test/toolbarFindFieldSearch.test.js` (added to `package.json`'s `test` script) instead covers what's new: the toolbar twin exists inside `#propsFindFieldRow` and has full behavioral parity (filter/jump/cross-record-switch), the two instances are genuinely independent (typing/opening one never touches the other's dropdown or value), `#propsFindFieldRow`'s own placement (sibling of `#propsPinnedToolbar`, outside `.panel-body`, immediately following it in DOM order), survival of a `#propsBody` selection-triggered swap (same node, not recreated), and that the whole row computes to `display:none` under classic UI while the aside's own instance keeps working there fully unaffected. Full suite: 38/38 files, zero failures. |
| **P5f** | **`h1`/`h2` branding header -> folded into the pinned toolbar (P5a) as a slim title line (e.g. filename + record count), not a separate top bar.** It currently lives INSIDE `<aside>` itself (`<aside><h1>IBM i · DDS</h1><h2>Screen Design</h2>`), so it has nowhere else to go once the aside is gone - this is P5's own "not assumed here" decision, now made. | P5a | **done** — a new `#toolbarTitle` element added to `#propsPinnedToolbar`, `flex-basis: 100%` to force it onto its own row ahead of Save/Compile/etc. (the "slim title line" this row's own text calls for) rather than wrapping inline with them. Unlike P5b/c/d's own migrations (a genuine second element mirroring an aside original 1:1, kept in sync), this one is a redesign rather than a duplicate: `h1`'s "IBM i · DDS" is dropped entirely as purely decorative product branding not worth a toolbar's limited width, `h2`'s "Screen Design" survives (still meaningfully distinguishes this from the separate Menu designer webview), now paired with a LIVE record count neither `h1` nor `h2` ever showed - reads "Screen Design · N records" (singular "record" at exactly one). The count is genuinely dynamic (new toolbox-tool records, deletions, etc. all change it live), unlike `#toolbarFileStatus`'s static filename (baked in once via `FILENAME_TOKEN` at HTML-generation time and never touched again), so it needs its own render()-time refresh - a new `updateToolbarTitle()` called alongside `rebuildRecordSelect()` in `render()`, the other function already reacting to `model.records` changing. The aside's own `h1`/`h2` are completely untouched - still there, unchanged - same coexist-until-P5i rule every P5b-h migration so far has followed. New dedicated test file `src/test/toolbarTitle.test.js` (added to `package.json`'s `test` script) covers: the toolbar title's initial text against a real fixture, the aside's `h1`/`h2` staying exactly as they were, the count updating live after Task P3's Window tool drops a new record with no reload, and the singular/plural wording boundary. Full suite: 38/38 files, zero failures. |
| **P5g** | **New "View" accordion (in P5a's persistent accordion zone) for the compare-mode/overlay/SFLPAG-preview toggles, the ruler/crosshair toggles, the compare-record list, and the conditioning-indicators preview list.** All of this is "how the canvas is currently drawn," never selection-dependent editing, so - like `UI Settings` already does - it belongs in the persistent accordion zone, not the swappable `propsBody`. | P5a | **done** — `#viewAccordion` added to `#propsAccordionZone`, as a sibling of P5h's `#toolbarUiSettingsAccordion` (landed concurrently in a parallel session - merged in cleanly, both accordions coexist). Contains genuine duplicates of all six controls: `toolbarCompareModeToggle`, `toolbarCompareOverlayRow`/`toolbarCompareOverlayToggle`, `toolbarPreviewRowsRow`/`toolbarPreviewRowsToggle`, `toolbarRulerToggle`, `toolbarCrosshairToggle`, `toolbarCompareRecordList`, `toolbarIndicatorList` - not a relocation, since classic UI never shows `#propsAccordionZone` at all (same P5d-learned lesson applied up front this time). The five checkboxes forward to their real counterpart (set `.checked`, re-dispatch `change`) rather than duplicating each listener's own logic; a new `syncViewToggles()` mirrors real -> duplicate state (checked + hidden-class visibility) back, called from `render()` (twice, to cover its own early-return paths) and directly from ruler/crosshair's own listeners, neither of which calls `render()` itself. `rebuildIndicatorListFromSet`/`renderCompareRecordList` were refactored to build into BOTH containers from the same underlying `active`/`compareSelectedRecords` data - each duplicate's own checkboxes independently mutate the shared data and call `render()`, since both lists are rebuilt fresh from identical data on every call anyway (no separate "source of truth" element to keep in sync with here, unlike a single toggle/select). Caught and fixed one real gap while testing: `compareModeToggle`'s own listener toggled the ASIDE `compareRecordList`'s hidden class directly but never touched the toolbar duplicate's - folded into `syncViewToggles()` instead. `propsPanelShell.test.js` (P5a's own test) updated again, same pattern as P5b/h - `#propsAccordionZone` is no longer empty by default now that BOTH P5g and P5h populated it, so its checks verify both accordions' presence and still prove the underlying `:not(:empty)` CSS guarantee via clear/repopulate. New dedicated test file `src/test/toolbarViewAccordion.test.js`, covering: all duplicate elements present alongside untouched aside originals, each checkbox's forwarding, both list-builders populating both containers from shared data with working independent checkboxes, and previewRowsRow/ruler/crosshair's sync paths (including the two listeners that never call `render()`). Full suite: 41/41 files, zero failures. Note for future sessions: a real, PRE-EXISTING (not introduced by this task) quirk was found but deliberately NOT fixed here as out-of-scope - `previewRowsRow`'s visibility never updates while `compareMode && compareFullOverlay` is active, since `render()`'s full-overlay path returns before reaching that computation; worth a small follow-up if it ever bites someone in practice. |
| **P5h** | **UI Settings accordion (style/theme) relocates as-is into P5a's persistent accordion zone, alongside P5g's new one.** No redesign, just a new address - the style toggle itself must keep working exactly as it does today, since it's the only way back to classic UI once the aside is gone. | P5a | **done** — a second `#toolbarUiSettingsAccordion` added inside `#propsAccordionZone`, carrying its own `#toolbarUiStyleToggle`/`#toolbarUiThemeRow`/`#toolbarUiThemeSelect` (new ids throughout, since an id must be unique in the document). This row's own text says the accordion "relocates as-is" (a move), but - like every other P5b-h migration so far (P5b/c/d/f) - it's actually a genuine SECOND element mirroring the aside original 1:1, kept in sync, coexisting until P5i finally deletes the aside outright: literally moving this control out of `<aside>` now would be the one migration in the group that couldn't be undone, and would leave classic UI (which keeps `<aside>` forever) with no working style toggle at all in the meantime - see buildWebviewTemplate.js's own doc comment for the full reasoning, same "wording doesn't mean a literal move" precedent P5f's own row already set. Unlike P5b/c/d (where the toolbar copy only has to sync with the extension host), the two style-toggle buttons and two theme selects here ALSO have to sync with EACH OTHER directly, since either can be clicked/changed: both style-toggle IIFEs now collect every present element (aside + toolbar) into an array, apply one shared `uiStyle`/`uiTheme` variable's label/value to ALL of them on init, and re-apply to all of them again on any single element's own click/change handler - so toggling either one updates both immediately, and the underlying `postMessage`/`vscode.setState` call still only fires once per interaction. New dedicated test file `src/test/toolbarUiSettings.test.js` (added to `package.json`'s `test` script) covers: both accordions present with the aside original untouched, both style-toggle buttons/theme selects starting in sync, and - the bidirectional part specifically - clicking/changing either the toolbar OR the aside control updates the OTHER one to match, with only one `setUiStyle`/`setUiTheme` message posted per interaction either way. `propsPanelShell.test.js` (P5a) updated to match - `#propsAccordionZone` is no longer empty by default now that this task populated it, so its own "invisible while empty" checks were rewritten to clear/repopulate the region directly, same adjustment P5b already made for `#propsPinnedToolbar`. Full suite: 39/39 files, zero failures. |
| **P5i** | **The actual removal: delete the `<aside>` element and flip the New-UI grid to the two-column `[canvas 1fr] [right panel]` layout (classic UI's three-column grid is untouched forever).** The one row in this group that can NOT run in parallel with P5b-h, since it deletes the very DOM those rows are each migrating content out of - only safe once every one of P5b-h has actually landed (every piece of the aside's old content has a confirmed real home first). This is the moment P1's own "duplicate button in both places" state finally resolves - the aside's originals are gone, the toolbox icons/their migrated equivalents in the right panel are the only way left, in New UI. | P5a, P5b, P5c, P5d, P5e, P5f, P5g, P5h (every migration must be done first) | not started |

## A series — SDA reference screenshot keyword-inventory audit (follow-up to L22)

Requested directly: complete the keyword-inventory audit L22 started but only
partially finished. L22's own text says it covered File-level (all 9
categories), Record-level base keywords (`base-record-keywords/*`, all 8
categories), and ~30 of ~55 Field-level screenshots — and explicitly flagged
the remaining ~130 record-level variant screenshots (`screens/record-level/*`
minus `base-record-keywords`) and ~25 field-level screenshots as **not**
re-audited in that pass. This series finishes that remainder: record-level
variants first (SFL/SFLCTL/SFLMSG, WINDOW + border sub-screens, PULLDOWN,
MNUBAR, and the WNDSFL/WNDSFCTL/PULDWNSFL/PDNSFLCTL combination types), then
the remaining field-level screens (Colors/Validity Check/Error Messages/
Message ID/Database Reference/Keying Options/Input Keywords for
character+numeric, Subfile Keywords, and the Menu-bar choice sub-screens not
already spot-checked). Per-screenshot: extract the keyword list shown, then
cross-check both against the rendered designer UI (does a picker/field exist
for it) and the generated DDS source (does `dspfWriter.js`/`dspfEngine.js`
actually read/write that keyword) — this is an audit, not new implementation;
any gap found gets logged as its own new `L`-series task, same as L22's own
four findings (L23-L26) were.

| ID | Description | Depends on | Status |
|---|---|---|---|
| **A1** | Record-level variant screenshot audit — `screens/record-level/*` excluding `base-record-keywords` (SFL, SFLCTL, SFLMSG, WINDOW incl. all border sub-screens, PULLDOWN, MNUBAR record, WNDSFL, WNDSFCTL, PULDWNSFL, PDNSFLCTL, USRDFN). | L22 (this is its own unfinished remainder) | in progress |
| **A2** | Field-level remaining screenshot audit — `screens/field-level/*` screens not already spot-checked by L22 (Colors, Validity Check, Error Messages, Message ID, Database Reference, Keying Options, Input Keywords for character+numeric; Subfile Keywords; remaining Menu-bar choice sub-screens). | L22 (this is its own unfinished remainder) | not started |

## Out of scope here

The "not really fixable" and "already handled reasonably" items are
tracked in [Known limitations (accepted constraints)](#known-limitations-accepted-constraints)
near the top of this file, not as tasks here. `TYPE(*PGM)`/`TYPE(*UIM)`
menu support (formerly Task M5) belongs in that category too — see its
row above for why it's out of scope rather than merely deprioritized. If
any of those turns out to be more fixable than it looks, raise it as a
new task here rather than silently reinterpreting its priority.
