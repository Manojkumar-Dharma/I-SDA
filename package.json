{
  "name": "i-sda",
  "displayName": "iSDA",
  "description": "Interactive visual designer for IBM i DDS display files (DSPF) — preview, click, drag, and edit fields with changes written straight back into the DDS source.",
  "version": "0.8.0",
  "publisher": "Manojkumar-IBMi",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Manojkumar-Dharma/I-SDA.git"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:dds.dspf"
  ],
  "contributes": {
    "customEditors": [
      {
        "viewType": "dspfDesigner.editor",
        "displayName": "iSDA Screen Design",
        "selector": [
          { "filenamePattern": "*.dspf" },
          { "filenamePattern": "*.DSPF" },
          { "filenamePattern": "*.dspf38" }
        ],
        "priority": "option"
      }
    ],
    "commands": [
      {
        "command": "dspfDesigner.openPreview",
        "title": "iSDA: Open Screen Design Preview",
        "icon": "$(open-preview)"
      },
      {
        "command": "dspfDesigner.createNewDspf",
        "title": "iSDA: Create New Display File",
        "icon": "$(file-add)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "dspfDesigner.openPreview",
          "when": "resourceExtname =~ /\\.(dspf|DSPF|dspf38)$/ || resourceLangId == dds.dspf || resourceScheme == member || resourceScheme == streamfile",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "dspfDesigner.createNewDspf",
          "when": "explorerResourceIsFolder",
          "group": "2_workspace"
        }
      ],
      "commandPalette": [
        {
          "command": "dspfDesigner.createNewDspf"
        }
      ]
    }
  },
  "scripts": {
    "test": "node src/test/extension.test.js",
    "build:webview-assets": "node src/fixtures/generateFixture.js && esbuild src/browserEntry.ts --bundle --format=iife --global-name=DspfParser --outfile=dist/dspfParser.browser.js && node src/buildWebviewTemplate.js",
    "compile": "npm run build:webview-assets && tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2"
  }
}
